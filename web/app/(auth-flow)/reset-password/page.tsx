import { ResetPasswordScreen } from "@/components/auth-flow/screens";

export const metadata = { title: "Wynos — ตั้งรหัสผ่านใหม่" };

export default function ResetPasswordPage() {
  return <ResetPasswordScreen />;
}
