import { ForgotPasswordScreen } from "@/components/auth-flow/screens";

export const metadata = { title: "Wynos — ลืมรหัสผ่าน" };

export default function ForgotPasswordPage() {
  return <ForgotPasswordScreen />;
}
