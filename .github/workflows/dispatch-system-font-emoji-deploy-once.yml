name: Archived system font emoji production deploy (2026-09-13)

# Archived after deploy #151 completed successfully.
# Manual-only and intentionally performs no deployment.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived system font/emoji production deploy dispatcher; no deployment action."
