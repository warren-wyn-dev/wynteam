name: Dispatch system font emoji production deploy once

on:
  push:
    branches:
      - ops/dispatch-system-font-emoji-deploy
    paths:
      - .github/workflows/dispatch-system-font-emoji-deploy-once.yml

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch production deploy
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run deploy-web.yml --repo warren-wyn-dev/wynteam --ref main
