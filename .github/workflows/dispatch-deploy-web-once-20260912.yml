name: Archived production web verification (2026-09-12)

# Inert audit marker for the one-shot Beta4 DM push deployment + production
# smoke verification. It never deploys and never runs automatically.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-shot production verification; no deployment action."
