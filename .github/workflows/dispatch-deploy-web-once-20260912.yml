name: Archived production stale-cache hotfix verification (2026-09-12)

# Archived after the production stale-cache hotfix deploy and external smoke
# completed successfully. This workflow is intentionally manual-only and
# performs no deployment or mutation.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived production stale-cache verification; no deployment action."
