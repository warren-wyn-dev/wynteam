name: Archived one-shot production dispatcher (2026-09-12)

# This workflow is intentionally inert. It was used once to dispatch the
# canonical manual-only deploy-web.yml workflow for the Beta4 DM push fix.
# Kept as a harmless audit marker because repository file deletion is not
# available through the current integration. It must not deploy anything.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-shot dispatcher; no deployment action."
