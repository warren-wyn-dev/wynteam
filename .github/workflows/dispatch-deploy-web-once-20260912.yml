name: Archived production DM reliability verification (2026-09-12)

# Inert audit marker for the one-shot Beta4 DM notification deployment and
# production smoke checks. It never deploys and never runs automatically.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived production verification; no deployment action."
