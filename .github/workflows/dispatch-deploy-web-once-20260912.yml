name: Archived production system-font verification (2026-09-13)

# Archived after the production deploy and live smoke completed successfully.
# This workflow is intentionally manual-only and performs no deployment or
# mutation.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived production system-font verification; no deployment action."
