name: Archived duplicate Web Push production verification (2026-09-12)

# Archived after the production deploy completed successfully. This workflow is
# intentionally manual-only and performs no deployment action.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived production verification; no deployment action."
