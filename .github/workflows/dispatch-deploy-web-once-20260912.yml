name: Archived production chat UI verification (2026-09-12)

# Archived after the production smoke completed successfully. This workflow is
# intentionally manual-only and performs no deployment or production mutation.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived production verification; no deployment action."
