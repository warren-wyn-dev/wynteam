name: Archived system font emoji production smoke (2026-09-13)

# Archived after the live production smoke test passed.
# Manual-only and intentionally performs no network check.
on:
  workflow_dispatch: {}

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived system font/emoji production smoke test; no check action."
