/// Shared placeholder message for every ZOKY-001 action that isn't wired
/// up yet (Search, Cart, Orders, category tap, Add to Cart, Buy Now,
/// Follow Store) -- same string `ClubPage` already uses for its own
/// not-yet-built actions, reused here instead of inventing new wording.
const zokyComingSoonMessage = 'ฟีเจอร์นี้จะมาเร็ว ๆ นี้';
