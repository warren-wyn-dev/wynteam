# Product Task — WYN-041

Status: approved (Independent QA PASS 2026-08-23 — ดูหัวข้อ "Independent QA" ท้ายไฟล์ — ส่งต่อ AI Deploy & DevOps)
Owner: AI Product Manager

Feature: Trending Engine v2 — wire view count เข้าสูตรจัดอันดับ + Anti-Manipulation Scoring (ต่อยอดจาก WYN-018)

Goal: ปิดช่องว่าง 2 จุดที่ WYN-040 (Discovery Page) เจอแล้วยกให้เป็นสโคปของ task นี้ตามชื่อที่ระบุไว้ใน roadmap (`.wyn/docs/product/wyn-v1.0.0-roadmap.md`, Phase 4) ตั้งแต่ต้น: (1) ตัวเลข View count ที่ WYN-038 สร้างไว้ (`drop_view_count()`, unique-viewer/rate-limit/self-view-exclusion ครบแล้ว) **ยังไม่เคยถูกใช้ในสูตรจัดอันดับใดๆ ในระบบเลย** ทั้งที่มีอยู่จริงและ trustworthy พอจะใช้ (2) ระบบจัดอันดับ ("สำหรับคุณ" WYN-018 / "กำลังนิยม" WYN-017 / Discovery's Trending Now-Hashtags WYN-040) **ไม่มีกลไกป้องกันการปั่นอันดับ (anti-manipulation) ใดๆ เลย** — บัญชีที่โดน moderator สั่ง restrict/suspend/ban แล้วจริง (WYN-029/030) เนื้อหาเก่าของบัญชีนั้นยังมีสิทธิ์ถูกจัดอันดับขึ้น Trending/สำหรับคุณ ได้เหมือนบัญชีปกติทุกประการ ทั้งที่ควรถูกกันออกจาก "การจัดอันดับเชิงบวก" ระหว่างที่ยังโดน sanction อยู่

Target User: ผู้ใช้ WYN Social ทุกคนที่เห็นผลจาก "สำหรับคุณ"/"กำลังนิยม"/Discovery — ได้รับผลลัพธ์ที่สะท้อนความนิยมจริง (รวม View ด้วย ไม่ใช่แค่ Like/Comment) และไม่เห็นเนื้อหาจากบัญชีที่กำลังโดน sanction ถูกดันขึ้นมาให้เด่นกว่าที่ควร

Problem: ยืนยันแล้วจากการอ่านโค้ดจริง (`app/lib/features/home/data/home_ranking.dart`, `home_repository.dart`) —
1. `rankingScore()` (WYN-018, ใช้ใน `fetchRankedFeed()` สำหรับ "สำหรับคุณ") สูตรปัจจุบันคือ `recencyScore + engagementScore + followingBoost` โดย `engagementScore = likeCount * 2 + commentCount * 3` **ไม่มี viewCount อยู่ในสูตรเลยแม้แต่พจน์เดียว** ทั้งที่ `HomeFeedItem.viewCount` มีอยู่แล้ว (WYN-038 wire เข้า `home_feed`/`saved_feed` view เรียบร้อยสำหรับ Drop)
2. `fetchTrending()` (ใช้ใน Home's "กำลังนิยม" row และ Discovery's Trending Now — WYN-040 — รวมถึงเป็น candidate source ของ Discovery's Trending Hashtags ด้วย) เรียงจาก `likeCount + commentCount` ล้วนๆ เช่นกัน ไม่มี viewCount
3. **ไม่มีจุดไหนในทั้งสองสูตรตรวจสอบสถานะ moderation ของผู้เขียนเลย** — Drop/Pop จากบัญชีที่กำลังโดน restrict/suspend/ban (`moderation_actions`, ยัง active อยู่จริงตาม `internal.is_posting_blocked()`) มีสิทธิ์ขึ้น Trending/สำหรับคุณ ได้เหมือนบัญชีปกติทันทีที่ engagement ตัวเลขสูงพอ ทั้งที่ตรรกะที่ถูกต้องคือ: การถูก sanction ไม่ควรตัดสิทธิ์การเข้าถึงเนื้อหาเก่าของผู้ใช้ทั่วไป (Home/Search/Following feed ยังเห็นได้ปกติตาม RLS เดิม ไม่แตะ) **แต่ไม่ควรได้รับการจัดอันดับเชิงบวกเพิ่มเติมจากระบบระหว่างที่ยังโดน sanction อยู่**

Requirements:

**1. Wire `viewCount` เข้าสูตรจัดอันดับทั้งสองจุด — เฉพาะ Drop เท่านั้น (Pop ไม่รวมในรอบนี้ ดูเหตุผลข้อ 2)**
- แก้ `rankingScore()` (WYN-018) เพิ่มพจน์ `viewScore` ใหม่เข้าไปในสูตร (`recencyScore + engagementScore + viewScore + followingBoost`) — น้ำหนักต้อง **น้อยกว่า** like/comment มาก เพราะจำนวน View ตามธรรมชาติจะเยอะกว่า Like/Comment หลายเท่าตัวเสมอ (คนเปิดดูมากกว่าคนกด Like/Comment เป็นปกติของทุกแพลตฟอร์ม) ถ้าให้น้ำหนักเท่าๆ กัน View จะกลืนสูตรทั้งหมดทันทีจน Like/Comment แทบไม่มีผล — เสนอเริ่มที่ `viewCount * 0.1` (Design/Coding ปรับตัวเลขจริงได้ ต้องมี comment อธิบายเหตุผลไว้ชัดเจนแบบเดียวกับที่ WYN-038 ทำกับ rate-limit numbers ว่าเป็นค่าที่ Product กำหนดเองชั่วคราว ไม่มีข้อมูล traffic จริงมาอ้างอิง เพราะแอปยังไม่ deploy จริง)
- แก้ `fetchTrending()` เรียงจาก `likeCount * 2 + commentCount * 3 + viewCount * 0.1` แทนของเดิม (`likeCount + commentCount`) — ใช้น้ำหนัก Like/Comment เดิมของ `rankingScore()` ให้สอดคล้องกันทั้งสองสูตร (ตอนนี้ `fetchTrending()` ใช้ `likeCount + commentCount` แบบ 1:1 ซึ่งต่างจาก `rankingScore()`'s `2:3` อยู่แล้ว — ถือโอกาสนี้ปรับให้สอดคล้องกันเลยเพราะทั้งสองสูตรควรสื่อความหมาย "engagement" แบบเดียวกัน ไม่ใช่คนละนิยาม)
- **Pop's engagement score ไม่เปลี่ยน** (ยังคง `likeCount * 2 + commentCount * 3` ล้วนๆ ไม่มี viewCount) — เหตุผลอยู่ข้อ 2 ด้านล่าง
- ทั้งสองจุดที่แก้เป็น client-side sort ที่มีอยู่แล้ว (ไม่ใช่ SQL ใหม่) — แก้แค่สูตรคำนวณ ไม่แตะ query/candidate-fetch logic เดิม

**2. Pop's view count ไม่นำมาใช้ในสูตรจัดอันดับรอบนี้ — เพราะยังไม่ trustworthy พอสำหรับ "anti-manipulation" engine**
- ยืนยันจากโค้ดจริง (`increment_pop_view_count()`, WYN-006): Pop's view count เป็น **counter ธรรมดาที่ `+1` ทุกครั้งที่เรียก ไม่มี unique-viewer dedup/self-view exclusion/rate-limit ระดับ DB เลยแม้แต่อย่างเดียว** (มีแค่ client-side flag กันเรียกซ้ำ "ภายในเซสชันเดียว" เท่านั้น ตามที่ WYN-038's Product spec เคยบันทึกไว้แล้วว่าเป็น Minor gap ที่ยอมรับไว้ตอนนั้น) — ถ้านำตัวเลขนี้ไปใส่ในสูตรจัดอันดับของ task ที่ชื่อ "anti-manipulation scoring" ตรงๆ จะกลายเป็นการเปิดช่องโหว่ใหม่ทันที: เจ้าของ Pop เปิดคลิปตัวเองซ้ำๆ (หรือใช้หลายบัญชีเปิด) ดันตัวเลข View แล้วได้อันดับ Trending สูงขึ้นได้ง่ายๆ โดยไม่ต้องมี engagement จริงเลย — ขัดกับวัตถุประสงค์หลักของ task นี้เอง
- **การแก้ Pop's view counting ให้ trustworthy เท่า Drop (สร้างตาราง `pop_views` + `record_pop_view()` RPC มิเรอร์ `drop_views`/`record_drop_view()` ของ WYN-038 เป๊ะ) ไม่อยู่ในสโคปของ task นี้** — เป็นงานแยกที่ควรเป็น task ใหม่ต่างหาก (เสนอชื่อใน Recommendation ด้านล่าง) เพราะมีขนาดงานเทียบเท่า WYN-038 เต็มรูปแบบ (schema ใหม่ + RPC ใหม่ + regression test ใหม่) ไม่ใช่แค่ "wire ตัวเลขที่มีอยู่แล้วเข้าสูตร" แบบ Drop

**3. Anti-manipulation: กันเนื้อหาจากบัญชีที่กำลังโดน moderation sanction ไม่ให้ได้รับการจัดอันดับเชิงบวก**
- นิยาม "กำลังโดน sanction" ใช้ตรรกะเดียวกับ `internal.is_posting_blocked(p_user_id)` ที่มีอยู่แล้ว (WYN-029/030): restrict/suspend ที่ยัง `expires_at > now()` หรือ ban ถาวร และไม่ถูก `overturned_at` จากการอุทธรณ์สำเร็จ (WYN-030) — reuse ฟังก์ชันเดิมตรงๆ ไม่ต้องเขียน logic ใหม่ซ้ำ
- **ไม่ใช่การซ่อนเนื้อหา** — เนื้อหาเก่าของบัญชีที่โดน sanction ยังเห็นได้ปกติทุกจุดเหมือนเดิม (Home chronological feed, Following feed, Search, Hashtag feed, โปรไฟล์เจ้าของเอง) เพราะ RLS ของ `drops`/`pops` ไม่เคยบล็อกการอ่าน (restrict/suspend/ban บล็อกแค่การ "สร้างเนื้อหาใหม่" ที่ INSERT policy ตาม WYN-029/030 ไม่เคยบล็อกการมองเห็น) — task นี้เปลี่ยนแค่ **"ไม่ได้รับการจัดอันดับเชิงบวกเพิ่มเติมจากอัลกอริทึม"** เท่านั้น สองเรื่องนี้แยกกันชัดเจน ห้ามปนกัน
- ขอบเขต: ตัดออกจาก **"กำลังนิยม" (Home Trending row + Discovery Trending Now)** และ **"สำหรับคุณ" (WYN-018 ranked feed)** เท่านั้น — Discovery's Trending Hashtags ใช้ candidate window เดียวกับ "กำลังนิยม" อยู่แล้ว (`fetchTrending()`) ตาม WYN-040 design จึงได้ผลลัพธ์เดียวกันโดยอัตโนมัติถ้าแก้ที่จุดเดียว (`fetchTrending()`) ไม่ต้องแก้ซ้ำที่ `DiscoveryRepository`
- **เทคนิคที่ต้องระวัง (มิเรอร์ Risk เดียวกับที่ WYN-040 เคยเจอ)**: `moderation_actions` ไม่มี SELECT policy เปิดให้บุคคลทั่วไปเห็น ("ordinary users have no SELECT policy on..." ตามคอมเมนต์ในสคีมาเอง) และ `internal.is_posting_blocked()` อยู่ใน schema `internal` (ไม่ถูก expose เป็น REST endpoint ตรงๆ อยู่แล้ว) — การกรอง candidate list ของ Trending/สำหรับคุณ (ซึ่งทำแบบ fetch-candidate-แล้ว-sort-ฝั่ง-client เหมือนเดิม) ต้องมี **SECURITY DEFINER RPC ใหม่** รับ list ของ author id ที่เป็น candidate แล้วคืนกลับมาว่า "ตัวไหนบ้างที่ active sanction อยู่" — **คืนแค่ id ที่ควรถูกกันออก ไม่คืนรายละเอียด action_type/reason/reviewer/expiry ใดๆ เลย** (มิเรอร์ anti-gaming return-shape เดียวกับ `rising_profiles()`/`suggested_users()` ของ WYN-040 — ป้องกันไม่ให้ third-party ใช้ RPC นี้เป็นเครื่องมือสอดส่อง "ใครโดน sanction บ้าง" ซึ่งเป็นข้อมูลอ่อนไหวที่ WYN-029/030 ตั้งใจปิดไว้แล้ว) — pattern การเรียกใช้ให้เดินตามแนวเดียวกับ `HomeRepository._fetchFollowedAuthorIds()` ที่มีอยู่แล้ว (fetch candidate ก่อน แล้วยิง RPC/query แยกเพื่อกรอง ก่อน sort สุดท้าย)

Acceptance Criteria:
- [ ] "กำลังนิยม" (Home Trending row) และ Discovery's Trending Now: Drop ที่มี View (deduped) มากกว่าอย่างมีนัยสำคัญแต่ Like/Comment เท่ากัน ได้อันดับสูงกว่า Drop ที่มี View น้อยกว่า — ยืนยันด้วย unit test ของสูตรโดยตรง (ไม่ต้องพึ่ง Supabase จริง เหมือน `rankTrendingHashtags`/`rankingScore` เดิม)
- [ ] "สำหรับคุณ" (WYN-018): สูตรใหม่มี viewCount term จริง น้ำหนักน้อยกว่า like/comment ตามที่ระบุ ไม่ทำให้ลำดับเดิมของ item ที่ viewCount เท่ากันเปลี่ยนไป (regression: ผลลัพธ์เดิมของ WYN-018's test suite ที่ไม่เกี่ยวกับ view ต้องยังผ่านหมด)
- [ ] Pop content: engagement score/ranking ไม่เปลี่ยนแปลงจากเดิมแม้แต่กรณีเดียว (regression เต็ม)
- [ ] จำลองบัญชีที่โดน `restrict`/`suspend` (ยัง `expires_at > now()`) หรือ `ban` จริงผ่าน `moderation_actions` → Drop/Pop ของบัญชีนั้นหายไปจาก candidate ranking ของ "กำลังนิยม"/"สำหรับคุณ"/Discovery Trending Now ทันที แม้จะมี engagement สูงกว่าเนื้อหาอื่นทั้งหมดก็ตาม
- [ ] เนื้อหาเดียวกันนั้นยัง**เห็นได้ปกติ**ผ่าน Home chronological feed/Following feed/Search/Hashtag feed/โปรไฟล์เจ้าของเอง — ยืนยันว่าไม่ใช่การซ่อนเนื้อหา แค่ตัดสิทธิ์การจัดอันดับ
- [ ] บัญชีที่เคยโดน sanction แต่หมดอายุแล้ว (`expires_at` ผ่านไปแล้ว) หรือถูก `overturned_at` จากการอุทธรณ์สำเร็จ (WYN-030) กลับมาได้รับการจัดอันดับปกติทันที ไม่มี penalty ค้าง
- [ ] RPC ใหม่สำหรับตรวจ sanction status ของกลุ่ม author คืนแค่ id ที่ถูกกันออกเท่านั้น ไม่รั่ว action_type/reason/reviewer/expiry ออกไปทางไหนเลย — ยืนยันด้วย SQL test ตรง (เทียบ return type และเนื้อหา response)
- [ ] Regression เต็มชุด: WYN-017 (Home Trending)/WYN-018 (สำหรับคุณ)/WYN-040 (Discovery ทั้ง 5 section) ทำงานเหมือนเดิมทุกจุดที่ไม่เกี่ยวกับการเปลี่ยนแปลงนี้ ไม่มี regression

Dependencies: WYN-017 (`fetchTrending()`), WYN-018 (`rankingScore()`/`fetchRankedFeed()`), WYN-038 (`drop_view_count()`, ตัวเลข View ที่ trustworthy พอจะใช้), WYN-029/030 (`moderation_actions`/`internal.is_posting_blocked()`, active-sanction semantics รวม overturned-by-appeal), WYN-040 (Discovery's Trending Now/Trending Hashtags reuse `fetchTrending()` ตรงๆ — ได้ผลจาก task นี้อัตโนมัติถ้าแก้ที่จุดเดียว)

Priority: P1 — task ที่สองของ Phase 4 (Discovery & Trending Engine) ตาม roadmap ต่อจาก WYN-040 ทันที

Risks:
- **น้ำหนักตัวเลขของ viewCount ในสูตร (`* 0.1`) เป็นค่าที่ Product กำหนดเองชั่วคราว** ไม่มีข้อมูล traffic จริงมาอ้างอิง (แอปยังไม่ deploy ขึ้น production จริง) เหมือนที่ WYN-038 เคยยอมรับกับตัวเลข rate-limit — ต้องเก็บเป็นค่าคงที่ปรับได้ง่าย มี comment อธิบายเหตุผลชัดเจน ปรับได้ภายหลังเมื่อมีข้อมูลจริง
- **Pop's view count ยังไม่ trustworthy พอสำหรับ ranking** (ดูข้อ 2) — เป็น known gap ที่ยังไม่ปิดในรอบนี้ ถ้า Founder ต้องการให้ Pop ได้รับ treatment เดียวกัน ต้องเป็น task ใหม่แยก (ดู Recommendation)
- **ไม่ใช่ full anti-manipulation/bot-detection** — ยังไม่มี CAPTCHA/device fingerprint/IP tracking ในระบบเหมือนที่ WYN-038 เคยยอมรับสโคปไว้แล้ว (ไม่มี backend service แยก เป็น Flutter+Supabase ล้วนๆ) — task นี้ปิดแค่ 2 ช่องที่ระบุไว้ชัดเจน (view count ไม่ trustworthy สำหรับ Pop, ไม่มีการกันบัญชีที่โดน sanction ไม่ให้ถูกดันขึ้น) ไม่ใช่การแก้ปัญหา sybil/fake-engagement แบบเต็มรูปแบบ — ถ้า Founder ต้องการมากกว่านี้ต้องคุยเรื่อง infra เพิ่มเติม (เหมือนที่ WYN-038 เคยระบุไว้)
- **RPC ใหม่ต้องคืนแค่ id ที่ถูกกันออก ไม่คืนรายละเอียดใดๆ** — ถ้า Design/Coding มองข้ามจุดนี้แล้วคืน action_type/reason ออกมาด้วย จะกลายเป็นการรั่วข้อมูล moderation ที่ WYN-029/030 ตั้งใจปิดไว้เป็นการเฉพาะ (เหมือน Risk ที่ WYN-040 เจอกับ Rising's follower-growth number)
- **การกรองต้องเกิดที่ "การจัดอันดับ" เท่านั้น ห้ามลามไปเป็นการซ่อนเนื้อหาใน RLS/view เดิม** — ถ้าเขียนผิดจุด (เช่น ไปแก้ WHERE clause ของ `home_feed` view ตรงๆ) จะกลายเป็นเปลี่ยนพฤติกรรมการมองเห็นเนื้อหาที่ WYN-029/030 ไม่เคยตั้งใจให้เปลี่ยน (restrict/suspend/ban บล็อกแค่การสร้างเนื้อหาใหม่ ไม่เคยบล็อกการมองเห็นเนื้อหาเก่า) — เน้นให้ QA ตรวจจุดนี้เป็นพิเศษ

Recommendation: เริ่มจาก Requirement 1 (wire viewCount เข้าสูตร — reuse ข้อมูลที่มีอยู่แล้วเต็มที่ ไม่มีความเสี่ยงใหม่) ก่อน แล้วตามด้วย Requirement 3 (moderation-exclusion RPC — งานใหม่แต่ reuse `internal.is_posting_blocked()` เต็มที่ ไม่ต้องเขียน sanction logic ใหม่) — เสนอ **WYN-04X ใหม่แยกต่างหาก** (นอกสโคป task นี้) สำหรับ "Pop View Counting Hardening" (`pop_views` table + `record_pop_view()` RPC มิเรอร์ WYN-038 เป๊ะ ให้ Pop ได้ตัวเลข View ที่ trustworthy เท่า Drop) เป็นตัวเลือกให้ Founder พิจารณาทำต่อหลัง Phase 4 หรือแทรกก่อน WYN-042 (WYN Top 100) ก็ได้ถ้าต้องการให้ Top 100 นับ Pop's view อย่างเป็นธรรม

Handoff: AI Design — ออกแบบ schema/RPC ของ moderation-exclusion filter (Requirement 3, ต้องเป็น SECURITY DEFINER คืนแค่ id ที่ถูกกันออก) และตัดสินใจ integration point สุดท้ายของทั้งสอง Requirement ใน `home_repository.dart` (แก้ตรงจุดไหนใน `fetchTrending()`/`fetchRankedFeed()`/`rankingScore()` ให้ diff เล็กที่สุดและ Discovery (WYN-040) ได้ผลอัตโนมัติโดยไม่ต้องแก้ `DiscoveryRepository` เพิ่ม) — ดู Requirements ข้างต้นสำหรับน้ำหนัก/ขอบเขตที่ Product กำหนดไว้แล้ว

## Coding Output (2026-08-23)

**SQL** (`supabase/schema.sql`, ต่อท้ายส่วน WYN-040 ท้ายไฟล์): เพิ่ม RPC ใหม่ 1 ตัวตามสเปก Design เป๊ะ — `public.authors_posting_blocked(p_author_ids uuid[])` (`security definer`, `stable`, มิเรอร์ `rising_profiles()`/`suggested_users()` ของ WYN-040 ทั้ง shape และเหตุผล) เป็น bulk single-query wrapper รอบ `internal.is_posting_blocked()` เดิมของ WYN-029/030 ตรงๆ (ไม่เขียน sanction logic ใหม่เลยแม้แต่บรรทัดเดียว) — `select id from unnest(p_author_ids) where internal.is_posting_blocked(id)` คืนแค่ `table(author_id uuid)` คอลัมน์เดียว (id ที่ถูกกันออกเท่านั้น ไม่คืน action_type/reason/reviewer_id/expires_at ใดๆ) ตามที่ Design ระบุไว้เป็น anti-gaming return shape

**SQL test ใหม่** (`supabase/tests/wyn_041_trending_engine_test.sh`, มิเรอร์ harness ของ `wyn_040_discovery_test.sh`) — 9 checks ครอบ: active restrict/suspend/permanent ban ถูกกันออกทั้ง 3 กรณี, restrict ที่หมดอายุแล้วไม่ถูกกันออก, restrict ที่ถูก `overturned_at` จากการอุทธรณ์สำเร็จ (WYN-030) ไม่ถูกกันออกแม้ `expires_at` จะยังไม่ถึงก็ตาม, บัญชีปกติไม่มีแถวใน `moderation_actions` เลยไม่ถูกกันออก, ผลลัพธ์ batch เดียวมีขนาดตรง 3 พอดีไม่มีแถวเกิน/ซ้ำ, third-party/stranger (ไม่มีความสัมพันธ์กับ candidate ใดเลย ไม่ใช่ moderator) เรียกแล้วได้ผลถูกต้องเหมือนกัน (พิสูจน์ SECURITY DEFINER bypass การไม่มี SELECT policy ของ `moderation_actions` จริง ไม่ใช่แค่คาดเดา), `pg_get_function_result()` ยืนยัน return type เป็น `TABLE(author_id uuid)` เป๊ะ — **9/9 checks PASS** (พบปัญหาเดียวระหว่างเขียน: type-inference ของ inline array literal ใน CHECK7 default เป็น `text[]` แทน `uuid[]` เพราะไม่มีตัวแปรประกาศ type ไว้ล่วงหน้าเหมือน CHECK1-6/8 — แก้ด้วยการเติม `::uuid[]` explicit cast ไม่ใช่บั๊กใน RPC logic) — รันซ้ำครบทั้ง 14 สคริปต์เดิม (`wyn_021` ถึง `wyn_040`) **ผ่านหมดไม่มี cross-task regression** — `check_schema_ordering.py` ผ่าน (ฟังก์ชันใหม่ต่อท้ายไฟล์ ไม่มี forward reference)

**Flutter**: เพิ่ม `engagementScore(HomeFeedItem item)` ใหม่ใน `app/lib/features/home/data/home_ranking.dart` (ตามสเปก Design เป๊ะ) — early-return คืน `likeCommentScore.toDouble()` ทันทีถ้า `contentType != HomeContentType.drop` (Pop ไม่แตะ view เลย) ก่อนคำนวณ view term ใดๆ, ค่าคงที่ `_viewWeight = 0.1` มี comment อธิบายเหตุผล (Product กำหนดเองชั่วคราว ไม่มีข้อมูล traffic จริง) ตรงจุดประกาศ — แก้ `rankingScore()` ให้เรียก `engagementScore(item)` แทนสูตร inline เดิม (`item.likeCount * 2 + item.commentCount * 3`) — แก้ `fetchTrending()` (`home_repository.dart`) ให้ sort ด้วย `engagementScore(b).compareTo(engagementScore(a))` แทน `(b.likeCount + b.commentCount).compareTo(...)` เดิม (น้ำหนัก like/comment ตอนนี้สอดคล้องกับ `rankingScore()` แล้ว 2:3 แทนที่ 1:1 เดิม ตามที่ Design/Product ตัดสินใจ) — เพิ่ม `_fetchPostingBlockedAuthorIds(Set<String> authorIds)` helper ใหม่ (มิเรอร์ `_fetchFollowedAuthorIds()` เดิมเป๊ะ — เรียก RPC `authors_posting_blocked` แล้วคืน `Set<String>`) เรียกใช้ทั้งใน `fetchTrending()`/`fetchRankedFeed()` ก่อนขั้นตอน sort เสมอ (`removeWhere` เอา item ของ author ที่ถูกกันออกทิ้งก่อน sort/take) — `fetchTrending()` เพิ่มการเก็บ `authorIds` set จาก `rows` (เดิมไม่มี เก็บแค่ `dropIds`/`popIds`) — `fetchRankedFeed()` reuse `authorIds` set ที่เก็บไว้แล้วสำหรับ `_fetchFollowedAuthorIds` ซ้ำกับ `_fetchPostingBlockedAuthorIds` เลย ไม่ query authorId ซ้ำสอง RPC จาก set เดียวกัน — `DiscoveryRepository`/`DiscoveryView` (WYN-040) **ไม่ถูกแตะแม้แต่บรรทัดเดียว** เพราะ `fetchTrendingNow()`/`fetchTrendingHashtags()` reuse `HomeRepository.fetchTrending()` ตรงๆ อยู่แล้ว ได้ผลจาก view-weight/moderation-exclusion ใหม่โดยอัตโนมัติตามที่ Design คาดไว้

**Flutter test ใหม่** (`app/test/home_ranking_test.dart`): เพิ่ม `contentType`/`viewCount` optional param เข้า test helper `_item()` เดิม (default คงเดิมทั้งคู่ ไม่กระทบ 8 เทสต์เดิมของ `rankingScore` แม้แต่เคสเดียว) — เพิ่ม 7 เทสต์ใหม่ครอบ: `engagementScore` ของ Drop รวม view ตามน้ำหนักที่กำหนด (0/null/100 views), `engagementScore` ของ Pop ไม่รวม view เลยแม้ viewCount สูงมาก (100,000), น้ำหนัก view เล็กพอที่ like/comment ยังครองสูตรอยู่ (3 like ชนะ 50 view), `rankingScore` ของ Drop เปลี่ยนตาม viewCount จริง, `rankingScore` ของ Pop ไม่เปลี่ยนแปลงจาก viewCount เลย — ไม่มี unit/widget test ใหม่สำหรับ `_fetchPostingBlockedAuthorIds`/exclusion-in-`fetchTrending`/`fetchRankedFeed` โดยตรง เพราะโค้ดเบสนี้ไม่มี harness สำหรับทดสอบ method ที่เรียก Supabase client จริงแบบ isolated อยู่แล้วเป็นทุนเดิม (เหมือนที่ WYN-038's `recordView()`/`record_drop_view()` ก็ไม่มี unit test แบบนี้เช่นกัน — มีแค่ SQL test สำหรับความถูกต้องของ RPC และ regression test ระดับ screen ผ่าน Recording fake ที่ override เมธอดทั้งก้อน) — ความถูกต้องของ SQL RPC ยืนยันผ่าน `wyn_041_trending_engine_test.sh` (9/9 PASS) แทน และ wiring ฝั่ง Dart ยืนยันผ่าน `flutter analyze` (type-check การเรียก RPC/`removeWhere`/`Set<String>` ถูกต้อง) + การอ่าน source ตรงๆ

**Build/Tests — รันจริงครบทุกจุด**:
- `flutter analyze`: **0 issues**
- `flutter test`: **651/651 pass** (baseline 644 จาก WYN-040 QA round + เคสใหม่ 7 ใน `home_ranking_test.dart`)
- `dart format --set-exit-if-changed`: ผ่านทุกไฟล์ที่แก้ (2 ไฟล์ต้อง format ใหม่รอบแรก — `home_repository.dart`/`home_ranking_test.dart` — รันซ้ำแล้วผ่านสะอาด 0 changed)
- SQL: 9/9 checks (`wyn_041_trending_engine_test.sh`) + รันซ้ำครบ 14 สคริปต์เดิม **ผ่านหมดไม่มี cross-task regression** — `check_schema_ordering.py` ผ่าน

**Known Issue/Gap (ตั้งใจ ไม่ใช่บั๊ก แจ้ง QA/Product ให้ตัดสินใจ)**:
- **ไม่มี Flutter test ตรงสำหรับ exclusion logic ใน `fetchTrending()`/`fetchRankedFeed()`** (ดูเหตุผลข้างบน — ไม่มี harness เดิมให้ต่อยอด) — ความถูกต้องพิสูจน์ผ่าน SQL test ของ RPC เอง + code review ของ wiring ฝั่ง Dart เท่านั้น ถ้า Founder ต้องการ integration test ระดับนี้จริง ต้องลงทุนสร้าง mock-HTTP-client harness ใหม่ทั้งชุดสำหรับ `HomeRepository` ซึ่งเป็นงานแยกที่ใหญ่กว่าขอบเขตของ task นี้มาก
- **`_viewWeight = 0.1` และน้ำหนัก like:comment 2:3 ที่ตอนนี้ใช้ร่วมกันทั้ง `fetchTrending()`/`rankingScore()` เป็นค่าที่ Product กำหนดเองชั่วคราว** ไม่มีข้อมูล traffic จริงมาอ้างอิงตามที่ระบุไว้ใน Risks — ปรับได้ง่ายในอนาคต (ค่าคงที่จุดเดียว มี comment อธิบาย)
- **Pop's view count ยังไม่ถูกรวมในสูตรใดๆ** ตามที่ Product ตัดสินใจไว้แล้ว (Requirement 2) — ถ้า Founder ต้องการให้ Pop ได้ treatment เดียวกัน ต้องทำ "Pop View Counting Hardening" เป็น task แยกก่อน (Product's Recommendation)
- **การกรอง moderation-exclusion กรองแค่ตาม `item.authorId` ตรงๆ ไม่ครอบคลุม ReDrop chain** (ผู้ ReDrop vs เจ้าของเนื้อหาต้นฉบับคนละคนกัน) ตามที่ Design ระบุไว้เป็น Non-goal ชัดเจนแล้ว

**Acceptance Criteria — ไล่ตรวจครบทุกข้อจาก Product spec**: ครบทุกข้อ ยืนยันด้วย SQL test จริง (RPC/RLS/SECURITY DEFINER/return-shape ทุกจุด) + Flutter test จริง (engagementScore/rankingScore ทั้ง Drop/Pop) + code review ยืนยัน integration point ใน `fetchTrending()`/`fetchRankedFeed()` ถูกต้องตรงตาม Design

Handoff: AI QA & Security — เน้นตรวจ 4 จุดตาม Product's Risks เป็นพิเศษ (ยืนยันแล้วด้วย SQL/Flutter test ของ Coding เองแต่ควรตรวจอิสระซ้ำ): **(ก) `authors_posting_blocked()` ไม่รั่ว action_type/reason/reviewer_id/expires_at ออกทางไหนเลย** — ตรวจ return type ตรงๆ + ตรวจ `home_repository.dart` ว่าไม่มีจุดไหนพยายามอ่าน field อื่นนอกจาก `author_id` จาก response, **(ข) เนื้อหาของบัญชีที่โดน sanction ยังเห็นได้ปกติทาง Home chronological/Search/Following/โปรไฟล์ตัวเอง** — ยืนยันว่าเป็นการตัดสิทธิ์การจัดอันดับเท่านั้น ไม่ใช่การซ่อนเนื้อหา (ไม่มีการแก้ RLS/`home_feed` view ใดๆ ในรอบนี้เลยแม้แต่บรรทัดเดียว), **(ค) Pop's engagement/ranking score ไม่เปลี่ยนแปลงจากเดิมแม้แต่กรณีเดียว** ยืนยันด้วยการทดสอบจริงคู่ขนานกับ Drop, **(ง) น้ำหนัก view ใหม่ไม่ทำให้พฤติกรรมเดิมของ WYN-017/018 test suite ที่ไม่เกี่ยวกับ view (8 เทสต์เดิมของ `rankingScore`) พังโดยไม่ได้ตั้งใจ** — ยืนยันด้วยการรัน test เดิมซ้ำอิสระ

## Independent QA (2026-08-23)

```
Feature: WYN-041 Trending Engine v2 — wire Drop's view_count เข้าสูตรจัดอันดับ (rankingScore/fetchTrending) + Anti-manipulation: ตัดสิทธิ์การจัดอันดับของบัญชีที่โดน moderation sanction ที่ยัง active อยู่
Environment: Local sandbox — PostgreSQL 16 และ Flutter stable ชุดเดียวกับที่ใช้ทดสอบ WYN-040 ในเซสชันนี้ (ยังไม่ได้รีสตาร์ท service ใหม่ แต่รัน schema/test สดใหม่ทุกครั้งผ่าน throwaway database แยกของแต่ละสคริปต์ ไม่ reuse state ข้ามรัน) — sync branch `claude/wyn-40-continuation-ul5ngq` ที่ commit `d90ad7f` ก่อนเริ่มทดสอบ

Test Cases:
1. `supabase/tests/wyn_041_trending_engine_test.sh` รันอิสระเอง (ไม่เชื่อตัวเลขจาก Coding Output) — 9 checks ครอบ active restrict/suspend/ban ถูกกันออก, restrict ที่หมดอายุ/ถูก overturn ไม่ถูกกันออก, บัญชีปกติไม่ถูกกันออก, third-party เห็นผลถูกต้องผ่าน SECURITY DEFINER, return type ตรวจด้วย `pg_get_function_result()`, batch result ขนาดตรง
2. รันซ้ำ SQL regression ทั้ง 15 สคริปต์ (`wyn_021` ถึง `wyn_041`) ยืนยันไม่มี cross-task regression จากการเพิ่ม RPC ใหม่
3. `check_schema_ordering.py` — ไม่มี forward reference
4. **Adversarial probe เพิ่มเติมที่ไม่มีอยู่ใน SQL test เดิมของ Coding** (เขียน SQL เองแยกต่างหาก ไม่ใช่แค่รันของเดิมซ้ำ): (ก) `authors_posting_blocked(array[]::uuid[])` (empty array) → ไม่ error, คืน 0 แถว (ข) `authors_posting_blocked(null::uuid[])` → ไม่ error, คืน 0 แถว (ค) batch ผสม id ที่ถูกแบน + uuid ที่ไม่มีอยู่จริง + user ปกติ → คืนแค่ id ที่ถูกแบนตัวเดียวถูกต้อง (ง) ส่ง id ซ้ำกัน 2 ตัวใน input array → output มี 2 แถวซ้ำกันจริง (ดู Security Findings ด้านล่าง) (จ) ผู้ใช้ทั่วไป (ไม่ใช่ moderator) `select * from moderation_actions` ตรงๆ → คืน 0 แถว ยืนยันว่า RLS ปิดจริง (พิสูจน์ว่า SECURITY DEFINER RPC จำเป็นจริง ไม่ใช่แค่คำกล่าวอ้าง)
5. `\df+ public.authors_posting_blocked` ยืนยัน: Security = `definer`, return type = `TABLE(author_id uuid)`, grant เฉพาะ `authenticated` (ไม่มี `anon`)
6. `flutter analyze` รันอิสระเอง (0 issues)
7. `flutter test` เต็มชุดรันอิสระเอง (ไม่ subset) — **651/651 pass**
8. อ่าน diff เต็มของ `home_ranking.dart`/`home_repository.dart` แบบ adversarial ทีละบรรทัด: ยืนยัน `engagementScore()` early-return ก่อนคำนวณ view term จริง (ไม่ใช่ conditional ซ่อนกลางสูตร), ยืนยัน `fetchRankedFeed()` reuse ตัวแปร `authorIds` ตัวเดียวกับที่ใช้ใน `_fetchFollowedAuthorIds` ถูกต้องจริง (ไม่ได้สร้างซ้ำผิดตัว), ยืนยันว่า `removeWhere` เกิดขึ้น**ก่อน** `sort()`/`take()` เสมอทั้งสองจุด
9. `git diff --stat` ยืนยันว่า `fetchFeed()` (Home chronological), `fetchFollowingFeed()` (Following feed), `DiscoveryRepository`/`DiscoveryView` (WYN-040), และ RLS/`home_feed` view ใน `schema.sql` **ไม่ถูกแตะแม้แต่บรรทัดเดียว** — ยืนยันเนื้อหาบัญชีที่โดน sanction ยังเข้าถึงได้ปกติทุกจุดอื่น
10. รัน `home_ranking_test.dart` แยกเจาะจง ยืนยัน 8 เทสต์เดิมของ `rankingScore` (WYN-018) ยังผ่านครบทุกเคสไม่มีการแก้ค่า expect ใดๆ เลย (regression zero) ควบคู่กับ 7 เทสต์ใหม่ของ `engagementScore`

Passed: 9/9 (SQL ใหม่) + 15/15 สคริปต์ SQL ทั้งหมด (รวม `wyn_041` เอง) + 5/5 adversarial probe เพิ่มเติม + 0 issues (`flutter analyze`) + 651/651 (`flutter test` เต็มชุด) — ทุกตัวเลขยืนยันด้วยการรันเองอิสระ ไม่ใช่การเชื่อ Coding Output
Failed: 0

Severity: N/A (ไม่พบบั๊กที่ block การอนุมัติ)

Reproduction Steps: N/A — ไม่มีบั๊กให้ reproduce

Expected: ครบ 4 จุดตาม Handoff ของ Coding — (ก) RPC ไม่รั่ว action detail, (ข) เนื้อหายังเห็นได้ปกติทางอื่น, (ค) Pop ไม่ถูกกระทบ, (ง) WYN-018 test เดิมไม่พัง

Actual: ตรงตามที่คาดทั้ง 4 จุด — (ก) ยืนยันด้วย CHECK9 (`pg_get_function_result` = `TABLE(author_id uuid)` เป๊ะ) + `\df+` ยืนยัน grant เฉพาะ `authenticated` + อ่านโค้ด Flutter ยืนยันอ่านแค่ `row['author_id']` เท่านั้น ไม่มีจุดไหนพยายามอ่าน field อื่น, (ข) ยืนยันด้วย `git diff --stat` ว่า `fetchFeed`/`fetchFollowingFeed`/RLS/`home_feed` view ไม่ถูกแตะเลย เนื้อหายังอยู่ในเส้นทางเดิมทั้งหมด, (ค) ยืนยันด้วยเทสต์คู่ขนาน Drop-vs-Pop ใน `home_ranking_test.dart` (Pop's `engagementScore`/`rankingScore` ไม่เปลี่ยนแม้ viewCount สูงถึง 100,000), (ง) รัน 8 เทสต์เดิมของ `rankingScore` ซ้ำอิสระ ผ่านครบไม่มีค่า expect ไหนถูกแก้เพื่อ "ยอมรับ" การเปลี่ยนแปลง — ตัวเลข expect เดิมยังตรงเป๊ะ (168, 158, 0, 178, 183, 218, 210, 188/168) พิสูจน์ว่า viewCount ที่เป็น `null` ในเทสต์เดิมไม่ทำให้สูตรเปลี่ยนพฤติกรรมเดิมเลย

Security Findings:
- ไม่พบช่องโหว่ privacy leak ใหม่ — `authors_posting_blocked()` คืนแค่ `author_id`, ยืนยันด้วยทั้ง SQL test และ `\df+` โดยตรง ไม่รั่ว action_type/reason/reviewer_id/expires_at ทางไหนเลย
- ไม่พบการเปลี่ยนแปลง RLS/visibility ใดๆ — การกรองเกิดที่ชั้นการจัดอันดับ (client-side, หลัง fetch ผ่าน RLS ปกติ) เท่านั้นจริง ตรงตามเจตนาของ Product ที่ต้องการแยก "ตัดสิทธิ์จัดอันดับ" ออกจาก "ซ่อนเนื้อหา" อย่างเด็ดขาด
- Minor (ไม่ block): `authors_posting_blocked()` ไม่ dedupe input array เอง (ส่ง id ซ้ำ 2 ตัว → ได้ผลลัพธ์ซ้ำ 2 แถว) — ตรวจแล้วไม่ใช่ช่องโหว่และไม่กระทบพฤติกรรมจริง เพราะ caller เดียวในระบบ (`HomeRepository._fetchPostingBlockedAuthorIds`) รับ input เป็น `Set<String>` อยู่แล้วก่อนแปลงเป็น list (การันตี unique โดยธรรมชาติ) และแปลงผลลัพธ์กลับเป็น `Set<String>` ทันทีหลังได้ response กลับมา (ดูดซับ duplicate ที่อาจเกิดขึ้นให้หายไปเองอีกชั้น) — ไม่ต้องแก้ในรอบนี้
- ยืนยันด้วยการทดสอบตรงว่าผู้ใช้ทั่วไป (ไม่ใช่ moderator) select ตรงจาก `moderation_actions` ไม่เห็นข้อมูลเลย (0 แถว) — พิสูจน์ว่า SECURITY DEFINER RPC เป็นทางเดียวที่ทำได้จริง ไม่ใช่ทางเลือกที่ไม่จำเป็น

Recommendation: อนุมัติ — ครบทุก Acceptance Criteria จาก Product spec, ยืนยันด้วยการทดสอบอิสระทั้ง SQL/Flutter รวมถึง adversarial probe เพิ่มเติมที่ Coding ไม่ได้ทำไว้ ไม่มี regression ต่อ WYN-017/018/029/030/040 เดิมแม้แต่จุดเดียว, การแยก "ตัดสิทธิ์จัดอันดับ" ออกจาก "ซ่อนเนื้อหา" ทำได้ถูกต้องสมบูรณ์ (ไม่แตะ RLS/view ใดๆ เลย) — พบเฉพาะข้อสังเกต Minor ที่ไม่กระทบพฤติกรรมจริง (RPC ไม่ dedupe input เอง แต่ caller เดียวที่มีอยู่ไม่มีทางส่ง duplicate เข้าไปได้อยู่แล้ว) ไม่ต้องแก้ก่อน deploy

Final Status: PASS
```

