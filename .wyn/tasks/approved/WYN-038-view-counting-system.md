# Product Task — WYN-038

Status: backlog
Owner: AI Product Manager

Feature: View Counting System (Unique Viewer / Rate Limit / Bot Detection) — Drop

Goal: ให้ Drop มีระบบนับ View ที่น่าเชื่อถือพอจะใช้เป็นสัญญาณ Trending ในอนาคต — task ที่ห้าของ Phase 3 (`.wyn/docs/product/wyn-v1.0.0-roadmap.md`), Master Spec section 6 "VIEW SYSTEM": "นับ Views อย่างมีระบบ ไม่ใช่ทุก Refresh = 1 View ต้องมี: Unique Viewer logic, Rate limiting, Bot detection, Suspicious traffic detection — เพื่อป้องกันการปั่นยอด" — และเป็น Dependency ตรงของ Phase 4's WYN-041 (Trending Engine v2): "WYN-038 (View system ให้ข้อมูล Trending Score)"

Target User: ทุกผู้ใช้ที่เปิดดู Drop (ผู้ถูกนับ View) และเจ้าของ Drop/ทีม Trending ในอนาคต (ผู้ใช้ข้อมูล View เพื่อวัดความนิยม/จัดอันดับ)

Problem: ตอนนี้ **Drop ไม่มีระบบนับ View เลยแม้แต่จุดเดียว** — `home_feed`/`saved_feed` view (WYN-007/WYN-013) จองคอลัมน์ `view_count` ไว้แล้วแต่ hardcode เป็น `null::bigint` สำหรับแถว Drop ทุกแถว (`supabase/schema.sql` บรรทัด 471, 556) และ `Drop` model/`DropRepository`/`DropDetailScreen`/`HomeDropCard` ไม่มี field/UI ของ view count เลย ทำให้ WYN-041 (Trending Engine v2) ที่ต้องพึ่งข้อมูลนี้ทำต่อไม่ได้ — Pop (WYN-006) มี `pops.view_count` อยู่แล้วแต่เป็น **counter ธรรมดาที่นับซ้ำได้ไม่จำกัด** (`increment_pop_view_count()` แค่ `+1` ทุกครั้งที่เรียก ไม่มี dedup/rate-limit ระดับ DB เลย มีแค่ client-side flag `_viewRecorded` กันเรียกซ้ำ "ภายในเซสชันเดียว" เท่านั้น — เปิดแอปใหม่/เข้าคลิปซ้ำนับเพิ่มได้เรื่อยๆ) ซึ่ง WYN-006's QA เคยพบและบันทึกไว้แล้วว่าเป็น Minor gap ไม่ block ตอนนั้น

Requirements:

**ขอบเขต: Drop เท่านั้น** — Pop ถูกถอดออกจาก Bottom Nav แล้วตั้งแต่ WYN-024 (V1.0.0 ยังไม่เปิด WYN Pop จนกว่าจะถึง V3) โค้ด/DB ของ Pop คงไว้แต่ไม่มีใครเข้าถึงได้จริงผ่าน UI ปกติ — การลงทุนแก้ระบบ View ของ Pop ตอนนี้จะไม่มีผลกับผู้ใช้จริง จึง **ไม่แตะ `pops.view_count`/`increment_pop_view_count()` เลยในรอบนี้** (ตัดสินใจโดย Product เพื่อลดความเสี่ยง/ขอบเขต ตามกติกา "เปลี่ยนแปลงเฉพาะส่วนที่จำเป็น") — ถ้า Founder ต้องการเปิด Pop กลับมาในอนาคต ให้ยก pattern เดียวกันของ Drop ไปทำซ้ำเป็น follow-up แยก

**Unique Viewer logic**
- นับ View แบบ **unique ต่อผู้ใช้ต่อ Drop ตลอดกาล (lifetime, นับครั้งเดียวตลอดไป)** ไม่ใช่นับทุกครั้งที่เปิดซ้ำ/refresh — ผู้ใช้คนเดิมเปิด Drop เดิมซ้ำกี่ครั้งก็นับแค่ 1
- **เจ้าของ Drop เปิดดู Drop ตัวเองไม่นับเป็น View** (ป้องกันการปั่นยอดง่ายๆ ด้วยการเปิดดูโพสต์ตัวเองซ้ำๆ)
- จุดที่ถือว่าเป็น "View" จริง = ผู้ใช้เปิด **`DropDetailScreen`** (ไม่ใช่แค่การ์ดเลื่อนผ่านใน Home Feed — scroll ผ่านไม่ใช่การ "ดู" จริงและปลอมง่ายด้วยการเลื่อน feed เร็วๆ) มิเรอร์แนวคิดเดียวกับที่ Pop นับตอนวิดีโอเริ่มเล่นจริง ไม่ใช่ตอนการ์ด thumbnail ปรากฏ

**Rate limiting**
- จำกัดจำนวน View record ใหม่ที่ **1 บัญชีสร้างได้ต่อหน่วยเวลา** (ป้องกันบัญชี/สคริปต์เดียวไล่เปิด Drop จำนวนมากเร็วๆ เพื่อปั่นยอดให้พวกเดียวกัน) — เกินโควตาแล้ว **เงียบๆ ไม่นับเพิ่ม ไม่ error** (การนับ View ไม่ควรทำให้ UI ล่ม/ผู้ใช้ปกติเห็น error)

**Bot detection / Suspicious traffic detection**
- **ขอบเขตที่ทำได้จริงตอนนี้** (ดู Risks): ไม่มี CAPTCHA/device fingerprint/IP tracking ในระบบ ทำ full bot detection ไม่ได้ในรอบนี้ — ใช้ 3 กลไกร่วมกันแทนเป็นแนวป้องกันเชิงพฤติกรรมระดับ V1:
  1. Self-view exclusion (ด้านบน)
  2. Rate limiting ต่อบัญชี (ด้านบน)
  3. **Velocity cap ต่อ Drop**: จำกัดจำนวน View record ใหม่ที่ **1 Drop รับได้ต่อหน่วยเวลาสั้นๆ** (ป้องกัน bot ring หลายบัญชีรุมกด Drop เดียวพร้อมกันเพื่อปั่นให้ดูเหมือนกำลังไวรัล) — เกินโควตาแล้วเงียบๆ ไม่นับเพิ่มเช่นกัน

**แสดงผล**
- เชื่อมค่า `view_count` จริงเข้า `home_feed`/`saved_feed` view แทน `null::bigint` เดิมสำหรับแถว Drop (คอลัมน์นี้ถูกจองไว้ตั้งแต่ WYN-007 แล้ว)
- แสดง View count บน `DropDetailScreen` (มิเรอร์ icon ตาที่ `PopClipView` ใช้อยู่แล้ว) และบน `HomeDropCard` (มิเรอร์ icon+เลขที่ `HomePopCard` ใช้อยู่แล้วกับ Pop — `HomeFeedItem.viewCount` field มีอยู่แล้วแค่ยังไม่ได้ใช้ฝั่ง Drop)

Acceptance Criteria:
- [ ] เปิด `DropDetailScreen` ของ Drop คนอื่นครั้งแรก → View count ของ Drop นั้นเพิ่ม 1
- [ ] เปิด `DropDetailScreen` ของ Drop เดิมซ้ำอีกหลายครั้ง (คนละเซสชัน/เปิดแอปใหม่) โดยผู้ใช้คนเดิม → View count **ไม่เพิ่มอีก** (นับแค่ครั้งแรกครั้งเดียว ตรวจสอบด้วย SQL โดยตรง ไม่ใช่แค่ดู UI)
- [ ] เจ้าของ Drop เปิดดู Drop ตัวเอง → View count **ไม่เพิ่ม**
- [ ] ผู้ใช้คนละคนเปิด Drop เดียวกัน → View count เพิ่มตามจำนวนคนจริง (unique ต่อคน)
- [ ] จำลองบัญชีเดียวเรียก record-view RPC ตรงถี่ๆ เกินโควตา rate-limit ต่อบัญชี → รายการเกินโควตาไม่ถูกนับ (ตรวจด้วย SQL โดยตรง), ไม่มี error กระเด็นกลับไปที่ client
- [ ] จำลองหลายบัญชีเรียก record-view RPC ตรงถี่ๆ กับ Drop เดียวกันเกินโควตา velocity-cap ต่อ Drop → รายการเกินโควตาไม่ถูกนับ, ไม่มี error
- [ ] ผู้ใช้ทั่วไป (ไม่ใช่เจ้าของ) เรียก SELECT ตรงบนตาราง View record **เห็นได้เฉพาะแถวของตัวเอง** (ว่าตัวเองเคยดู Drop ไหนบ้าง) **เห็นไม่ได้ว่าคนอื่นดู Drop ไหนบ้าง** (ป้องกัน privacy leak ว่าใครดูอะไร มิเรอร์บทเรียนจาก WYN-029's actor-identity-leak bug) — แต่ View count ที่แสดงใน Home Feed/Drop Detail ต้องยังถูกต้อง/ตรงกันสำหรับทุกคนที่ดู (ไม่ใช่แค่เจ้าของ/คนที่เคยดูเอง)
- [ ] Home Feed การ์ด Drop และหน้า `DropDetailScreen` แสดง View count จริงตรงกับค่าใน DB
- [ ] Regression: Drop ที่ถูก soft-delete (WYN-037) ไม่ถูกนับ View เพิ่มได้อีก, การนับ View ไม่กระทบ Like/Comment/Save/ReDrop/Poll count เดิม, Pop's view count เดิมยังทำงานแบบเดิมไม่เปลี่ยนแปลง (ไม่ถูกแตะในรอบนี้)

Dependencies: WYN-005 (Drop core), WYN-007 (Home Feed's `home_feed` view — คอลัมน์ `view_count` ถูกจองไว้แล้ว), WYN-013 (`saved_feed` view มิเรอร์ปัญหาเดียวกัน), WYN-037 (soft delete — View ต้องเช็คว่า Drop ยังไม่ถูกลบ) — ทั้งหมด merge เข้า `main` แล้ว ไม่ block

Priority: P1 — task ที่ห้าของ Phase 3 ตามลำดับ Roadmap ต่อจาก WYN-034/035/036/037 และเป็น Dependency ตรงของ WYN-041 (Phase 4)

Risks:
- **ไม่ใช่ bot detection แบบเต็มรูปแบบ** — Master Spec เขียนกว้างว่า "Bot detection" แต่ระบบนี้ไม่มี CAPTCHA/device fingerprint/IP logging/ML ใดๆ (repo เป็น Flutter+Supabase ล้วนๆ ไม่มี backend service แยก) การตรวจจับ bot จริงจังต้องมี infra เพิ่มที่ยังไม่มี — ยอมรับ scope reduction นี้: ใช้ self-view exclusion + rate-limit ต่อบัญชี + velocity-cap ต่อ Drop แทน เป็นแนวป้องกันเชิงพฤติกรรมที่ทำได้จริงด้วย SQL ล้วนๆ ตอนนี้ ถ้า Founder ต้องการมากกว่านี้ต้องคุยเรื่อง infra เพิ่มเติมเป็น follow-up
- **ตัวเลขโควตา rate-limit/velocity-cap เป็นค่าที่ Product กำหนดเองชั่วคราว** ไม่มีข้อมูล traffic จริงมาอ้างอิง (แอปยังไม่ deploy ขึ้น production จริง) — เสนอตัวเลขเริ่มต้นให้ Design/Coding ใน Recommendation แต่ต้องปรับได้ง่ายภายหลังเมื่อมีข้อมูลจริง (เก็บเป็นค่าคงที่ในโค้ด ไม่ hardcode กระจัดกระจาย)
- **View count ที่ Client เพิ่มแบบ optimistic อาจไม่ตรงกับ DB ชั่วคราว** ถ้า RPC เงียบๆ ปฏิเสธ (เกิน rate-limit/velocity-cap หรือเป็นเจ้าของ Drop เอง) — ค่าจะกลับมาตรงเองเมื่อ feed refresh ครั้งถัดไป มิเรอร์ risk เดียวกับที่ Pop (WYN-006) ยอมรับไว้แล้วโดยไม่มีปัญหา
- **ไม่มี physical purge/archival job สำหรับ View record เก่า** — ตารางจะโตเรื่อยๆ ตามจำนวน (Drop × unique viewer) ไม่มี TTL/cron เหมือนหลาย task ก่อนหน้าที่ยอมรับ gap เดียวกัน (ระบบยังไม่มี cron infrastructure จริง) — ยอมรับไว้ก่อน ปรับปรุงเมื่อ scale จริงบังคับ

Recommendation:
1. Schema: ตารางใหม่ `drop_views` (`drop_id references drops(id) on delete cascade`, `viewer_id references profiles(id) on delete cascade`, `created_at timestamptz not null default now()`, **primary key (drop_id, viewer_id)** — ตัว primary key เองคือกลไก unique-viewer dedup ระดับ DB โดยตรง ไม่ต้องเขียน logic แยก)
2. **ไม่มี raw client INSERT policy เลย** — เขียนผ่าน RPC เดียว `record_drop_view(drop_id uuid)` (SECURITY DEFINER) เท่านั้น มิเรอร์ pattern `increment_pop_view_count()`/`edit_drop()` เดิม ภายในฟังก์ชันเช็คตามลำดับแล้ว **no-op เงียบๆ (ไม่ raise exception)** ถ้าข้อใดข้อหนึ่งไม่ผ่าน: (a) Drop ไม่มีอยู่จริงหรือถูก soft-delete แล้ว (เช็คแบบเดียวกับ `internal.is_drop_deleted()` ที่ WYN-037 สร้างไว้แล้ว) (b) `auth.uid()` คือเจ้าของ Drop เอง (c) บัญชีนี้มี insert เข้า `drop_views` เกิน **20 ครั้งในช่วง 60 วินาทีที่ผ่านมา** (rate-limit ต่อบัญชี — นับจาก `count(*) from drop_views where viewer_id = auth.uid() and created_at > now() - interval '60 seconds'`) (d) Drop นี้มี insert เข้า `drop_views` เกิน **50 ครั้งในช่วง 10 วินาทีที่ผ่านมา** (velocity-cap ต่อ Drop) — ถ้าผ่านทุกเงื่อนไข ค่อย `insert into drop_views ... on conflict (drop_id, viewer_id) do nothing` (กัน race condition ซ้อนกับ primary key อีกชั้น)
3. **SELECT policy ของ `drop_views` ต้องจำกัดแค่ `auth.uid() = viewer_id`** (แต่ละคนเห็นได้แค่ว่าตัวเองเคยดู Drop ไหนบ้าง) — **ห้ามเปิด select-all-authenticated แบบที่ `drop_likes`/`drop_comments` ใช้** เพราะ View ต่างจาก Like ตรงที่เป็นข้อมูลที่ผู้ใช้ไม่ได้ตั้งใจเปิดเผยว่า "ใครดูอะไร" (มิเรอร์บทเรียนจาก `.wyn/tasks/bugs/WYN-029-moderation-actor-identity-leak.md` ที่เคยพบว่า identity รั่วไหลเพราะ SELECT policy กว้างเกินไป) — ปัญหาคือถ้า SELECT ถูกจำกัดแบบนี้ `count(*)` subquery ใน `home_feed`/`saved_feed` (ที่เป็น `security_invoker = true`) จะเห็นแค่แถวของผู้ดู feed คนนั้นเอง ทำให้ View count ที่แต่ละคนเห็นไม่ตรงกัน (ผิด) — **ทางแก้**: สร้างฟังก์ชันแยก `public.drop_view_count(drop_id uuid) returns bigint` (**SECURITY DEFINER**, bypass RLS ของ `drop_views` ตรงๆ เพื่อนับทุกแถวจริง คืนแค่ตัวเลข ไม่คืนแถว/viewer identity ใดๆ ออกมาเลย) แล้วให้ `home_feed`/`saved_feed` เรียก `public.drop_view_count(d.id) as view_count` แทน correlated subquery ตรงๆ — วิธีนี้ทั้งสองอย่างได้พร้อมกัน: เลขถูกต้องสำหรับทุกคน + ไม่มีใครเห็น raw viewer list ของคนอื่นได้เลยไม่ว่าทางไหน
4. Flutter: `Drop` model เพิ่ม `viewCount` (non-null, default 0 จาก DB เสมอเพราะ `drop_view_count()` คืน 0 ไม่ใช่ null) + `withExtraView()` มิเรอร์ `Pop`'s เดิม — `DropRepository` เพิ่ม `recordView(dropId)` เรียก RPC `record_drop_view` (fire-and-forget, `catchError` กลืน error เหมือน Pop เพราะ View count ไม่ควรบล็อก/ทำ error UI) — `DropDetailScreen` เพิ่ม `_viewRecorded` flag เรียกครั้งเดียวตอนเปิดหน้า (มิเรอร์ `PopClipView`'s `_recordView()` ทุกจุด) — `HomeFeedItem.toDrop()` ต้องส่ง `viewCount` ต่อเข้า `Drop` ด้วย (ตอนนี้ยังไม่ส่ง) — `HomeDropCard` เพิ่ม icon+เลข view count มิเรอร์ `HomePopCard` บรรทัดที่ใช้ `Icons.visibility_outlined`
5. ค่าคงที่ rate-limit (20/60s) และ velocity-cap (50/10s) ให้ประกาศเป็นตัวแปร/comment อธิบายเหตุผลไว้ต้นฟังก์ชัน SQL ชัดเจนว่าเป็นค่าที่ Product กำหนดเองชั่วคราว ปรับได้ภายหลัง (มิเรอร์ที่ WYN-037 ทำกับกรอบเวลา 30 นาที/30 วัน)

Handoff: AI Design — ยืนยัน UI ตำแหน่ง/สไตล์ icon view count บน `DropDetailScreen`/`HomeDropCard` (มิเรอร์ Pop ที่มีอยู่แล้วให้มากที่สุดเพื่อความสม่ำเสมอทั้งแอป), ทบทวน schema/RPC/RLS design ข้อ 1-5 ด้านบนก่อนส่งต่อ AI Coding — โดยเฉพาะจุดที่ 3 (privacy ของ `drop_views` SELECT policy + `drop_view_count()` SECURITY DEFINER) ที่เป็นจุดเสี่ยงด้าน security สูงสุดของ task นี้ ต้องตรวจทานให้แน่ใจก่อน

---

## Coding Output (2026-08-23)

**SQL** (`supabase/schema.sql`, ต่อท้ายส่วน WYN-037): ตารางใหม่ `drop_views` (`drop_id`/`viewer_id` references + `created_at`, **primary key (drop_id, viewer_id)** เป็นกลไก unique-viewer dedup ตัวเดียวจบ) + index บน `created_at` รองรับ query ช่วงเวลาของ rate-limit/velocity-cap — **ไม่มี raw client INSERT/UPDATE/DELETE policy บนตารางนี้เลย** มีแค่ SELECT policy เดียวจำกัด `auth.uid() = viewer_id` (ตรงตาม Design ยืนยันไว้ ไม่เปิด select-all-authenticated แบบ `drop_likes` เด็ดขาด) — `record_drop_view(p_drop_id uuid)` (SECURITY DEFINER) เช็คตามลำดับ a→d แล้ว no-op เงียบๆ (ไม่ raise) ถ้าไม่ผ่าน: (a) Drop ไม่มีอยู่จริง/ถูก soft-delete (เช็ค `v_drop.deleted_at is not null` ตรงจาก row ที่ดึงมาแล้ว แทนที่จะเรียก `internal.is_drop_deleted()` ซ้ำอีกรอบ — ความหมายเดียวกันทุกประการ แค่ไม่ query ซ้ำ) (b) `auth.uid()` คือเจ้าของ Drop เอง (c) บัญชีนี้ insert เกิน 20 ครั้งใน 60 วิที่ผ่านมา (d) Drop นี้มี insert เกิน 50 ครั้งใน 10 วิที่ผ่านมา — ผ่านทุกข้อค่อย `insert ... on conflict (drop_id, viewer_id) do nothing` (กันเผื่อ race ซ้อน primary key อีกชั้น) — ทั้งสองค่าคงที่มี comment อธิบายไว้ตรงจุดที่ใช้ว่าเป็นค่าที่ Product กำหนดเองชั่วคราว ปรับได้ภายหลัง (มิเรอร์สไตล์ WYN-037's 30 นาที/30 วัน) — `drop_view_count(p_drop_id uuid) returns bigint` (SECURITY DEFINER, `stable`) bypass RLS นับทุกแถวจริง คืนแค่ตัวเลขไม่คืน viewer identity ใดๆ — แก้ `home_feed`/`saved_feed` view (append ใหม่ทั้งก้อนต่อท้ายไฟล์ มิเรอร์วิธีที่ WYN-034/WYN-035 เคยทำตอนเพิ่มคอลัมน์ ไม่แก้ history เดิมในที่เก่า) ให้ Drop branch ทั้งสองจุดของ `home_feed` (top-level + ReDrop branch) และจุดเดียวของ `saved_feed` เรียก `public.drop_view_count(d.id) as view_count` แทน `null::bigint` — **Pop's branch ทั้งสองจุด (`p.view_count` ทั้งใน `home_feed`/`saved_feed`) ไม่ถูกแตะเลย** ตามขอบเขตที่ Product กำหนด

**Flutter**: `Drop` model เพิ่ม `viewCount` (`int`, default `0` มิเรอร์ pattern เดียวกับ `redropCount` — ไม่ required เพื่อไม่ให้ call site เดิมพัง) + `withExtraView()` มิเรอร์ `Pop.withExtraView()` เป๊ะๆ + parse `view_count` แบบ defensive ใน `Drop.fromMap()` (default 0 เมื่อไม่มี key เพราะ `_dropSelect` ยังไม่ได้ขอคอลัมน์นี้จริงๆ — ดู "Known Issues/Gap" ด้านล่าง) — `DropRepository.recordView(dropId)` เรียก RPC `record_drop_view` มิเรอร์ `PopRepository.recordView()` — `HomeFeedItem.toDrop()` ส่ง `viewCount: viewCount ?? 0` ต่อเข้า `Drop` ตามที่ Design สั่ง — `DropDetailScreen` เพิ่ม `_viewRecorded` flag + `_isOwnDrop` getter, เรียก `_recordViewOnce()` ผ่าน `Future.microtask()` ใน `initState()` (ไม่เรียก `setState()` ตรงๆ ระหว่าง `initState()` กำลังรัน — ใช้ microtask แทนเพื่อคง posture เดียวกับที่ `PopClipView` ใช้ async gap หลัง `await controller.initialize()` แม้ Drop จะไม่มีขั้นตอน async ให้รอจริงก็ตาม) — ถ้า `_isOwnDrop` ข้ามการเรียก RPC ตั้งแต่ต้นไม่ต้องรอ server ปฏิเสธเงียบๆ — เพิ่ม Component 1 (`DropDetailScreen`, icon ไม่ระบุ size พิเศษ) และ Component 2 (`HomeDropCard`, `size: 20` ตาม convention ของไฟล์นั้น) ตามตำแหน่ง/ลำดับที่ Design spec ระบุเป๊ะๆ (ก่อน `Spacer()`/ปุ่ม Save) — ทั้งสองจุดห่อด้วย `Semantics(label: 'เข้าชมแล้ว $viewCount ครั้ง', excludeSemantics: true)` (ต่างจาก Pop เดิมที่ไม่มี ตามที่ Design สั่งไว้ชัดเจน) ไม่ใช้ `IconButton`

**บั๊ก/gap จริงที่พบและแก้เองระหว่างเขียนโค้ด (ก่อนส่ง QA)**:
1. **`HomeFeedItem.fromDrop()` ไม่เคยส่ง `viewCount` ต่อ** — ถ้าไม่แก้ `HomeDropCard` ที่ build จาก `HomeFeedItem.fromDrop(drop)` (ใช้จริงใน `hashtag_feed_screen.dart`'s Drop branch) จะโชว์ข้อความ `"null"` ตรงๆ บนหน้าจอแทนตัวเลข เพราะ `HomeFeedItem.viewCount` เป็น `int?` ไม่มี default — แก้โดยส่ง `viewCount: drop.viewCount` ต่อ (ซึ่ง `Drop.viewCount` มี default 0 อยู่แล้วเสมอ)
2. **regression test เดิมที่มีอยู่แล้วพังจริง**: `home_feed_screen_test.dart`'s `'renders a mix of Drop and Pop cards with type-specific UI'` เดิมยืนยันว่า `Icons.visibility_outlined` มีแค่ชิ้นเดียว (เฉพาะการ์ด Pop) — พอเพิ่ม icon นี้ให้ Drop ด้วยแล้ว จะเจอ 2 ชิ้นและ assertion เดิม (`findsOneWidget`) จะ fail — แก้เป็น `findsNWidgets(2)` พร้อม comment อธิบายเหตุผล และแก้ helper `_dropItem()`/`_pollItem()` ในไฟล์เดียวกันให้ default `viewCount: 0` (ไม่งั้นการ์ด Drop ในทุกเทสต์ของไฟล์นี้จะโชว์ `"null"` เช่นกัน) — เจอ 2 จุดเดียวกันซ้ำใน `view_profile_screen_test.dart` (fixture `HomeFeedItem` ที่ render ผ่าน `ProfileRedropsTab`'s `HomeDropCard`) แก้ด้วยวิธีเดียวกัน
3. ไม่มีบั๊กจริงฝั่ง SQL — ทุก check ของสคริปต์ใหม่ผ่านตั้งแต่รอบแรกที่รันจริง (ไม่ต้อง debug ย้อนกลับ)

**Known Issue/Gap (ตั้งใจ ไม่ใช่บั๊ก แจ้ง QA/Product ให้ตัดสินใจ)**: `Drop.viewCount` มีค่าจริงตรงกับ DB **เฉพาะ** Drop ที่ถูกเปิดผ่าน `HomeFeedItem.toDrop()`/`fromDrop()` (คือทาง Home Feed/Saved/Hashtag Feed/ReDrop ทุกทาง) — ทางอื่นของ `DropRepository` (`fetchById` ที่เปิดจาก notification, `fetchByAuthor`/`searchByCaption` ที่เปิดจาก Profile grid/Search) **ยังคืน 0 เสมอ** เพราะ `_dropSelect` ยังไม่ได้ขอคอลัมน์ `view_count` (ต้องเรียก `drop_view_count()` แยกต่อ Drop เพิ่ม ซึ่ง Design's Handoff list ไม่ได้ระบุให้ทำในรอบนี้ — ระบุแค่ `HomeFeedItem.toDrop()` เท่านั้น) — ผลคือถ้าผู้ใช้เปิด `DropDetailScreen` ของ Drop เดียวกันจาก Profile grid/Search/Notification แทนที่จะมาจาก Home/Saved Feed จะเห็นเลข View เป็น 0 เสมอแม้ Drop นั้นมี View จริงแล้ว (ตัวเลขจะกลับมาถูกต้องทันทีถ้าเปิดผ่าน Home/Saved Feed) — Coding เลือกทำตาม Handoff list ตรงๆ ตามกติกา "ทำการเปลี่ยนแปลงที่เล็กที่สุด" แทนที่จะขยาย scope เอง แต่ต้องแจ้งไว้ชัดเจนให้ QA/Product ตัดสินใจว่าเป็น gap ที่ยอมรับได้รอบนี้หรือต้อง fast-follow

**Tests (Flutter, ยังไม่ได้รันจริง — ดู Build ด้านล่าง)**: เพิ่ม 10 เคสใหม่ตาม pattern เดิม —
- `drop_test.dart`: +3 (`viewCount` default 0, `withExtraView()` bump ไม่กระทบ count อื่น, `Drop.fromMap` parse/default `view_count`)
- `home_feed_item_test.dart`: +3 (`toDrop()` ส่ง `viewCount` จริง, `toDrop()` default 0 เมื่อ `view_count` เป็น null, กลุ่มใหม่ `HomeFeedItem.fromDrop` 2 เคสยืนยัน forward `Drop.viewCount` ถูกต้อง) และแก้ 1 เคสเดิมให้ตั้ง `view_count: 7` แทน `null` เพื่อยืนยันค่าจริงถูกส่งผ่าน
- `drop_detail_screen_test.dart`: +4 กลุ่มใหม่ `View count (WYN-038)` — เปิด Drop คนอื่นเรียก `recordView` ครั้งเดียว+bump optimistic, เปิด Drop ตัวเองไม่เรียก `recordView` เลย, `recordView` ไม่เรียกซ้ำแม้ rebuild จากปุ่ม Like, Semantics label มีจริง
- `home_feed_screen_test.dart`/`view_profile_screen_test.dart`: แก้ fixture/assertion ตามข้อ 2 ใน "บั๊ก/gap ที่พบ" ด้านบน (ไม่ใช่เคสใหม่ แต่จำเป็นเพื่อไม่ให้ suite เดิมพัง)
- `RecordingDropRepository`: เพิ่ม `recordViewCalls`/`recordViewArgs` มิเรอร์ `RecordingPopRepository` เดิมทุกจุด

**Build/Tests — รันไม่ได้จริงเพราะ infra ไม่ใช่เพราะโค้ดพัง**: sandbox ของ session นี้ไม่มี Flutter/Dart SDK ติดตั้งอยู่เลย (`flutter`/`dart` ไม่มีใน PATH, ค้นทั้งระบบไม่เจอ Flutter SDK ที่ไหน, และ proxy ที่มีให้ไม่อนุญาตให้ดาวน์โหลด Flutter SDK จาก `storage.googleapis.com`/`github.com` — ตรวจสอบแล้วด้วย `curl` ได้ 400 ทั้งคู่) **จึงรัน `flutter analyze`/`flutter test` จริงไม่ได้ในรอบนี้** — ไม่ขอยืนยันตัวเลขที่ไม่ได้รันจริงตามกติกา แทนที่ด้วยการตรวจสอบทดแทนที่ทำได้จริงในสภาพแวดล้อมนี้: (1) อ่าน diff ทุกไฟล์ที่แก้ทวนกับ pattern เดิมของโปรเจกต์บรรทัดต่อบรรทัด (2) ตรวจสมดุลวงเล็บ/เครื่องหมายปีกกาทุกไฟล์ที่แก้ด้วยสคริปต์นับอัตโนมัติ ผ่านทุกไฟล์ (3) ไล่หา call site ทุกจุดที่ construct `HomeFeedItem`/`Drop` ตรงๆ ทั่วทั้ง `app/test/` และ `app/lib/` เพื่อยืนยันไม่มีจุดไหนพลาด `viewCount` จนโชว์ `"null"` (พบ 2 จุดจริงตามที่แจ้งไว้ข้างบน แก้แล้วทั้งคู่) (4) เทียบ semantics/API signature กับ `Pop`/`PopClipView`/`HomePopCard`'s ของเดิมทุกจุดที่ Design สั่งให้มิเรอร์ — **บาลานซ์ Flutter test baseline เดิม 597/597 ก่อน task นี้ (ยืนยันจาก `.wyn/tasks/approved/WYN-037-edit-delete-drop.md`) + เคสใหม่ 10 เคส = คาดว่าควรเป็น 607/607 แต่ยังไม่มีการยืนยันด้วยการรันจริง** — ต้องให้ AI QA & Security หรือ session ถัดไปที่มี Flutter SDK รัน `flutter analyze`/`flutter test` อิสระก่อนอนุมัติ ห้ามเชื่อตัวเลขคาดการณ์นี้แทนการรันจริงเด็ดขาด

**SQL live verification — รันได้จริงและผ่านจริง**: เขียน `supabase/tests/wyn_038_view_counting_test.sh` ใหม่ (มิเรอร์ harness ของ `wyn_037_edit_delete_drop_test.sh` เป๊ะๆ) รันภายใต้ role `authenticated` จริงผ่าน local PostgreSQL 16 — **29/29 checks PASS** ครอบ: unique-viewer dedup (เรียกซ้ำ 3 ครั้งไม่นับเพิ่ม), self-view exclusion (เจ้าของดูเองไม่นับและไม่มี row ถูก insert เลยด้วยซ้ำ), หลายคนดู Drop เดียวกันนับถูกต้องตามจริง, rate-limit ต่อบัญชี (25 Drop ต่างกัน เรียกรัวๆ เหลือแค่ 20 แถวไม่ error), velocity-cap ต่อ Drop (55 บัญชีต่างกันถล่ม Drop เดียว เหลือแค่ 50 แถวไม่ error), SELECT policy เห็นได้แค่แถวตัวเอง (พิสูจน์ทั้งแบบ unfiltered count และ filter ตรงหา viewer คนอื่น), `drop_view_count()` คืนเลขตรงกันไม่ว่าเจ้าของ/ผู้ดู/คนแปลกหน้าเรียก, Drop ที่ soft-delete แล้วรับ View เพิ่มไม่ได้อีก (ไม่ error), regression Like/Comment/Save/ReDrop/Poll ไม่กระทบกัน, Pop's `increment_pop_view_count()`/`view_count` เดิมยังทำงานแบบเดิมไม่เปลี่ยน, `home_feed`/`saved_feed` ทั้ง Drop branch + ReDrop branch + Pop branch คืนค่าตรงกับที่คาดไว้, **ไม่มี raw client INSERT policy จริง** (ทดสอบ insert ตรงบายพาส RPC แล้วยืนยันโดน RLS ปฏิเสธจริง) — รันซ้ำ 11 สคริปต์เดิมทั้งหมด (`wyn_021`/`wyn_027`/`wyn_029`–`wyn_037`) พร้อมกับสคริปต์ใหม่ **ผ่านหมดทั้ง 12 สคริปต์ ไม่มี cross-task regression**

**Acceptance Criteria — ไล่ตรวจครบทุกข้อจาก Product spec**: ครบทุกข้อ (View เพิ่ม 1 ครั้งแรกที่เปิด Drop คนอื่น, เปิดซ้ำไม่เพิ่มอีก [ยืนยันด้วย SQL ตรง], เจ้าของเปิดเองไม่เพิ่ม, คนละคนเปิด Drop เดียวกันนับตามจริง, rate-limit เกินโควตาไม่นับ+ไม่ error, velocity-cap เกินโควตาไม่นับ+ไม่ error, SELECT policy เห็นแค่แถวตัวเอง+View count รวมถูกต้องสำหรับทุกคน, Home Feed/Drop Detail แสดง View count จริงตรงกับ DB [ยืนยันด้วย SQL — ฝั่ง Flutter UI ยังไม่ได้ยืนยันด้วย `flutter test` จริงตามที่แจ้งไว้ข้างบน], regression Drop ที่ soft-delete/Like/Comment/Save/ReDrop/Poll/Pop's view count ไม่กระทบกัน)

Handoff: AI QA & Security — **ข้อสำคัญที่สุดสำหรับรอบนี้**: sandbox ของ Coding ไม่มี Flutter SDK ให้รัน `flutter analyze`/`flutter test` ได้จริงเลย ต้องรันทั้งสองอย่างอิสระเป็นอันดับแรกก่อนตรวจอย่างอื่นทั้งหมด (คาดว่าควรเป็น 607/607 แต่ยังไม่ยืนยัน) — ถ้าพบ error การคอมไพล์หรือเทสต์ fail ให้ส่งกลับ Debug ทันที — นอกจากนี้ขอให้ตรวจเป็นพิเศษ: (1) `record_drop_view()`'s ลำดับเช็ค a→d ตรงตาม Product's Recommendation หรือไม่ (2) `drop_views`' SELECT policy ยืนยันว่าไม่เปิดกว้างเกิน `auth.uid() = viewer_id` จริง (3) Gap ที่แจ้งไว้ (`Drop.viewCount` เป็น 0 เสมอสำหรับ fetch path นอกเหนือ Home/Saved Feed) ว่าต้อง block หรือยอมรับเป็น fast-follow (4) `Future.microtask()` ใน `initState()` ทำงานถูกต้องตามที่ตั้งใจจริงหรือไม่เมื่อรันด้วย Flutter SDK จริง

---

## Independent QA — Round 1 (AI QA & Security, 2026-08-23) — PASS (3 บั๊กจริงพบและแก้ก่อนอนุมัติ — ทั้งหมดอยู่ในไฟล์ test ไม่ใช่โค้ด production)

**บริบท**: ทำ QA อิสระต่อจาก Coding Output ทันที ไม่เชื่อตัวเลข/สรุปที่ Coding รายงาน — งานนี้ commit ไว้แล้วเป็น WIP (`064ffd5`, ก่อน QA เริ่ม เนื่องจาก Coding ทำตาม RULES.md ข้อ "ใกล้หมด quota ต้อง commit ทันที") — ตรวจสอบ diff จริงของ commit นั้นเทียบ parent แทนการเชื่อ Coding Output อย่างเดียว

**1. Flutter SDK — ติดตั้งสำเร็จ และรันจริงทั้งสองคำสั่งอิสระ**: sandbox ของ QA เองก็ไม่มี Flutter SDK ติดตั้งไว้ล่วงหน้าเหมือนที่ Coding เจอ (`which flutter dart` ว่างเปล่า) แต่ทดลองดาวน์โหลดเองก่อนยอมแพ้ตามที่สั่ง — `curl https://storage.googleapis.com/flutter_infra_release/releases/releases_linux.json` ได้ HTTP 200 จริง (ตรงข้ามกับที่ Coding รายงานว่าได้ 400 — อาจเป็นเพราะ proxy policy ต่างกันระหว่าง session หรือ URL ที่ลองต่างกัน) ดาวน์โหลด Flutter stable 3.47.1 (`flutter_linux_3.47.1-stable.tar.xz`, ~1.5GB) จาก `storage.googleapis.com` สำเร็จ แตกไฟล์ที่ scratchpad, `flutter pub get` ใน `app/` สำเร็จ (deps resolve ปกติ) จากนั้นรัน **`flutter analyze` อิสระจริง: 0 issues** และ **`flutter test` อิสระจริง**

**2. รอบแรกของ `flutter test` เจอ 3 เทสต์ fail จริง (604 pass / 3 fail จาก 607 ทั้งหมด)** — ไล่ debug แต่ละเคสจนเจอ root cause จริงแล้วแก้ทั้งหมด (ทุกจุดเป็นบั๊กในไฟล์ test ที่ Coding เขียน/แก้ ไม่ใช่บั๊กในโค้ด production — โค้ด `schema.sql`/`drop.dart`/`drop_repository.dart`/`drop_detail_screen.dart`/`home_feed_item.dart`/`home_drop_card.dart` ไม่ต้องแก้อะไรเลย):

- **บั๊กจริงจุดที่ 1 — `drop_comment_delete_test.dart` (เทสต์เดิมจาก WYN-005 พังเพราะ WYN-038)**: เทสต์ `'deleting an own comment...'` เดิม scroll หา `find.text('1')` เพื่อยืนยัน comment count ลดลงเหลือ 1 หลังลบคอมเมนต์ — Drop fixture ของเทสต์นี้เป็นของ `'someone-else'` ไม่ใช่เจ้าของ ('me' เป็นผู้เปิดดู) ทำให้ `_recordViewOnce()` ใหม่ของ WYN-038 ยิง optimistic bump `viewCount: 0 → 1` ทันทีตอนเปิดหน้า กลายเป็นมี `Text('1')` สองตัวพร้อมกัน (comment count กับ view count) → `find.text('1')` เจอ 2 ตัวใน `scrollUntilVisible` → `StateError: Bad state: Too many elements` — ยืนยัน **red จริง** (รันแยกไฟล์นี้อย่างเดียวก็ fail เหมือนกัน ไม่ใช่แค่ order-dependent) ก่อนแก้ **แก้**: ตั้ง `viewCount: 10` ให้ Drop fixture ของเทสต์นี้ (ค่าใดๆ ที่ทำให้ 10/11 ไม่ชนกับ 2/1 ของ comment count) — รันซ้ำผ่าน **green จริง**
- **บั๊กจริงจุดที่ 2 — `drop_detail_screen_test.dart`'s Semantics test ใหม่ ผิดสมมติฐาน 2 ชั้นซ้อนกัน**: เทสต์ใหม่ `'the view count has a Semantics label'` ใช้ `find.bySemanticsLabel('เข้าชมแล้ว 42 ครั้ง')` (exact string match) แล้วคาดหวัง `findsOneWidget` — เขียน debug test แยกต่างหาก dump semantics tree จริงเพื่อหา root cause แทนการเดา พบว่า interaction row ทั้งแถวของ `DropDetailScreen` (Like/Follow/ReDrop/Save/View ทุก label) **ไม่มี semantics boundary (`container: true`) เลยสักจุดเดียว** ทำให้ label ทั้งหมดถูก merge รวมเป็น SemanticsNode เดียวกับ header image (เป็น gap ที่มีอยู่ก่อน WYN-038 แล้ว ตั้งแต่ WYN-005/008/013 — task นี้แค่เป็นเทสต์แรกในโปรเจกต์ที่ลองใช้ `bySemanticsLabel` แบบ exact-match กับจุดนี้ จึงเพิ่งถูกจับได้) → exact-match ล้มเหลวเสมอไม่ว่า Drop id/scroll position ใดๆ — แก้เป็น `find.bySemanticsLabel(RegExp('เข้าชมแล้ว 42 ครั้ง'))` (substring match ตามที่ doc comment ของ `bySemanticsLabel` เองแนะนำไว้สำหรับกรณี merged semantics) — ยืนยัน label ที่ config ไว้ถูกต้องมีอยู่จริงในเนื้อหาที่ merge ออกมา ไม่ใช่การพิสูจน์ว่า screen reader จะประกาศแยกเป็นอิสระได้ (นั่นต้องแก้ production code เพิ่ม semantics boundary ทั้งแถว ซึ่งกระทบ Like/Follow/ReDrop/Save ด้วยไม่ใช่แค่ View — อยู่นอกขอบเขต task นี้ **บันทึกเป็น Minor finding ไม่ block** ด้านล่าง) — พอแก้ assertion แล้วเทสต์ผ่านต่อไปจนจบ (ไม่ short-circuit ที่ `expect()` เหมือนก่อน) แล้วไปเจอ**บั๊กจริงจุดที่ 3 ซ้อนอยู่หลังจุดนี้ทันที**
- **บั๊กจริงจุดที่ 3 — เทสต์เดียวกัน สร้าง `RecordingDropRepository()` inline ในตัว testWidgets body**: ไฟล์นี้มี convention ที่เขียนไว้ชัดเจนแล้วตั้งแต่ WYN-035/WYN-037 (comment ต้นไฟล์: "constructed here rather than inline inside a testWidgets body, same 'avoid a leaked GoTrue auto-refresh timer at teardown' reasoning") — เทสต์ Semantics ใหม่ของ WYN-038 ละเมิด convention นี้ (`dropRepository: RecordingDropRepository()` inline แทนที่จะประกาศใน `setUpAll` เหมือนตัวอื่นทุกตัวในไฟล์) — เดิมไม่มีใครเจอเพราะ assertion ก่อนหน้า (บั๊กจุดที่ 2) fail ก่อนเสมอ ทำให้ teardown check ไม่เคยรันถึง — พอแก้บั๊กจุดที่ 2 แล้ว เทสต์รันจนจบและ `_verifyInvariants` เจอ Timer ค้างจริง (`A Timer is still pending even after the widget tree was disposed`) — ยืนยัน **red จริง** ก่อนแก้ — **แก้**: เพิ่มตัวแปร `viewCountSemanticsTestRepo` ประกาศใน `setUpAll` ตาม convention เดิมทุกจุด ย้ายการสร้าง repo ออกจาก testWidgets body — รันซ้ำผ่าน **green จริง**

**3. หลังแก้ทั้ง 3 จุด — รัน `flutter analyze`/`flutter test` เต็มไฟล์ซ้ำอิสระอีกครั้ง (ไม่ใช่แค่ไฟล์ที่แก้)**: **`flutter analyze`: 0 issues**, **`flutter test`: 607/607 ผ่านทั้งหมด** (597 เดิมก่อน task + 10 เคสใหม่ของ WYN-038 ตามที่ Coding ประมาณการไว้ ยืนยันด้วยการรันจริงตามที่สั่ง ไม่ใช่แค่คาดการณ์) รันซ้ำอีกรอบเพื่อยืนยันไม่ flaky — ผลเดิมทุกครั้ง

**4. SQL — รันอิสระซ้ำทั้งหมดจริง (ไม่เชื่อตัวเลขจาก Coding)**: local PostgreSQL 16 มีอยู่แล้วในเครื่อง (`ps aux` เจอ process รันอยู่) — รัน `bash supabase/tests/wyn_038_view_counting_test.sh` อิสระเอง: **29/29 checks PASS** ตรงกับที่ Coding รายงานทุกตัว — รัน 11 สคริปต์เดิมทั้งหมดอิสระต่อ (`wyn_021`, `wyn_027`, `wyn_029`–`wyn_037`): **ผ่านหมดทั้ง 11 สคริปต์ ไม่มี cross-task regression** เช่นกัน

**5. อ่าน SQL จริงยืนยัน 4 จุด security-critical ที่ต้องตรวจเป็นพิเศษ**:
- `record_drop_view()`'s ลำดับเช็คตรงตาม Product's Recommendation เป๊ะ: (a) `v_drop is null or v_drop.deleted_at is not null` → `return` (ไม่ raise) (b) `v_drop.author_id = v_me` → `return` (c) rate-limit `count(*) ... viewer_id = v_me and created_at > now() - interval '60 seconds' >= 20` → `return` (d) velocity-cap `count(*) ... drop_id = p_drop_id and created_at > now() - interval '10 seconds' >= 50` → `return` — ทุกจุดใช้ `return` เปล่าๆ ไม่มี `raise exception` จุดไหนเลยในฟังก์ชันทั้งหมด (ยืนยันด้วยการอ่าน source ตรงๆ ทุกบรรทัด)
- `drop_views`' SELECT policy: `create policy "Users can view only their own Drop view history" on public.drop_views for select to authenticated using (auth.uid() = viewer_id);` — จำกัดแค่นี้จริง **ไม่มี** policy อื่นใดบนตารางนี้เลย (ไม่มี INSERT/UPDATE/DELETE policy ให้ client) ยืนยันด้วย CHECK16 (SQL insert ตรงบายพาส RPC ถูก RLS ปฏิเสธจริง) และ CHECK7a/7b/7c (เห็นได้แค่แถวตัวเอง ไม่เห็นแถวคนอื่นแม้ filter ตรงๆ)
- `drop_view_count()` เป็น `security definer stable` bypass RLS ของ `drop_views` ตรงๆ คืนแค่ `count(*)` ไม่คืนแถว/viewer identity ใดๆ — ยืนยันด้วย CHECK8a/8b/8c (เจ้าของ/ผู้เคยดู/คนแปลกหน้าเห็นเลขเดียวกันเป๊ะ)
- `home_feed`/`saved_feed`'s Pop branch (ทั้งสอง view) ยัง `p.view_count` ดิบเหมือนเดิมทุกตัวอักษร ไม่ถูกแตะเลย — ยืนยันด้วยการอ่าน diff ตรงๆ ใน `schema.sql` (บรรทัด Pop branch ไม่มีการเปลี่ยนแปลง) และ CHECK11/CHECK15 (SQL live)

**6. ไล่หา call site `Drop(...)`/`HomeFeedItem(...)` ทั่ว repo**: `grep` ทั่ว `app/lib` พบว่าไม่มีจุดใดสร้าง `Drop(...)` ตรงๆ นอกเหนือจากใน `drop.dart` เอง (ทุก production call site ผ่าน `Drop.fromMap()`/`copyWith()`/`toDrop()`/`withExtraView()` ซึ่งมี default 0 ครบทุกทาง) — ไม่มี call site ไหนพังจาก signature ที่เพิ่ม `viewCount` เข้ามา ยืนยันเพิ่มเติมด้วย `flutter analyze` สะอาด 0 issues (compile ผ่านทุกไฟล์จริง)

**7. Gap ข้อ 6 (`Drop.viewCount` เป็น 0 เสมอนอกเหนือ Home/Saved Feed path — Profile grid/Search/Notification/`fetchById`) — ตัดสินใจ: ยอมรับเป็น non-blocking fast-follow ไม่ block รอบนี้** เหตุผล:
   - ยืนยันด้วย `grep`/อ่าน `_dropSelect` (`'*, ...'` เทียบ `public.drops` ตรงๆ ไม่ผ่าน `home_feed`/`saved_feed`) ว่า gap มีอยู่จริงตามที่ Coding แจ้ง และกระทบจริง 4+ entry point ของ `DropDetailScreen` (`profile_drop_grid_tab.dart`, `search_drop_results_tab.dart`, `notification_list_screen.dart`, `push_notification_service.dart` — ใช้ `fetchByAuthor`/`searchByCaption`/`fetchById` ตรงๆ ไม่ผ่าน `HomeFeedItem.toDrop()`)
   - Product's Acceptance Criteria ข้อที่เกี่ยวข้องเขียนไว้แค่ "Home Feed การ์ด Drop และหน้า `DropDetailScreen` แสดง View count จริงตรงกับค่าใน DB" — ไม่ได้ระบุ Profile grid/Search/Notification เป็น entry point ที่ต้องถูกต้องด้วย และ Design's Handoff (เอกสารที่ผ่านการตัดสินใจ scope โดยเจตนา) จำกัดขอบเขตไว้ที่ `HomeFeedItem.toDrop()` เท่านั้นอย่างชัดเจน (ไม่ใช่ Coding ตัดสินใจเองฝ่ายเดียว)
   - **ไม่ใช่ security/data-integrity issue** — ค่าจริงใน DB ถูกต้อง 100% เสมอ (RPC ยังยิงและนับถูกต้องไม่ว่าจะเปิดจากทางไหน) ปัญหาแค่ "เลขเริ่มต้นที่แสดงตอนเปิดหน้า" อาจนิ่งที่ 0 ชั่วคราวถ้าเปิดจาก entry point ที่ไม่ผ่าน Home/Saved Feed แล้วจะกลับมาถูกต้องเองทันทีที่เปิดผ่าน Home/Saved Feed อีกครั้ง
   - แก้ให้ครบทุก entry point ต้องขยาย `_dropSelect`/เพิ่ม logic เรียก `drop_view_count()` ต่อแถวใน `fetchByAuthor`/`searchByCaption` (มีความเสี่ยง N+1 RPC call ต่อหน้า grid ที่ query ทีละ 21 แถว) เป็นการตัดสินใจเชิง performance/design ที่ควรผ่าน Design ก่อน ไม่ใช่สิ่งที่ QA ควรแก้เองแบบเร่งด่วนนอกกระบวนการ
   - **เสนอ**: บันทึกเป็น fast-follow task ให้ Product พิจารณาว่าจะขยายขอบเขตไปยัง Profile grid/Search/Notification หรือไม่ในรอบถัดไป

**8. ไล่เทียบ Acceptance Criteria ทั้งหมดทีละข้อกับโค้ดจริง (ไม่เชื่อ checklist ของ Coding)**: ครบทุกข้อ — เปิด Drop คนอื่นครั้งแรกนับ 1 (CHECK1), เปิดซ้ำไม่เพิ่ม (CHECK2), เจ้าของเปิดเองไม่เพิ่ม (CHECK3/CHECK3b), คนละคนนับตามจริง (CHECK4), rate-limit เกินโควตาไม่นับ+ไม่ error (CHECK5a/5b), velocity-cap เกินโควตาไม่นับ+ไม่ error (CHECK6a/6b), SELECT policy เห็นแค่แถวตัวเอง+view count รวมถูกต้องทุกคน (CHECK7a-c, CHECK8a-c), Home Feed/Drop Detail แสดงค่าจริงตรง DB (CHECK12-14 ฝั่ง SQL + `flutter test` ฝั่ง UI 20/20 ในกลุ่ม "View count (WYN-038)"), regression soft-delete/Like/Comment/Save/ReDrop/Poll/Pop (CHECK9a-b, CHECK10a-f, CHECK11, CHECK15)

**9. Regression เพิ่มเติมที่ตรวจ**: mobile/responsive — Design ยอมรับความเสี่ยง overflow ของ Row เดิมไว้แล้วอย่างมีเหตุผล (ความเสี่ยงระดับเดียวกับ Like/Comment/ReDrop count เดิมที่มีอยู่ก่อน ไม่ใช่ความเสี่ยงใหม่) ไม่มีเหตุผลให้ block เพิ่มเติม — ตรวจ secret exposure ในไฟล์ใหม่ (`wyn_038_view_counting_test.sh`, `schema.sql`) ด้วย `grep` หา password/secret/api key/token — ไม่พบ

**บั๊กที่พบทั้งหมดอยู่ในไฟล์ test เท่านั้น (ไม่มีจุดใดต้องแก้โค้ด production)** — `git diff` ของรอบ QA นี้แก้แค่ 3 ไฟล์: `app/test/drop_comment_delete_test.dart`, `app/test/drop_detail_screen_test.dart`, `app/test/home_feed_screen_test.dart`

**ผลลัพธ์**: **WYN-038 — PASS** (หลังแก้ 3 บั๊กจริงที่ QA พบ ทั้งหมดในไฟล์ test) — `flutter analyze` 0 issues, `flutter test` 607/607 (ยืนยันด้วยการรันจริงอิสระ ไม่ใช่ค่าคาดการณ์), SQL regression 29/29 ของ WYN-038 เอง + ผ่านครบทั้ง 11 สคริปต์เดิม ไม่มี cross-task regression — Gap ข้อ 6 (`Drop.viewCount` = 0 นอกเหนือ Home/Saved Feed) ตัดสินใจยอมรับเป็น non-blocking fast-follow ตามเหตุผลข้อ 7 ด้านบน

**Minor findings ไม่ block (บันทึกไว้สำหรับอนาคต)**:
- Interaction row ทั้งแถวของ `DropDetailScreen` (Like/Follow/ReDrop/Save/View) ไม่มี semantics boundary (`container: true`) เลย ทำให้ label ทั้งหมดถูก merge เป็น SemanticsNode เดียวกับ header image ในทางเทคนิค — screen reader จริงจะประกาศเป็นก้อนเดียวยาวๆ แทนที่จะแยกทีละปุ่ม แม้แต่ละปุ่มจะมี `Semantics(label: ...)` ของตัวเองก็ตาม — เป็น gap ที่มีมาตั้งแต่ WYN-005/008/013 ไม่ใช่ที่ WYN-038 สร้างใหม่ (WYN-038 แค่เป็นเทสต์แรกที่จับได้เพราะลอง `bySemanticsLabel` exact-match กับจุดนี้เป็นครั้งแรก) — เสนอ fast-follow แยก: เพิ่ม `Semantics(container: true)` ครอบแต่ละปุ่มในแถวนี้ (กระทบ Like/Follow/ReDrop/Save ด้วย ไม่ใช่แค่ View — ควรทำเป็น task เดียวแก้ทั้งแถว)
- Gap ข้อ 6 ด้านบน (`Drop.viewCount` = 0 นอกเหนือ Home/Saved Feed path)

Handoff: AI Deploy & DevOps — commit/push/PR/merge/deployment log
