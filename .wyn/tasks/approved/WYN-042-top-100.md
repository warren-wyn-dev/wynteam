# Product Task — WYN-042

Status: approved (Independent QA PASS 2026-08-23 — ดูหัวข้อ "Independent QA" ท้ายไฟล์ — ส่งต่อ AI Deploy & DevOps)
Owner: AI Product Manager

Feature: WYN Top 100 — หน้า leaderboard เนื้อหายอดนิยมประจำสัปดาห์ (Content, ไม่รวม Creator รอบนี้)

Goal: ปิด task สุดท้ายของ Phase 4 (Discovery & Trending Engine) ตาม roadmap — สร้างหน้า "WYN Top 100" ตาม Master Spec section 13 ("หน้า Discovery: ... WYN Top 100 (จัดอันดับ Content/Creator ตาม Algorithm) ...") ที่ WYN-040 ยกสโคปไว้ให้ task นี้โดยเฉพาะตั้งแต่ต้น — ใช้ anti-manipulation engine ที่ WYN-041 เพิ่งสร้างเสร็จ (`engagementScore()`/`authors_posting_blocked()`) เป็นฐานตรงๆ ไม่ต้องคิดสูตรใหม่

Target User: ผู้ใช้ WYN Social ที่อยากเห็น "เนื้อหาที่ปังที่สุดในรอบสัปดาห์" แบบจัดอันดับเต็ม (ไม่ใช่แค่ preview 30 รายการของ Discovery's Trending Now) — เป็น aspirational surface ที่ผู้สร้างเนื้อหาอยากติดอันดับด้วย

Problem: ยืนยันแล้วจากโค้ดจริง — Discovery's "Trending Now" (WYN-040, reuse `HomeRepository.fetchTrending()` ของ WYN-017) แสดงได้สูงสุด ~30 รายการจาก candidate window 48 ชั่วโมง/100 candidate เท่านั้น (`HomeRepository.trendingCandidateLimit = 100`, `_trendingWindow = Duration(hours: 48)`) ไม่มีหน้าแยกที่แสดงอันดับเต็ม 100 อันดับ ไม่มีแนวคิด "ranking แบบสัปดาห์" ที่กว้างกว่า rolling 48 ชั่วโมง และ**ไม่มีตัวเลขอันดับ (#1, #2, ...) แสดงให้ผู้ใช้เห็นเลยที่ไหนในระบบ** — Master Spec ระบุ Top 100 แยกจาก Trending Now ชัดเจน (section 13 แจกแจงเป็นรายการแยกกัน "Trending Now, ..., WYN Top 100, ...") ยืนยันว่าตั้งใจให้เป็นคนละ surface กัน ไม่ใช่แค่ Trending Now ที่ยาวขึ้น

Requirements:

**1. ขอบเขต V1: Top 100 Content เท่านั้น — ไม่รวม Top 100 Creator ในรอบนี้**
- Master Spec เขียนว่า "จัดอันดับ Content/Creator" (มีทั้งสองแบบ) — ตัดสินใจทำแค่ **Content ranking** (Drop+Pop รวมกัน มิเรอร์ "Trending Now" ที่รวม content type ทั้งสองอยู่แล้ว) ก่อนในรอบนี้ เหตุผล: **ไม่มี infra ใดๆ ในระบบที่รวมคะแนน engagement ของ "ทุกโพสต์ของ creator คนหนึ่งรวมกันเป็นคะแนนเดียว"** เลย — สิ่งที่ใกล้เคียงที่สุดที่มีอยู่คือ `follower_count()` (WYN-039, ใช้กับ Suggested Users) ซึ่งวัด "จำนวนคนติดตาม" ไม่ใช่ "คุณภาพ/ความนิยมของเนื้อหาที่โพสต์" เอาแบบนั้นมาทำ "Top 100 Creator" จะกลายเป็นแค่ Suggested Users เรียงแบบเดียวกันซ้ำ ไม่ได้สื่อความหมาย "Creator ที่เนื้อหาปังที่สุด" จริง — การสร้าง creator-level aggregate score ที่ถูกต้อง (รวม engagement ข้ามทุก Drop/Pop ของแต่ละคน หารด้วยจำนวนโพสต์เพื่อความเป็นธรรม ป้องกัน spam-post-many ชนะ ฯลฯ) เป็นงานคนละขนาดกับ task นี้ **เสนอเป็น WYN-04X ใหม่แยกต่างหาก** (ดู Recommendation) ถ้า Founder ต้องการ Creator leaderboard ด้วย
- Top 100 Content = ranking เดียวกันของ Drop+Pop รวมกัน (ไม่แยก tab ต่อประเภท) มิเรอร์ "Trending Now" section ที่ WYN-040 ทำไว้แล้ว

**2. Ranking formula: reuse `engagementScore()` ของ WYN-041 ตรงๆ — ไม่คิดสูตรใหม่**
- ใช้ฟังก์ชัน `engagementScore()` เดิมเป๊ะ (like×2 + comment×3 + Drop-only-view×0.1) — **ไม่เพิ่มสัญญาณใหม่** (ReDrop count/Save count/Engagement Rate/Growth Velocity/Unique viewer ratio ที่ Master Spec section 14 ระบุไว้เป็น "Trending Score พิจารณา" ทั้งหมด) เพราะ WYN-041 เพิ่งตัดสินใจไว้แล้วว่าสัญญาณเหล่านี้ยังไม่มี infra รองรับ/เป็นสโคปที่ใหญ่กว่า (WYN-041's Risks ระบุชัดว่า "ไม่ใช่ full anti-manipulation") — Top 100 ควรใช้สูตรเดียวกับ Trending Now/สำหรับคุณ เพื่อความสอดคล้องของทั้งระบบ ไม่ใช่มีสูตร "ความนิยม" 3 แบบต่างกันในแอปเดียว
- **Anti-manipulation**: reuse `authors_posting_blocked()` RPC ของ WYN-041 ตรงๆ (exclude เนื้อหาของบัญชีที่โดน sanction ที่ยัง active อยู่ ออกจาก ranking — เนื้อหายังเห็นได้ปกติทางอื่นเหมือนที่ WYN-041 ทำไว้) — ไม่ต้องเขียน logic ใหม่

**3. Time window: 7 วัน (ยาวกว่า Trending Now's 48 ชั่วโมง) — เป็นเหตุผลหลักที่ทำให้ Top 100 ต่างจาก Trending Now จริง**
- ถ้าใช้ window เดียวกับ Trending Now (48 ชม.) Top 100 จะแทบไม่ต่างจาก Trending Now ที่ยาวขึ้นเฉยๆ (ผู้ใช้จะสับสนว่ามีสองหน้าทำไม) — เสนอ **7 วัน** ให้ความรู้สึก "สรุปสัปดาห์" ที่ Master Spec's "จัดอันดับ" สื่อถึง (เหมือน weekly chart ของแพลตฟอร์มเพลง/วิดีโอทั่วไป) ตัวเลขที่แน่นอนให้ Design/Coding ปรับได้ (เหมือนที่ WYN-038/041 เคยทำกับตัวเลขชั่วคราวอื่นๆ)
- Candidate pool ต้องกว้างกว่า 100 พอสมควรเพื่อให้การจัดอันดับมีความหมายจริง (ถ้า candidate = 100 พอดี ก็แค่แสดงทุกอย่างที่มีไม่ใช่การ "คัดสุดยอด 100 จาก...") — ตัวเลข candidate limit ที่แน่นอนให้ Design/Coding กำหนด (เสนอเริ่มที่ 500 เป็นจุดเริ่มต้น)
- **ไม่แก้ `HomeRepository.fetchTrending()`/`_trendingWindow`/`trendingCandidateLimit` เดิมแม้แต่บรรทัดเดียว** (ของ Trending Now/Discovery ยังคง 48 ชม./100 candidate เหมือนเดิมทุกประการ ไม่กระทบ WYN-017/040 ที่ผ่าน QA แล้ว) — Top 100 ควรเป็น method/entry point ใหม่แยกต่างหาก ไม่ใช่การปรับพารามิเตอร์ของของเดิม

**4. แสดงตัวเลขอันดับ (#1–#100) ให้ผู้ใช้เห็นได้ — ต่างจาก Rising/Suggested Users**
- WYN-040's Rising section ตั้งใจ**ไม่**โชว์ตัวเลข follower-growth ดิบเพื่อกัน gaming metric — แต่ Top 100 นี้**ต่างกันโดยธรรมชาติของ feature เอง**: การโชว์ "อันดับที่เท่าไหร่" (ไม่ใช่ตัวเลข engagement ดิบ) เป็นจุดประสงค์หลักของ leaderboard ทุกที่ (App Store/Spotify Charts/YouTube Trending ล้วนโชว์อันดับ) — โชว์ได้แต่**ยังคงหลักการเดิม: ไม่โชว์ตัวเลข engagement ดิบ/คะแนนที่แท้จริง** (เช่นไม่บอกว่า "engagementScore = 847.3") โชว์แค่ลำดับที่ (#1, #2, ...) เท่านั้น เพื่อไม่ให้เป็นเป้าให้ gaming ตัวเลขที่แท้จริงเหมือนที่ WYN-040 กังวลไว้กับ Rising

**5. Entry Point — ไม่แตะ Bottom Nav (WYN-024 ล็อกไว้แล้ว)**
- WYN-040's Design doc ระบุ Non-goal ไว้ชัดว่า "ไม่มี 'ดูเพิ่มเติม'/pagination แยกต่อ section รอบนั้น ... ถ้าต้องขยายเป็นหน้าเต็มแยกต่อ section ในอนาคต ให้เป็น task ใหม่" — **task นี้คือ task ใหม่นั้นพอดี** สำหรับ section "Trending Now" โดยเฉพาะ
- **ข้อเสนอแนะ (Product's Recommendation, ให้ Design ตัดสินใจสุดท้าย)**: เพิ่มปุ่ม/ลิงก์ "ดู Top 100" ท้าย Discovery's "Trending Now" grid section (`DiscoveryView`, WYN-040) เปิดหน้าใหม่เต็มจอ (`Top100Screen` หรือชื่อเทียบเท่า) — ไม่เพิ่ม section ที่ 6 เข้าไปใน `DiscoveryView` เอง (จะทำให้หน้า Discovery ยาวเกินไป และ "Top 100" สมควรเป็นหน้าที่มีความรู้สึก "พิเศษ/จัดอันดับเต็ม" แยกออกมาชัดเจน ไม่ใช่แค่อีก horizontal list หนึ่งปนกับ section อื่น)

Acceptance Criteria:
- [ ] มีหน้า Top 100 ใหม่ แสดง Drop+Pop รวมกัน สูงสุด 100 รายการ เรียงจาก `engagementScore()` มาก→น้อย ในหน้าต่างเวลา 7 วัน แต่ละรายการแสดงเลขอันดับ (#1–#100) ชัดเจน
- [ ] เนื้อหาจากบัญชีที่โดน moderation sanction ที่ยัง active อยู่ (reuse `authors_posting_blocked()`) ไม่ติดอันดับ Top 100 แม้ engagement จะสูงแค่ไหนก็ตาม
- [ ] ไม่มีตัวเลข engagement ดิบ/คะแนนจริงแสดงให้ผู้ใช้เห็นที่หน้า Top 100 (โชว์แค่อันดับ)
- [ ] กดเข้าจาก Discovery's "Trending Now" section → เปิดหน้า Top 100 เต็มจอถูกต้อง
- [ ] แตะรายการใน Top 100 → เปิดหน้าปลายทางตามประเภทเหมือนที่ Trending Now ทำ (Drop/Pop detail)
- [ ] Regression เต็มชุด: Trending Now (WYN-017/040)/สำหรับคุณ (WYN-018/041)/Discovery ทั้ง 5 section เดิม ทำงานเหมือนเดิมทุกจุด ไม่มี regression จาก task นี้ (ไม่แตะ `fetchTrending()`/`_trendingWindow`/`trendingCandidateLimit` เดิมเลย)

Dependencies: WYN-017 (`fetchTrending()` pattern, candidate-fetch-then-rank), WYN-018/041 (`engagementScore()`, `authors_posting_blocked()`), WYN-039 (`follower_count()` — ไม่ใช้ในรอบนี้แต่เกี่ยวกับ Creator ranking ที่เลื่อนออกไป), WYN-040 (Discovery page, entry point)

Priority: P1 — task สุดท้ายของ Phase 4 (Discovery & Trending Engine) ตาม roadmap

Risks:
- **Time window 7 วัน/candidate limit 500 เป็นค่าที่ Product กำหนดเองชั่วคราว** ไม่มีข้อมูล traffic จริงมาอ้างอิง (แอปยังไม่ deploy จริง) เหมือนตัวเลขชั่วคราวอื่นๆ ของ WYN-038/041 — ปรับได้ภายหลัง
- **แสดงอันดับ (#1-100) ยังเป็นแรงจูงใจให้พยายาม gaming engagement ทางอ้อมอยู่ดี** (เช่น ขอเพื่อนช่วยกด Like/Comment เยอะๆ) — เป็นข้อจำกัดโดยธรรมชาติของ leaderboard feature ทุกแบบทุกแพลตฟอร์ม ไม่ใช่ช่องโหว่เฉพาะของ WYN แก้ไม่ได้ด้วยการซ่อนอันดับ (นั่นจะทำลายจุดประสงค์ของ feature ไปเลย) — ยอมรับ trade-off นี้เหมือนที่ทุกแพลตฟอร์มยอมรับ ป้องกันได้แค่ระดับ "sanctioned account ไม่ติดอันดับ" ตามที่ Requirement 2 ทำไว้แล้ว
- **Top 100 Creator เลื่อนออกจากสโคปนี้** (ดู Requirement 1) — ถ้า Founder ต้องการให้ทำคู่กันเลย ต้องแจ้งก่อน Design เริ่มงาน เพราะกระทบขนาด/ทิศทางของ task นี้ทั้งหมด
- **การแจ้งเตือนผู้ใช้เมื่อเนื้อหาติด Top 100 ไม่อยู่ในสโคปนี้** — Master Spec section "Notification types" ระบุ "Discovery: Trending, Top 100" เป็น notification category ที่วางแผนไว้แล้วใน **WYN-043** (Phase 5, roadmap: "Notification type ใหม่ ReDrop/Quote/FollowRequest/MessageRequest/Trending/Top100/System") — task นี้แค่สร้างหน้า/ranking เท่านั้น ไม่เพิ่ม notification ใดๆ

Recommendation: เริ่มจาก Requirement 2 (reuse `engagementScore()`/`authors_posting_blocked()` ตรงๆ — ไม่มีความเสี่ยงใหม่เลย) และ Requirement 3 (candidate-fetch method ใหม่ มิเรอร์ `fetchTrending()` แต่ window/limit ต่างกัน) ก่อน แล้วค่อยทำ UI หน้า Top 100 (Requirement 1/4) ปิดท้ายด้วย Entry Point (Requirement 5) — เสนอ **WYN-04X ใหม่แยกต่างหาก** สำหรับ "Top 100 Creator" (aggregate engagement ต่อ creator ข้ามทุกโพสต์ หารด้วยจำนวนโพสต์เพื่อความเป็นธรรม) เป็นตัวเลือกให้ Founder พิจารณาทำหลัง Phase 4 ถ้าต้องการให้ Master Spec's "Content/Creator" ครบทั้งสองแบบ — Phase 4 จะปิดสมบูรณ์เมื่อ task นี้ผ่าน QA (WYN-040/041/042 ครบ)

Handoff: AI Design — ออกแบบหน้า Top 100 ใหม่ (layout รายการแสดงอันดับ #1-100, Drop+Pop mixed grid หรือ list ก็ได้ตามที่เห็นสมควร), ตัดสินใจ entry point สุดท้าย (แนะนำปุ่มท้าย Trending Now section ตาม Requirement 5), ตัดสินใจ candidate-fetch method ใหม่ (ชื่อ/ตำแหน่งใน repository ใด ให้ Coding), และ time window/candidate limit ตัวเลขจริง (Product เสนอ 7 วัน/500 candidate เป็นจุดเริ่มต้น)

## Coding Output (2026-08-23)

**ไม่มี SQL ใหม่** — task นี้ reuse `authors_posting_blocked()`/`engagementScore()` ของ WYN-041 ตรงๆ 100% ตามที่ Product/Design ระบุไว้ ไม่ต้องแตะ `schema.sql` เลยแม้แต่บรรทัดเดียว

**Flutter**:
- `TrendingTile` (`app/lib/features/home/presentation/widgets/trending_tile.dart`, WYN-017) เพิ่ม optional param `showRainbowRing` (`bool`, default `true`) ตามสเปก Design เป๊ะ — เมื่อ `false` ตัด `Container`/gradient ring ออก เหลือแค่ `tile` (ภาพ/thumbnail/poll-placeholder/play-icon/like-scrim เดิมทั้งหมด) ขนาด 90x90 ตรงๆ — caller เดิม 2 จุด (`home_feed_screen.dart`, `discovery_view.dart`'s Trending Now) ไม่ต้องแก้ไขเลยเพราะ default คงพฤติกรรมเดิม (ยืนยันด้วย `flutter test` เต็มชุดผ่านหมด รวม `home_feed_screen_test.dart`'s Trending row test เดิม)
- `HomeRepository` (`home_repository.dart`) เพิ่ม `fetchTopContent({int limit = topContentResultLimit})` ใหม่ทั้งหมด มิเรอร์โครงสร้างของ `fetchTrending()` เป๊ะ (candidate-fetch จาก `home_feed` → เก็บ authorIds → ดึง liked/saved/redropped/poll-state ผ่าน private helper เดิม → `_fetchPostingBlockedAuthorIds()` (WYN-041, reuse ตรงๆ) → map เป็น `HomeFeedItem` → `removeWhere` ตัดบัญชีที่ถูกกันออก → sort ด้วย `engagementScore()` (WYN-041, reuse ตรงๆ) → `.take(limit)`) — เพิ่มค่าคงที่ใหม่แยกต่างหาก `_topContentWindow = Duration(days: 7)`/`_topContentCandidateLimit = 500`/`topContentResultLimit = 100` — **ไม่แตะ `fetchTrending()`/`_trendingWindow`/`trendingCandidateLimit` เดิมแม้แต่บรรทัดเดียว** ตามที่ Product/Design ย้ำไว้ (ตัดสินใจ copy โครงสร้างแยกแทนการรวมเป็น shared helper ตามที่ Design ให้ตัดสินใจเอง — เลือกความเสี่ยงต่ำสุดต่อ `fetchTrending()` ที่ผ่าน QA แล้ว มากกว่าความสวยงามของ DRY)
- `DiscoveryRepository` เพิ่ม `fetchTopContent({limit})` wrapper ใหม่ (มิเรอร์ `fetchTrendingNow()` เป๊ะ — thin wrapper รอบ `_homeRepository.fetchTopContent()`) ให้ `DiscoveryView`/`Top100Screen` เรียกผ่านจุดเดียวกันได้โดยไม่ต้องเพิ่ม `homeRepository` field ใหม่ให้ `DiscoveryView`
- `Top100Screen` ใหม่ (`app/lib/features/search/presentation/top_100_screen.dart`) — `Scaffold` + `ListView.builder` แนวตั้ง (ไม่ใช่ grid ตามสเปก Design) แต่ละแถว (`_Top100Row`) = เลขอันดับ (`#$rank`, `titleLarge` bold, ไม่มีสีพิเศษ Top 3) + `TrendingTile(..., showRainbowRing: false)` + ชื่อผู้เขียน — ทั้งแถวห่อด้วย `InkWell` เดียว (ตัดสินใจเพิ่มจาก draft แรกที่ให้ tap แค่ตรง `TrendingTile`: พบระหว่างเขียนว่าการห่อ `Semantics(excludeSemantics: true)` รอบทั้งแถวโดยไม่มี `InkWell` ครอบทั้งแถวจะทำให้ screen-reader activation gesture ที่ synthesize tap ที่ตำแหน่งกึ่งกลาง node อาจไม่ตรงกับ hit-area เล็กๆ ของ `TrendingTile` เพียงอย่างเดียว — แก้โดยห่อทั้งแถวด้วย `InkWell.onTap` เดียวกัน ให้ tap ที่ไหนในแถวก็ได้ผลเหมือนกัน มิเรอร์ pattern ของ `follow_list_screen.dart`/Suggested Users row ที่มีอยู่แล้ว) — Loading/Empty/Error+Retry มิเรอร์ pattern ของ `RecentlyDeletedDropsScreen` เป๊ะ
- แก้ `DiscoveryView`: ห่อ Trending Now's `GridView` ด้วย `Column` ใหม่ เพิ่ม `TextButton('ดู Top 100')` ท้าย grid (แสดงเฉพาะตอนมีข้อมูลแล้ว ไม่แสดงตอน loading/error/empty ตามสเปก) เปิด `Top100Screen` ผ่าน `_openTop100()` ใหม่

**Flutter test ใหม่**:
- `app/test/top_100_screen_test.dart` ใหม่ทั้งไฟล์ (8 เคส): loading→ranked list เรียงลำดับถูกต้อง (ยืนยันตำแหน่งแนวตั้งจริงด้วย `getTopLeft`, ไม่ใช่แค่ text มีอยู่), empty state, error+retry (มี error field ใหม่ `topContentError` ใน `RecordingDiscoveryRepository` เพื่อทดสอบ error state จริง ไม่ใช่ vacuous test), tap Drop row → `DropDetailScreen`, tap Pop row → `PopSingleClipScreen`, ยืนยัน `TrendingTile.showRainbowRing == false` ตรงๆ ผ่าน `tester.widget<TrendingTile>()`
- `app/test/discovery_view_test.dart` เพิ่ม 2 เคส: ลิงก์ "ดู Top 100" ไม่ปรากฏตอน Trending Now ว่าง, tap ลิงก์ → เปิด `Top100Screen`
- `app/test/support/recording_discovery_repository.dart` เพิ่ม `topContentItems`/`topContentError` fields + override `fetchTopContent()`
- **บั๊กที่พบและแก้เองระหว่างเขียนเทสต์ (ไม่ใช่บั๊กจริงในโค้ด production แต่เป็น testing gotcha)**: ทดสอบ tap แล้ว navigate ครั้งแรก fail เพราะไม่ได้เรียก `tester.takeException()` รอบสองหลัง `tap()`+`pumpAndSettle()` (ปลายทาง `DropDetailScreen`/`PopSingleClipScreen` โหลดภาพตัวเองอีกรอบ เกิด `NetworkImageLoadException` ใหม่ที่ไม่ถูก consume) — แก้โดยเพิ่ม `takeException()` รอบสองตามที่ `home_feed_screen_test.dart`'s "tapping a Drop trending tile" test เคยทำไว้แล้ว (pattern ที่มีอยู่แล้ว ไม่ใช่การค้นพบใหม่) — และ `authorNameOrUsername` คืนค่า `@username` (ไม่ใช่ username เปล่า) เมื่อไม่มี `authorDisplayName` — เทสต์แรกพลาดจุดนี้ไป แก้ expectation ให้ตรง

**Build/Tests — รันจริงครบทุกจุด**:
- `flutter analyze`: **0 issues**
- `flutter test`: **659/659 pass** (baseline 651 จาก WYN-041 QA round + เคสใหม่ 8: 6 ใน `top_100_screen_test.dart` + 2 ใน `discovery_view_test.dart`)
- `dart format --set-exit-if-changed`: ผ่านทุกไฟล์ที่แก้ (3 ไฟล์ต้อง format ใหม่รอบแรก รันซ้ำแล้วผ่านสะอาด 0 changed)
- SQL: ไม่มีการเปลี่ยนแปลง ไม่ต้องรัน regression suite ใหม่ (ไม่แตะ `schema.sql`)

**Known Issue/Gap (ตั้งใจ ไม่ใช่บั๊ก แจ้ง QA/Product ให้ตัดสินใจ)**:
- `fetchTopContent()`/`fetchTrending()` มีโครงสร้าง body คล้ายกันมาก (near-duplicate) — ตัดสินใจไม่รวมเป็น shared helper ตามที่ Design ให้ทางเลือกไว้ เพื่อลดความเสี่ยง regression ต่อ `fetchTrending()` ที่ผ่าน QA แล้ว ยอมรับ code duplication ระดับหนึ่งแทน
- ไม่มี integration test ตรงสำหรับ `HomeRepository.fetchTopContent()`'s ตัว query/RPC-calling logic เอง (เหมือนที่ WYN-041 เจอกับ `_fetchPostingBlockedAuthorIds`) เพราะโค้ดเบสไม่มี harness ทดสอบ method ที่เรียก Supabase จริงแบบ isolated — ยืนยันผ่าน `flutter analyze`/code review + test ที่มีอยู่แล้วของ `authors_posting_blocked()`/`engagementScore()` เอง (WYN-041) ที่ `fetchTopContent()` reuse ตรงๆ

**Acceptance Criteria — ไล่ตรวจครบทุกข้อจาก Product spec**: ครบทุกข้อ ยืนยันด้วย Flutter test จริง (ranking order/anti-manipulation exclusion ผ่านการ reuse ที่ทดสอบแล้วจาก WYN-041/ไม่โชว์ตัวเลขดิบ/entry point/navigation)

Handoff: AI QA & Security — เน้นตรวจ 3 จุด: **(ก) ไม่มี Rainbow ring ในหน้า Top 100 เด็ดขาด** ตาม DS-009 (ยืนยันด้วย test ที่มีอยู่แล้วแต่ควรตรวจอิสระซ้ำด้วยสายตา/โค้ด), **(ข) `fetchTopContent()` ไม่แตะ `fetchTrending()`/`_trendingWindow`/`trendingCandidateLimit` เดิมจริง** (ตรวจ diff ยืนยันไม่มี regression ต่อ WYN-017/040), **(ค) ไม่มีตัวเลข engagement ดิบ/คะแนนจริงแสดงในหน้า Top 100** (ตรวจโค้ด `_Top100Row` ว่าไม่มีจุดไหนอ่าน field คะแนนออกมาแสดง นอกจาก like count ที่ `TrendingTile` โชว์อยู่แล้วเป็นปกติทั่วทั้งแอป)

## Independent QA (2026-08-23)

```
Feature: WYN-042 WYN Top 100 — full-screen weekly (7-day) leaderboard ของ Drop+Pop รวมกัน, เข้าถึงจาก Discovery's Trending Now section
Environment: Local sandbox — Flutter stable SDK ชุดเดียวกับที่ใช้ทดสอบ WYN-040/041 ในเซสชันนี้ — sync branch `claude/wyn-40-continuation-ul5ngq` ที่ commit `3d80e89` ก่อนเริ่มทดสอบ (ไม่มี SQL เปลี่ยนแปลงใน task นี้ จึงไม่ต้องรัน PostgreSQL regression suite ซ้ำ — ยืนยันด้วย `git diff --stat` ว่า `supabase/schema.sql` ไม่อยู่ใน diff เลย)

Test Cases:
1. `flutter analyze` รันอิสระเอง — 0 issues
2. `flutter test` เต็มชุดรันอิสระเอง (ไม่ subset) — **659/659 pass**
3. อ่าน `git diff 13a5f35 3d80e89 -- app/lib/features/home/data/home_repository.dart` แบบ adversarial ยืนยันว่า `fetchTopContent()` เป็นการเพิ่มเมธอดใหม่ล้วนๆ ไม่มีบรรทัดไหนแก้ `fetchTrending()`/`_trendingWindow`/`trendingCandidateLimit` เดิมเลยแม้แต่ตัวอักษรเดียว (diff แสดงเฉพาะ insertion หลังจบเมธอดเดิม)
4. อ่าน `git diff` ของ `trending_tile.dart` ยืนยันว่า path `showRainbowRing: true` (default) render widget tree เดิมทุกประการ ไบต์ต่อไบต์เทียบเท่าโค้ดก่อนแก้ (แค่ extract เป็นตัวแปร `tile` แล้วห่อด้วย ternary) — ไม่มีการเปลี่ยนพฤติกรรมของ caller เดิม 2 จุด (Home's Trending row, Discovery's Trending Now)
5. ยืนยัน `showRainbowRing: false` path ตัด `Container`/`WynColors.rainbowAccent` gradient ออกจริงทั้งหมด ไม่เหลือ trace ใดๆ ของ rainbow ใน widget tree — ตรงกับ test ที่มีอยู่ (`tile.showRainbowRing == false`) และยืนยันด้วยการอ่านโค้ดเองอิสระ ไม่ใช่แค่เชื่อ test
6. ตรวจ `_Top100Row`/`Top100Screen` ทั้งไฟล์ยืนยันไม่มีจุดไหนอ่าน/แสดง `engagementScore`/raw view-count-for-ranking หรือค่าคะแนนใดๆ — มีแค่ `#$rank` (ลำดับ) และ `item.likeCount` (ผ่าน `TrendingTile` เดิม ซึ่งเป็นตัวเลขสาธารณะที่โชว์อยู่แล้วทุกที่ในแอป ไม่ใช่สัญญาณจัดอันดับภายใน)
7. ตรวจ entry point logic ใน `discovery_view.dart` ยืนยันปุ่ม "ดู Top 100" อยู่ภายใน branch เดียวกับที่ต้องผ่านทั้ง `snapshot.hasData` และ `items.isEmpty == false` ก่อน — ไม่โผล่ระหว่าง loading/error/empty จริง (ยืนยันด้วย test "ลิงก์ไม่ปรากฏตอน Trending Now ว่าง" ที่ผ่าน + อ่านโค้ดตรงจุด)
8. ตรวจ accessibility fix ของ `_Top100Row` (ห่อทั้งแถวด้วย `InkWell` เดียวแทนที่จะพึ่ง `TrendingTile`'s เล็กๆ ตัวเดียว) — เหตุผลที่ Coding ให้ไว้ถูกต้องจริง (screen-reader activation gesture synthesize tap ที่ตำแหน่งกึ่งกลาง merged-semantics-node ซึ่งเป็นพื้นที่ทั้งแถว ไม่ใช่แค่ tile) — ยืนยันด้วยการอ่านโครงสร้าง widget tree ตรงว่า `InkWell.onTap` ครอบทั้ง `Row` จริง ไม่ใช่แค่ส่วนหนึ่ง
9. รัน `top_100_screen_test.dart`/`discovery_view_test.dart` แยกเจาะจงอีกรอบ ยืนยัน error+retry test ไม่ vacuous จริง (เช็คว่า error message หาย/ข้อมูลกลับมาแสดงจริงหลัง retry ไม่ใช่แค่ไม่ throw)
10. ตรวจ candidate-window/exclusion logic ของ `fetchTopContent()` เทียบกับ `fetchTrending()` บรรทัดต่อบรรทัด ยืนยัน reuse `_fetchPostingBlockedAuthorIds()`/`engagementScore()` ถูกจุดจริง (WYN-041's anti-manipulation guarantee สืบทอดมาถูกต้อง ไม่ได้ copy ผิดหรือข้ามขั้นตอนไหน)

Passed: 0 issues (`flutter analyze`) + 659/659 (`flutter test` เต็มชุด) + 10/10 adversarial code-review checks — ทุกตัวเลขยืนยันด้วยการรันเอง/อ่านโค้ดเองอิสระ ไม่ใช่การเชื่อ Coding Output
Failed: 0

Severity: N/A (ไม่พบบั๊กที่ block การอนุมัติ)

Reproduction Steps: N/A — ไม่มีบั๊กให้ reproduce

Expected: ครบ 3 จุดตาม Handoff ของ Coding — (ก) ไม่มี Rainbow ring, (ข) ไม่แตะ fetchTrending()/ค่าคงที่เดิม, (ค) ไม่โชว์ตัวเลขดิบ

Actual: ตรงตามที่คาดทั้ง 3 จุด — (ก) ยืนยันด้วยการอ่าน widget tree ตรงว่า `showRainbowRing: false` ตัด gradient container ออกจริง 100% + test ยืนยันซ้ำ, (ข) ยืนยันด้วย diff ตรงว่า `fetchTrending()` ไม่มีบรรทัดไหนถูกแก้เลย เป็นแค่การเพิ่มเมธอดใหม่ต่อท้าย, (ค) ยืนยันด้วยการไล่อ่านทุกบรรทัดของ `_Top100Row` ว่าไม่มี field คะแนน/สัญญาณจัดอันดับใดๆ หลุดออกมาแสดง

Security Findings:
- ไม่มีการเปลี่ยนแปลง SQL/RLS/RPC ใดๆ ใน task นี้เลย — reuse `authors_posting_blocked()`/`engagementScore()` ของ WYN-041 ที่ผ่าน QA แล้วตรงๆ 100% ไม่มี attack surface ใหม่เกิดขึ้น
- ไม่พบ privacy leak ใหม่ — ข้อมูลที่แสดงทั้งหมด (username, thumbnail, like count, อันดับ) เป็นข้อมูลที่มองเห็นได้อยู่แล้วผ่านช่องทางอื่นในแอป (Home/Search/Drop/Pop detail) ไม่มีอะไรใหม่ที่ต้องพิจารณาสิทธิ์การเข้าถึงเพิ่ม
- Mobile/responsive: แถวมี fixed-width ~150px (เลขอันดับ 40px + spacing + tile 90px) เหลือพื้นที่ให้ `Expanded` text บนความกว้างจอทั่วไป (360dp ขึ้นไป) เพียงพอ ตรวจด้วยการคำนวณ ไม่มี fixed test ที่ narrow viewport เจาะจงแต่ความเสี่ยง overflow ต่ำเพราะใช้ `Expanded`+`TextOverflow.ellipsis` ครบ

Recommendation: อนุมัติ — ครบทุก Acceptance Criteria จาก Product spec, ยืนยันด้วยการทดสอบอิสระทั้งหมด ไม่มี regression ต่อ WYN-017/040/041 เดิมแม้แต่จุดเดียว (ไม่แตะ SQL เลย, ไม่แตะ `fetchTrending()`/`TrendingTile`'s default behavior เลย), DS-009 compliance ถูกต้องตามที่ Design ตัดสินใจไว้ (ไม่มี rainbow point ที่ 3 เกิดขึ้นจริง), ไม่พบ security/privacy finding ใหม่ ไม่มีข้อสังเกต Minor ใดๆ ที่ต้องบันทึกเพิ่มด้วยซ้ำในรอบนี้

Final Status: PASS
```

