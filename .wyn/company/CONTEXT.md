# WYN Context / Knowledge Base

> เอกสารนี้คือ source of truth เกี่ยวกับ WYN โปรดอัปเดตทุกครั้งที่มีข้อมูลใหม่ที่ยืนยันแล้วจาก Founder หรือจาก repository ห้ามเดาหรือสมมติข้อมูลที่ไม่ทราบแน่ชัด — ให้ระบุ `UNKNOWN`

- **WYN Vision**: WYN คือแพลตฟอร์มโซเชียลมีเดียที่ออกแบบมาเพื่อคนรุ่นใหม่ (Gen Z) ให้สามารถเชื่อมต่อ แสดงตัวตน และสร้างชุมชนของตัวเองได้อย่างเป็นธรรมชาติ รวดเร็ว และปลอดภัย (ยืนยันโดย Founder เมื่อ 2026-08-13)
- **WYN Mission**: สร้างพื้นที่โซเชียลที่เรียบง่าย ตอบสนองไว และให้ความสำคัญกับความปลอดภัยและความเป็นส่วนตัวของผู้ใช้ Gen Z มากกว่าแพลตฟอร์มเดิมในตลาด (ยืนยันโดย Founder เมื่อ 2026-08-13)
- **Target Users**: วัยรุ่น / Gen Z (ยืนยันโดย Founder เมื่อ 2026-08-13 ผ่าน `/product`)
- **Core Product**: โซเชียลมีเดียทั่วไป (general social media platform) สำหรับกลุ่ม Gen Z (ยืนยันโดย Founder เมื่อ 2026-08-13)
- **Current Version**: UNKNOWN (repository มีเพียงเอกสาร WYN AI Company และ product foundation ยังไม่มี source code หรือ version marker)
- **Current Features**: WYN-002 (Authentication & Onboarding), WYN-003 (User Profile — view/edit display name, bio, avatar), WYN-004 (Feed & Post — global feed, ข้อความ+รูป, Like, Comment, ลบโพสต์ตัวเอง — โค้ด Dart ถูกลบไปแล้วตอน WYN-007, ตาราง DB ยังอยู่รออนุมัติ Founder), WYN-005 (Drop — โพสต์รูปภาพ 1:1, Like/Comment/Like Comment/ลบ Comment ของตัวเอง/Share/Save/ลบ Drop ของตัวเอง/Follow ผู้เขียน, Bottom Nav 4 แท็บ), WYN-006 (Pop — คลิปสั้นแนวตั้ง, vertical swipe feed, Like/Comment ครบ 3 ความสามารถ/Share/Save/View count/ลบ Pop ของตัวเอง/Follow ผู้เขียนทำงานจริง), WYN-007 (Home — Search bar placeholder + Feed รวม Drop/Pop เรียงตามเวลา, การ์ดแยกประเภทมองด้วยตาออกทันที, แตะการ์ดเปิดรายละเอียดตามประเภท, การ์ดมี Like/Comment/Share/Save ครบทั้งสองแบบ) และ WYN-008 (Follow system — Follow/Unfollow ผู้ใช้อื่นใช้ร่วมกันทั้ง Drop/Pop, self-follow กันสองชั้น, จำนวน+รายชื่อ Followers/Following ใน Profile) **ทั้งเจ็ดผ่าน QA (PASS ระดับโค้ด/static)** แล้ว (ดู `.wyn/tasks/approved/`) ยังห้าม deploy จริงจนกว่าจะมี Supabase project จริง, native platform config, และ `flutter build`/dynamic test ผ่านจริง — งานถัดไปคือ AI Deploy & DevOps เมื่อ infra พร้อม หรือ WYN-009 (Search) ตาม roadmap
- **Product Direction — เปลี่ยนแล้ว (2026-08-14)**: Founder แทนที่ทิศทางเดิมทั้งหมดด้วย spec ใหม่ "WYN V0.1 — CORE APP FEATURE PROMPT" — โครงสร้างใหม่คือ Bottom Nav 4 เมนู Home (Search+Feed รวม)/Drop (โพสต์รูปภาพ)/Pop (โพสต์คลิปสั้นแนวตั้ง)/Profile พร้อม Like/Comment/Share/Save/Follow/Notification เต็มรูปแบบ — ดูรายละเอียดเต็มที่ `.wyn/company/DECISIONS.md` (2026-08-14) WYN-002/WYN-003 ยังใช้เป็นฐานได้ (Auth/Profile ตรงกับ spec ใหม่) แต่ WYN-004 (Feed & Post แบบรวม) ถูกแทนที่ด้วย Drop+Pop แยกกัน (โค้ดเดิมของ WYN-004 ยังไม่ถูกลบ แต่ไม่มี route ชี้ไปแล้ว — รอ Founder/Product ตัดสินใจว่าจะลบหรือ repurpose ตอนทำ WYN-007) — AI Product Manager วาง roadmap เต็มไว้ที่ `.wyn/docs/product/wyn-v0.1-roadmap.md` (9 task ใหม่: WYN-005 ถึง WYN-013) **WYN-005 (Drop) — PASS** (QA รอบ 3, 2026-08-14): QA รอบ 1 FAIL (Major: ไม่มี Like Comment) → Debug แก้ → QA รอบ 2 FAIL (Major: ไม่มีทางลบ Comment ของตัวเองได้) → Debug แก้ → **QA รอบ 3 PASS** (Requirements/Design Components/Acceptance Criteria ไล่ตรวจแยกกันครบทั้ง 3 หัวข้อ พบแค่ข้อสังเกต Minor ไม่ block: ไม่แสดงเวลาที่โพสต์แบบ relative — บันทึกไว้เป็นรายการปรับปรุงรอบถัดไป) ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง **WYN-006 (Pop — คลิปสั้นแนวตั้ง) — PASS** (QA รอบ 1, 2026-08-14): Product+Design spec (ตัดสินใจ layout/เสียงอย่างมีเหตุผลเพื่อไม่ให้ลอก TikTok) → Coding ส่งมอบครบ (schema mirror WYN-005 รวม Like/Delete Comment ตั้งแต่ต้น, `PopFeedScreen` vertical `PageView` พร้อม video controller lifecycle, `CreatePopScreen` จำกัด 60 วิ — พบและแก้บั๊ก double-tap Like เองก่อนส่ง QA) → **QA รอบ 1 PASS ทันที** (ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบ 3 หัวข้อ, ตรวจ RLS/view-count RPC security/video lifecycle/Follow UI-only ทั้งหมดผ่าน, ทำ red→green double-tap ซ้ำเองอิสระ — พบแค่ 2 ข้อสังเกต Minor ไม่ block: ปุ่ม Follow ขาด Semantics label, view-count RPC ไม่มี rate-limit) ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task แรกที่ผ่าน QA รอบเดียวตั้งแต่ WYN-004 **WYN-007 (Home — Search bar + Feed รวม Drop/Pop)**: task spec เต็มเขียนเสร็จแล้วที่ `.wyn/tasks/backlog/WYN-007-home-feed.md` — Product ตัดสินใจ Search bar เป็น placeholder รอบนี้ (ของจริงผูกกับ WYN-009) และตัดสินใจลบโค้ด WYN-004 เดิม (`app/lib/features/feed/`) ทิ้งระหว่าง implement (มอบหมาย Coding) แต่**ไม่ลบตาราง `posts`/`likes`/`comments` เอง** เพราะเป็น destructive DB structure change ที่ต้องขออนุมัติ Founder — บันทึก APPROVAL_REQUIRED ไว้ที่ `.wyn/company/APPROVALS.md` แล้ว (รออนุมัติ) Design spec เต็มที่ `.wyn/docs/design/wyn-007-home.md` เสร็จแล้ว (การ์ด Pop ใน Home ใช้ thumbnail 1:1 + play icon แทนเล่นวิดีโอเต็มอัตราส่วน, แตะการ์ด Pop เปิดหน้าคลิปเดี่ยวไม่กระโดดเข้ากลาง Pop Feed, Search bar placeholder สื่อสารชัดเจนว่า "เร็ว ๆ นี้", แนะนำ database-side UNION ALL view สำหรับรวม feed) **AI Coding ส่งมอบครบแล้ว** (สร้าง view `home_feed` รวม `drops`/`pops` จริง, ลบโค้ด WYN-004 (`app/lib/features/feed/`) ทิ้งทั้งหมดโดยไม่แตะตาราง DB, ระหว่างลบพบว่า `confirm_delete_dialog.dart` อยู่ในโฟลเดอร์เดียวกันแต่ Drop/Pop ใช้ร่วม จึงย้ายไป `app/lib/core/widgets/` ก่อน, แยก `PopClipView` ออกจาก `pop_feed_screen.dart` เพื่อ reuse กับหน้าคลิปเดี่ยวใหม่ของ Home, พบและแก้บั๊ก double-tap Like เองก่อนส่ง QA ด้วย regression test ที่พิสูจน์ fail จริงก่อนแก้/pass หลังแก้ `flutter analyze`/`flutter test`: 81/81 ผ่าน) **QA รอบ 1 — FAIL** (Major: การ์ด Home ทั้ง Drop และ Pop ไม่มีปุ่ม Share เลย และไอคอน Comment กดไม่ได้ ทั้งที่ Design spec ระบุไว้ตรงกันทั้งสองการ์ดว่าแถวปฏิสัมพันธ์ต้องมี Like/Comment/Share/Save ครบ — ยังกด Share/Comment ได้จากหน้ารายละเอียดที่เปิดจากการแตะการ์ด แต่ไม่ใช่ quick action ตรงจากการ์ดตามที่ Design ตั้งใจ; pagination/security ของ view `home_feed` ตรวจแล้วถูกต้อง, ไม่มีการแตะตาราง `posts`/`likes`/`comments`, regression กับ Drop/Pop เดิมผ่านหมด) **Debug รอบ 1 แก้แล้ว**: เพิ่มปุ่ม Share (`dropShareLink`/`popShareLink` เดิม, ทำ `_dropShareLink` เป็น public) และทำไอคอน Comment เป็น `IconButton` เรียก `onTap` เดียวกับการ์ด ใน `home_drop_card.dart`/`home_pop_card.dart` ทั้งคู่ พิสูจน์ red→green จริง (revert แล้วเทสต์ fail 3 เคส, restore แล้ว pass ทั้ง 83 เคส) `flutter analyze`/`flutter test`: สะอาด 83/83 **QA รอบ 2 — PASS** (ไล่ตรวจ Requirements/Design Components/Acceptance Criteria ครบทั้ง 3 หัวข้อใหม่ทั้งหมดอีกครั้ง ยืนยันบั๊กรอบ 1 แก้จริงและไม่มี regression, ทำ red→green ซ้ำเองอิสระอีกครั้งยืนยันตรงกับที่ Debug รายงาน — พบข้อสังเกต Minor เดียวไม่ block: กด Comment บนการ์ด Pop เปิดหน้าคลิปเดี่ยวก่อนแทนที่จะเปิด comment sheet ตรง ๆ ทันที ซึ่ง QA รอบ 1 อนุญาตไว้แล้วว่าไม่ต้อง scroll/เปิดตรงในรอบนี้ก็ได้ — เสนอ fast-follow `openCommentsOnStart` flag ไว้พิจารณาภายหลัง) **อนุมัติแล้ว** — เป็น task ที่ 7 ของโปรเจกต์ที่ผ่าน QA แล้ว **WYN-008 (Follow system) — PASS** (QA รอบ 1, 2026-08-14): Follow/Unfollow ใช้ระบบเดียวกันทั้ง Drop/Pop (ตาราง `follows` ใหม่ + `FollowRepository` เดียวใช้ร่วมกัน), self-follow กันสองชั้น (UI ซ่อนปุ่ม + DB `CHECK` constraint), ปุ่ม Follow ใหม่ใน `DropDetailScreen` + เชื่อมปุ่มเดิมของ `PopClipView` (UI-only จาก WYN-006) ให้ทำงานจริงพร้อมเพิ่ม Semantics label ที่เคยขาด, หน้า `FollowListScreen` ใหม่ (แถวไม่มี tap affordance ตามที่ Design ตั้งใจ — ยังไม่มีหน้าโปรไฟล์คนอื่นให้ไปจนกว่าจะถึง WYN-013), จำนวน Followers/Following ใน `ViewProfileScreen` เป็น tap target จริง — Coding พบและแก้ gap เอง 2 จุดก่อนส่ง QA (`IndexedStack` reload-on-visit fix, testing gotcha ของ `pumpWidget` ไม่รีเซ็ต State) **QA รอบ 1 PASS ทันที** (ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบ 3 หัวข้อ ทำ red→green จริงด้วยตัวเองยืนยัน double-tap safety ของปุ่ม Follow ทั้ง Drop/Pop, ตรวจ self-follow guard สองชั้น/FK naming convention/`IndexedStack` fix ทั้งหมดผ่าน — พบแค่ Minor เดียวไม่ block: comment ใน schema.sql อ้างอิง path `approved/` ล่วงหน้า) `flutter analyze`/`flutter test`: สะอาด 95/95 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่สองต่อจาก WYN-006 ที่ผ่าน QA รอบเดียว **WYN-013 (Profile V2) — Coding ส่งมอบแล้ว รอ QA**: Product ตัดสินใจรวม scope ที่เหลือของ WYN-011 (หน้าดู Saved) เข้ามาใน WYN-013 เลยแทนที่จะแยก task (ปุ่ม Save toggle มีอยู่แล้วตั้งแต่ WYN-005/006/007) และตัดสินใจ reuse `ViewProfileScreen` เดิมแบบ parameterize สองบุคลิกในไฟล์เดียว (เจ้าของโปรไฟล์เห็นปุ่มแก้ไข/logout, คนอื่นเห็นปุ่ม Follow) แทนที่จะแยกสองหน้า — Design ตัดสินใจ TabBar 3 แท็บ (Drop grid/Pop list/Saved) แบบ conditional length ซ่อนแท็บ Saved ตามธรรมชาติเมื่อดูโปรไฟล์คนอื่น, Pop list ใช้ thumbnail grid 3 คอลัมน์ (ไม่ใช่ full-screen swipe แบบ Pop Feed เดิม) มิเรอร์ media area ของ `HomePopCard`, Saved tab ผสม Drop+Pop ปนกันเรียงตามเวลาที่บันทึก (ไม่แยก sub-tab) — Coding สร้าง view `saved_feed` ใหม่ (มิเรอร์แนวทาง `home_feed` ของ WYN-007, `security_invoker = true` ให้ RLS ของ `saves` ยังบังคับใช้จริง), `SavedRepository` ใหม่ (ไม่มี `userId` param เพราะ RLS จำกัดให้เห็นเฉพาะของตัวเองอยู่แล้ว), `fetchByAuthor` ใหม่ในทั้ง `DropRepository`/`PopRepository`, widget ใหม่ 5 ตัว (`PopGridTile`, `SavedGridTile`, `ProfileDropGridTab`, `ProfilePopGridTab`, `ProfileSavedTab`), เพิ่มจุด tap-to-profile ใหม่ 4 จุด (`DropDetailScreen`, `PopClipView`, `HomeDropCard`, `HomePopCard` — ห่อเฉพาะ avatar+ชื่อด้วย `InkWell` ซ้อนในการ์ด ไม่ชนกับ tap เดิม) และทำให้แถวใน `FollowListScreen` (WYN-008 ตั้งใจไม่ให้กดได้เพราะตอนนั้นยังไม่มีปลายทาง) กดได้จริงแล้ว — Coding พบและแก้ gap เอง 3 จุดก่อนส่ง QA: `IndexedStack` reload fix จาก WYN-008 ยังทำงานถูกต้อง, testing gotcha `pumpWidget` ไม่รีเซ็ต State เจอซ้ำอีกครั้ง, testing gotcha ใหม่ `find.bySemanticsLabel()` ต้องมี active semantics tree ถึงจะเจอ (แก้โดย assert widget type แทน) — ทำ red→green regression proof กับจุดที่ critical ที่สุด (`_isOwnProfile` persona guard เพราะถ้าพังจะทำให้ Saved tab ส่วนตัวรั่วไปหน้าคนอื่น) **QA รอบ 1 PASS** (ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบ 3 หัวข้อ ทั้ง 12 ข้อ AC, sync branch จาก main ใหม่และรัน `flutter analyze`/`flutter test` อิสระเองแทนที่จะเชื่อตัวเลขจาก Coding Output, ทำ red→green proof ของ `_isOwnProfile` persona guard เองอิสระอีกครั้งยืนยันตรงกับที่ Coding รายงาน, ตรวจ `saved_feed` security_invoker + RLS ของ `saves` แล้วยืนยันไม่มีทาง bypass, ตรวจ `SavedRepository` ไม่มีช่องให้ inject userId คนอื่น — พบ Minor เดียวไม่ block: จุด tap-to-profile บนการ์ด Home (จุดเสี่ยงชนกันมากที่สุดตาม Design spec) ไม่มี regression test ถาวรใน `home_feed_screen_test.dart` แม้ตัวฟีเจอร์จะทำงานถูกต้องจริง — QA พิสูจน์ด้วย widget test ชั่วคราวที่เขียนเองแล้วลบทิ้งว่าแตะ avatar/ชื่อเปิดโปรไฟล์จริงไม่ชนกับ tap เดิม เสนอเป็น fast-follow เพิ่ม test ถาวร) `flutter analyze`/`flutter test`: สะอาด 102/102 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่สามต่อจาก WYN-006/WYN-008 ที่ผ่าน QA รอบเดียว **WYN-009 (Search) — Coding ส่งมอบแล้ว รอ QA**: แทนที่ `SearchPlaceholderScreen` (WYN-007) ด้วยหน้าค้นหาจริง — คำค้นเดียวเหนือ `TabBar` 3 tab (User/Drop/Pop) ใช้ร่วมกันทั้ง 3 tab ไม่ใช่กล่องแยก, reuse `FollowListScreen` row layout สำหรับ User และ `DropGridTile`/`PopGridTile` สำหรับ Drop/Pop ตรง ๆ, debounce 400ms ด้วย `Timer`/`.cancel()` จริง, เพิ่ม `ProfileRepository.searchProfiles`/`DropRepository.searchByCaption`/`PopRepository.searchByCaption` ใหม่ (ไม่แก้ method เดิม), hashtag search defer เป็น caption ILIKE substring แทนระบบ hashtag entity เต็มรูปแบบ (ตัดสินใจโดย Product พร้อมเหตุผล) — ไม่สร้าง DB view ใหม่เพราะแต่ละ tab paginate อิสระไม่มีปัญหา cross-table pagination แบบ Home/Saved — Coding เขียน red→green proof สำหรับ debounce-cancel เอง พบว่า test แรกที่เขียนไว้ไม่จับบั๊กได้จริงเพราะ `tester.pump()` แบบ pump เดียวยาวจะ coalesce หลาย timer callback ให้เหลือ rebuild เดียว ต้องเขียนใหม่แยก pump เป็นช่วง ๆ ถึงจะพิสูจน์ได้จริง (บันทึกเป็น pattern ใหม่) **QA รอบ 1 PASS** (ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบ 3 หัวข้อ ทั้ง 10 ข้อ AC, sync branch จาก main ใหม่และรัน `flutter analyze`/`flutter test` อิสระเอง, ทำ red→green proof ของ debounce-cancel เองอิสระอีกครั้งยืนยันตรงกับที่ Coding รายงาน — พบ Minor เดียวไม่ block: `ProfileRepository.searchProfiles`'s `.or()` filter interpolate คำค้นดิบเข้า PostgREST filter DSL string โดยไม่ escape อักขระพิเศษ (comma/dot/paren) แต่ประเมินแล้วไม่ใช่ช่องโหว่ privacy เพราะตาราง `profiles` เป็น select-all-authenticated อยู่แล้วและคอลัมน์ที่คืนกลับตายตัวไม่ได้รับอิทธิพลจาก injection เสนอ sanitize เป็น fast-follow) `flutter analyze`/`flutter test`: สะอาด 110/110 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่สี่ต่อจาก WYN-006/WYN-008/WYN-013 ที่ผ่าน QA รอบเดียว **WYN-012 (Notification) — Coding ส่งมอบแล้ว รอ QA**: ตาราง `notifications` ใหม่ populated ด้วย DB trigger 5 ตัว (ไม่ใช่ derived view เพราะต้องมี mutable read-state ต่อแถว) ครอบคลุม Like Drop/Pop, Comment Drop/Pop, Follow — self-notification guard ที่ trigger level เทียบ actor กับเจ้าของเนื้อหาก่อน insert, ไม่มี insert policy ให้ client เลย (insert เกิดจาก security-definer trigger เท่านั้น), Mention defer เหมือน hashtag ของ WYN-009, ไอคอนกระดิ่ง+badge (cap "9+") ข้าง search bar ใน Home ไม่ใช่ Bottom Nav tab ใหม่, `NotificationListScreen` reuse โครงสร้างแถวของ `FollowListScreen`, relative time (`relativeTimeLabel`) เริ่มใช้ครั้งแรกในโปรเจกต์ที่หน้านี้, unread highlight ใช้ snapshot ตอน fetch ไม่ re-derive จาก DB state ใหม่ระหว่างอยู่ในหน้า — Coding เจอ testing gap เอง (test แรกของ unread-highlight เป็น vacuous เพราะ test double ไม่ mutate ข้อมูลเดิม) เขียนใหม่ให้มี notification ผสม unread/read แล้วพิสูจน์ red→green ได้จริง (บันทึก pattern ใหม่) **QA รอบ 1 PASS** (ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบ 3 หัวข้อ ทั้ง 10 ข้อ AC, sync branch จาก main ใหม่และรัน `flutter analyze`/`flutter test` อิสระเอง, อ่าน SQL trigger logic ทั้ง 5 ตัวยืนยัน self-notification guard ถูกทิศจริง (เทียบ actor กับเจ้าของเนื้อหาถูกต้อง) และยืนยันด้วยเหตุผลว่า `notify_follow` ไม่ต้อง guard เพิ่มเพราะ CHECK constraint ของ WYN-008 ตรวจก่อน AFTER trigger เสมอ, ยืนยัน RLS ไม่มี insert/delete policy ให้ client เลย, ทำ red→green proof ของ unread-highlight mapping เองอิสระอีกครั้งทดสอบทิศตรงข้ามจากที่ Coding ทดสอบไว้ (ยืนยันจับบั๊กได้ทั้งสองทิศ) — พบ Minor เดียวไม่ block: ไม่มี regression test ถาวรครอบคลุมวงจร badge-refresh-after-return เต็มรูปแบบ ยืนยันด้วย widget test ชั่วคราวที่ QA เขียนเองแล้วลบทิ้งว่าทำงานถูกต้องจริง) `flutter analyze`/`flutter test`: สะอาด 124/124 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่ห้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009 ที่ผ่าน QA รอบเดียว
- **Pop (WYN-006) ระงับการพัฒนาเพิ่มเติมไว้ก่อน (2026-08-14)**: Founder สั่งระงับ (ไม่ใช่ยกเลิก) งานพัฒนาต่อยอด Pop เพื่อหันไปทำ WYN CLUB — Pop ที่มีอยู่แล้วในแอปยังทำงานปกติทุกจุด (โค้ด/route/DB ไม่ถูกแตะต้อง) ดูรายละเอียดที่ `.wyn/company/DECISIONS.md`
- **WYN CLUB (Community/กลุ่มความสนใจ) — ฟีเจอร์ใหม่ที่ Founder เพิ่มเข้า roadmap (2026-08-14)**: เอกสารต้นฉบับเต็มที่ `.wyn/docs/product/wyn-club-founder-brief.md` — ทำเฉพาะ Core Club System รอบแรกตามที่ Founder ระบุ ไม่สร้าง Bottom Nav tab ใหม่ (อยู่ภายใน Home) ดูรายละเอียดที่ `.wyn/company/DECISIONS.md` **WYN-014 (Club Core) — PASS (QA รอบ 1, 2026-08-14)**: Product แบ่ง scope ของ spec เต็ม 19 หัวข้อออกเป็น WYN-014 (Core: สร้าง/เข้าร่วม Club, Club Page, โพสต์, Role 4 ระดับ, Admin system, Pinned Post, กฎ Club) กับ WYN-015 (Discovery/Search/Notification/Profile/Home integration — วางแผนไว้ ยังไม่เริ่ม) — Design ออกแบบ 7 หน้าจอ (CLUB section ใน Home, CreateClubScreen, ClubPage header 3 สถานะปุ่ม Join, TabBar Posts/Members/About, การ์ดโพสต์รองรับ Text/รูปเดี่ยว/หลายรูป carousel/Link, Members tab role badge+เมนูจัดการ, About tab กฎ Club) — Coding สร้างระบบ role-based permission ระบบแรกของโปรเจกต์: ตาราง `clubs`/`club_members`/`club_posts`/`club_post_likes`/`club_post_comments` ใหม่, ฟังก์ชัน `club_role()` เป็น single reusable authorization primitive, trigger `clubs_add_owner_membership` สร้างสมาชิกภาพ Owner อัตโนมัติ, trigger `clubs_prevent_owner_id_change` กัน Admin ชิงความเป็นเจ้าของผ่าน update ปกติ, มอบหมายทุก role/status mutation (approve/reject/set role/remove/ban) ให้ RPC function 5 ตัวแทน raw RLS policy (มิเรอร์ pattern `increment_pop_view_count` ของ WYN-006) เพราะ permission graph ซับซ้อนเกินกว่าจะเขียนเป็น RLS policy ตรงๆ ได้อย่างปลอดภัย, `club_members` ไม่มี raw update policy เลย, storage bucket `club-media` เป็น **non-public bucket แรกของโปรเจกต์** (ต่างจาก avatars/drop-images/post-images/pop-videos ที่ public หมด) เพราะโพสต์ Club ต้องเห็นเฉพาะสมาชิกที่ approved เท่านั้นแม้ Club จะเป็น Public — ฝั่ง Dart ใช้ signed URL สดใหม่ทุกครั้งที่อ่านแทนการ cache public URL ถาวร, ขยาย `saves.content_type` ให้รองรับ `'club_post'` reuse ตารางเดิมของ WYN-005 — Coding พบและแก้ gap เอง 2 จุดก่อนส่ง QA: layout overflow ของ Cover image ใน `ClubPage` (แก้จาก `AspectRatio(16:9)` เต็มความกว้างจอเป็น fixed-height 140px ตาม Design's "ความสูงจำกัด"), และแก้ path การอัปโหลดรูปโพสต์หลายรูปให้ไม่ชน `club_posts_have_content` CHECK constraint (อัปโหลดรูปก่อน insert แถวแทนที่จะ insert ก่อนแล้วค่อยอัปเดต) — เขียน regression test ครอบคลุมทุก AC รวม role-permission boundary matrix ครบทุกคู่ role×action (Owner/Admin/Moderator×set-role/remove/ban) และ post-visibility gating สำหรับคนไม่ join, ทำ red→green regression proof อิสระ 2 จุด (Admin-แต่งตั้ง-Admin privilege escalation, non-member เห็นเนื้อหาโพสต์) `flutter analyze`/`flutter test`: สะอาด 148/148 (เพิ่มจาก 124 เดิม) **QA รอบ 1 PASS**: sync `main` ใหม่หลัง merge PR #63 แล้วรัน `flutter analyze`/`flutter test` อิสระเองยืนยันตรงกับตัวเลขที่ Coding รายงาน, ไล่ Requirements/Design Components/Acceptance Criteria แยกกันครบทั้ง 3 หัวข้อทั้ง 16 ข้อ AC, อ่าน SQL ยืนยัน `club_role()` เป็น single source of truth ที่ทุก policy/RPC เรียกใช้ร่วมกัน, ยืนยัน RPC ทั้ง 5 ตัว re-derive role ของผู้เรียกเองไม่เชื่อค่าจาก client และมี NULL-safety ถูกต้อง (`coalesce()` ป้องกัน `null not in (...)` ที่จะปล่อยคนแปลกหน้าผ่าน), ยืนยัน `club_members` ไม่มี raw update policy เลย, ยืนยัน insert policy ผูก status กับ privacy ของ club จริงป้องกัน self-approve เข้า Private club, ยืนยัน `clubs_prevent_owner_id_change` trigger ปิดช่องที่ Admin จะชิงความเป็นเจ้าของผ่าน update ปกติ, ยืนยัน storage bucket `club-media` แยก visibility cover/icon กับรูปโพสต์ถูกต้อง — **ทำ red→green regression proof อิสระ 2 จุด** (privilege escalation ทำซ้ำ reproduction เดียวกับ Coding ยืนยันตรงกัน, post-visibility gating ทำทิศตรงข้ามจากที่ Coding ทดสอบ — พิสูจน์ gate จับบั๊กได้ทั้งสองทิศทาง) — พบ Minor เดียวไม่ block: ผู้ใช้ที่ถูกแบนพยายาม join ซ้ำเห็น error message ทั่วไปแทนข้อความเฉพาะ (ปลอดภัย ไม่ crash ไม่ใช่ security gap) `flutter test`: สะอาด 148/148 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่หกต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012 ที่ผ่าน QA รอบเดียว และเป็น task แรกที่มีระบบ role-based permission เต็มรูปแบบผ่าน QA **WYN-015 (Club Discovery & Integration) — PASS (QA รอบ 1, 2026-08-14)**: เชื่อม Club Core (WYN-014) เข้ากับระบบหลักของแอปครบทั้ง 5 จุดตามที่ Product เสนอไว้ใน Recommendation ของ WYN-014 เอง — Explore Clubs (หน้าใหม่แทนที่ placeholder ของ WYN-014 แบ่ง "กำลังนิยม"/"ใหม่ล่าสุด" พร้อม Category filter, ตัด "แนะนำสำหรับคุณ" ออกเพราะไม่มี behavioral signal รองรับ), Search integration (Club tab ที่ 4 ต่อจาก User/Drop/Pop เดิม ค้นหาจาก Name เท่านั้น), Notification integration (4 ประเภทใหม่: `club_join_request`/`club_join_approved`/`club_post_like`/`club_post_comment` — `club_join_request` เป็น **fan-out trigger ตัวแรกของโปรเจกต์** ที่ insert หลายแถวต่อเหตุการณ์เดียว ต่างจาก trigger เดิมทุกตัวที่ insert แถวเดียวเสมอ, `club_join_approved` ใช้ `auth.uid()` จับตัวผู้อนุมัติที่แท้จริงแม้เรียกผ่าน security-definer RPC), Profile integration ("Club ของฉัน" section แสดงเฉพาะ own profile ผ่าน optional-repository-param pattern ที่ตั้งใจไม่ thread ผ่านทุก call site เพราะ section ผูกกับ own-profile เท่านั้น), Home integration (toggle "สำหรับคุณ"/"จาก Club ของคุณ" ด้วย `SegmentedButton` เหนือ feed หลัก, "จาก Club ของคุณ" query ข้าม Club หลายอันพร้อมกันโดยพึ่ง RLS ของ `club_posts` ล้วนๆ ไม่ต้องสร้าง DB view ใหม่) — Coding พบและแก้ gap เอง 2 จุดก่อนส่ง QA (RenderFlex overflow ใน `ViewProfileScreen`'s My Clubs section ที่การเพิ่มความสูงแก้ overflow ภายในแต่กลับทำ overflow ของทั้งหน้าแย่ลงในสัดส่วน 1:1 — แก้ด้วยการหาค่ากึ่งกลางที่แม่นยำผ่านการทดสอบจริง) `flutter analyze`/`flutter test`: สะอาด 162/162 (เพิ่มจาก 148 เดิม) **QA รอบ 1 PASS**: sync `main` ใหม่หลัง merge PR #67 แล้วรัน `flutter analyze`/`flutter test` อิสระเองยืนยันตรงกับตัวเลขที่ Coding รายงาน, อ่าน SQL trigger ทั้ง 4 ตัวยืนยัน fan-out logic ถูกต้อง (เฉพาะ Owner/Admin ที่ approved ของ club เดียวกัน), ยืนยัน `auth.uid()` ใน UPDATE trigger จับผู้อนุมัติจริงไม่ใช่ requester, ยืนยัน self-notification guard ของ post_like/post_comment ถูกทิศ, ยืนยัน RLS ของ `notifications` ไม่มี insert policy ใหม่จาก column ที่เพิ่ม, ยืนยัน `fetchFromJoinedClubs` ไม่มีทางรั่ว RLS เพราะพึ่ง `club_posts`'s select policy ล้วนๆ ไม่ใช่ service-role bypass, ยืนยัน `ViewProfileScreen`'s optional Club repository ปลอดภัยด้วย double-gate ในโค้ดเอง (`_isOwnProfile && clubRepository != null`) ไม่ได้พึ่งวินัยของ caller — **ทำ red→green regression proof อิสระคนละจุดกับ Coding** (empty-state message ของ "จาก Club ของคุณ") — พบ Minor เดียวไม่ block: `FromYourClubsFeed`'s empty state ขาดปุ่ม "สำรวจ Club" ตามที่ Design spec ระบุไว้ตรงๆ (ไม่ block เพราะ `ClubSection`'s ปุ่มเดิมยังแสดงอยู่เสมอเหนือ toggle ไม่ว่าจะสลับโหมดไหน ผู้ใช้ไม่ตันจริง) `flutter test`: สะอาด 162/162 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่เจ็ดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014 ที่ผ่าน QA รอบเดียว
- **WYN Platform Expansion — ZOKY Marketplace (2026-08-14)**: Founder ส่ง "WYN PLATFORM — MASTER DEVELOPMENT PROMPT" ขยาย WYN จาก Social app เดียวเป็น **WYN Platform** 5 ส่วน: WYN Social (เดิม) / ZOKY Marketplace (E-commerce ใหม่) / ZOKY Sellers by WYN (แอปร้านค้า) / WYN Admin (หลังบ้านกลาง) / Shared Backend — ดูรายละเอียดเต็มที่ `.wyn/company/DECISIONS.md` (2026-08-14 "ขยาย WYN เป็น WYN Platform") **สถาปัตยกรรมที่ตัดสินใจแล้วสำหรับรอบนี้**: ZOKY Marketplace Customer เป็น feature module ใหม่ในแอป Flutter เดียวเดิม (`app/lib/features/zoky/`) ไม่สร้างแอปแยก, เพิ่ม Bottom Nav tab ที่ 5 "ZOKY" ต่อจาก Home/Drop/Pop/Profile เดิม (โครงสร้างเดิมไม่เปลี่ยน), backend ใช้ Supabase project เดียวกัน เพิ่ม table domain ใหม่เข้า `supabase/schema.sql` เดิม, งานใช้ prefix ใหม่ **ZOKY-XXX** แยกจาก WYN-XXX แต่อยู่ใน governance/workflow เดียวกันทั้งหมด (Product→Design→Code→QA→Debug) — **ขอบเขตรอบนี้**: ทำเฉพาะ ZOKY Marketplace Customer-facing (Home/Search/Product Detail/Store/Cart/Checkout/Order/Review) ตาม Phase 1-3 ของ master prompt ก่อน ยังไม่ทำ ZOKY Sellers by WYN/WYN Admin/Finance-Analytics-Moderation เต็มรูปแบบ/ZOKY Food-Rider-Delivery จนกว่าจะถึง Phase ที่เกี่ยวข้อง — WYN Social เดิมทั้งหมดไม่ถูกแตะต้อง ดู roadmap เต็มที่ `.wyn/docs/product/zoky-platform-roadmap.md` **ZOKY-001 (Marketplace Foundation) — PASS (QA รอบ 1, 2026-08-14)**: task แรกของสาย ZOKY — Bottom Nav tab ที่ 5 "ZOKY" (index 4 หลัง Profile ไม่กระทบ 4 tab เดิม), ZOKY Home (search/cart/orders placeholder, category chips, banner คงที่, "แนะนำสำหรับคุณ"/"ขายดี" เป็น label+"เร็ว ๆ นี้" เพราะยังไม่มี Order data ให้ rank, New Products/Recommended Stores เป็น horizontal card, main product grid infinite-scroll), Product Detail (carousel มิเรอร์ `ClubPostImages`, ราคา+ส่วนลด, variant chips preview-only, ปุ่ม Add to Cart/Buy Now แสดงผลแต่ไม่ทำงานจริง), Store (header มิเรอร์ `ViewProfileScreen`, ปุ่มติดตามร้าน placeholder, **ไม่มีปุ่ม Chat Seller เลย**เพราะไม่มีระบบ messaging ในโปรเจกต์ให้สื่อสารอย่างมีความหมาย) — Database: ตาราง `categories`/`stores`/`products`/`product_variants` ใหม่ RLS select-all-authenticated **ไม่มี insert/update/delete policy ให้ client เลยรอบนี้** เพราะยังไม่มี Seller workflow (Phase 4) — Coding พบและแก้เอง 2 จุดก่อนส่ง QA: ใส่ ZOKY tab ผิดตำแหน่ง (ก่อน Profile แทนที่จะเป็นหลัง) และบั๊กจริงใน `StoreScreen`'s `FutureBuilder<Store?>` (`AsyncSnapshot.hasData` คือ `data != null` ไม่ใช่เช็ค connection state จริง ทำให้ future ที่ resolve เป็น `null` จริง (ร้านไม่พบ) แสดง spinner หมุนไม่จบตลอดกาล แก้ด้วยเช็ค `connectionState != ConnectionState.done` แทน) `flutter analyze`/`flutter test`: สะอาด 191/191 (เพิ่มจาก 162 เดิม) **QA รอบ 1 PASS**: sync `main` ใหม่หลัง merge PR #71 แล้วรัน `flutter analyze`/`flutter test` อิสระเองยืนยันตรงกับตัวเลขที่ Coding รายงาน, อ่าน RLS ยืนยันตารางใหม่ทั้ง 4 ไม่มี insert/update/delete policy จริง, อ่าน diff ยืนยันไม่มีไฟล์ WYN Social เดิมไฟล์ไหนถูกแก้ logic นอกจาก `root_shell.dart`'s เพิ่ม tab ใหม่ (ไม่แก้ของเดิม), ทำ red→green regression proof อิสระคนละจุดกับ Coding (`Product.discountPercent`'s สูตรคำนวณเปอร์เซ็นต์ส่วนลด) — พบ Minor เดียวไม่ block: `ProductDetailScreen`/`StoreScreen` ไม่แสดง "Rating" เลยทั้งที่ Product Requirements และ Design Components ระบุไว้ตรง ๆ ทั้งคู่ (ไม่ block เพราะไม่มี AC ทดสอบตรง ๆ และไม่มี data source ให้ rating จริงรอบนี้ แต่บันทึกไว้ว่าเป็นครั้งที่ 5 ของ pattern "spec ระบุ component ชัดเจน Coding มองข้ามไม่บันทึกเป็น scope cut ที่ตั้งใจ") `flutter test`: สะอาด 191/191 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่แปดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015 ที่ผ่าน QA รอบเดียว และเป็น task แรกของสาย ZOKY Marketplace ที่ผ่าน QA **ZOKY-002 (Search & Filter) — PASS (QA รอบ 1, 2026-08-14)**: task ที่สองของสาย ZOKY — `ZokySearchScreen` ใหม่ (แยกจาก `SearchScreen` เดิมของ WYN Social โดยตั้งใจ คนละ entry point คนละ content domain แต่ reuse pattern debounce+TabBar+`SearchStateMessage` เป๊ะ) ค้นหา Product/Store ตามชื่อ (ILIKE case-insensitive เรียกตรงไม่ผ่าน `.or()` string ป้องกัน Minor แบบเดียวกับที่เคยพบใน WYN-009), แท็บ Product มีปุ่ม "ตัวกรอง" (Category chip + ช่วงราคา 2 ช่อง text field ใน bottom sheet) และ "เรียงลำดับ" (Newest/Price Low→High/Price High→Low ผ่าน dropdown menu apply ทันที — Rating/Sales/Recommended sort defer เหตุผลเดียวกับ ZOKY-001's Known Issues เพราะยังไม่มีข้อมูล Order/Review), แท็บ Store แสดงผลเป็น `StoreResultCard` ใหม่ (มิเรอร์ `ClubDiscoveryCard` โครงแถวเต็มความกว้าง ต่างจาก `StoreMiniCard` เดิม) — ZOKY Home's search bar/category chip ที่เคย `SnackBar` placeholder เปลี่ยนไปเปิด `ZokySearchScreen` จริง (category chip ส่ง `initialCategory` pre-filter) — Coding พบและแก้เอง 1 จุดก่อนส่ง QA: `RecordingZokyRepository` สร้าง inline ใน testWidgets callback (gotcha "Timer is still pending" ที่เคยบันทึกไว้แล้ว) ย้ายเข้า `setUp()` `flutter analyze`/`flutter test`: สะอาด 200/200 (เพิ่มจาก 191 เดิม) **QA รอบ 1 PASS**: sync `main` ใหม่หลัง merge PR #75 แล้วรัน `flutter analyze`/`flutter test` อิสระเองยืนยันตรงกับตัวเลขที่ Coding รายงาน, อ่าน `.ilike()` ยืนยันเรียกตรงไม่ผ่าน `.or()` string ปลอดภัยจริง, อ่าน diff ของ PR #75 ยืนยันไม่มีไฟล์ใต้ `app/lib/features/search/` (WYN-009) ถูกแก้แม้แต่บรรทัดเดียว ยืนยัน `ZokySearchScreen` แยกจริงไม่กระทบ WYN Social Search, ไล่ Requirements/Design Components/Acceptance Criteria ครบทั้ง 3 หัวข้อไม่พบ gap เลย, ทำ red→green regression proof อิสระคนละจุดกับ Coding (สลับ `PopupMenuItem`'s value ของ Sort menu ให้ label ผูกผิดคู่ชั่วคราว) — **ไม่พบ finding ใด ๆ เลยรอบนี้ (ครั้งแรกในสาย ZOKY)** `flutter test`: สะอาด 200/200 — ย้ายเข้า `.wyn/tasks/approved/` แล้ว รอ AI Deploy & DevOps เมื่อมี infra จริง — เป็น task ที่เก้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001 ที่ผ่าน QA รอบเดียว **ZOKY-003 (Cart & Checkout & Order) — Product spec ส่งมอบแล้ว (2026-08-14)**: task ที่สามของสาย ZOKY — Founder ยืนยัน "ทำจนเสร็จให้หมดทุกอย่างเลยนะ พอดีจะนอนแล้ว" ทีมทำงานต่อเนื่องอัตโนมัติยาวจน ZOKY-003/ZOKY-004 เสร็จโดยไม่ต้องรอ Founder ตอบกลับระหว่างทาง ขอบเขตที่ตัดสินใจไว้ในสเปก: ไม่มี payment gateway จริง (ปุ่ม "ยืนยันคำสั่งซื้อ" สร้าง Order ตรง ๆ), ค่าธรรมเนียม `ZOKY_MARKETPLACE_FEE` เก็บใน `platform_config` table (default 10%) และ snapshot ค่าไว้ในแต่ละ Order ตอนสร้างไม่ query ค่าปัจจุบันย้อนหลัง, 1 Order ต่อ 1 ร้านค้า (ตะกร้าเดียวมีได้หลายร้าน แต่ Checkout แยกสร้าง Order ทีละร้าน), ที่อยู่จัดส่งเป็นฟอร์มข้อความอิสระต่อ Order ไม่มีสมุดที่อยู่รอบนี้, สถานะ Order มีแค่ 3 แบบ (pending/delivered/cancelled — ไม่มี "จัดส่งแล้ว" แยกเพราะยังไม่มี ZOKY Sellers by WYN ที่จะ trigger สถานะนั้นจริง เป็น known simplification ที่บันทึกไว้ชัดเจน ไม่ใช่บั๊ก), stock หักจาก `products.stock` เท่านั้นไม่หักจาก `product_variants.stock` (variant ยังเป็น preview-only เหมือน ZOKY-001) — เน้น RPC ต้อง atomic จริง (ตรวจ+หัก stock+สร้างหลาย Order+snapshot ค่าธรรมเนียม ในธุรกรรมเดียว ป้องกัน race condition ตอนหลาย Order แย่ง stock พร้อมกัน) ส่งต่อ AI Design แล้ว
- **Color Direction ใหม่ (2026-08-14)**: Blue + White + Soft Gray (Founder กำหนดตรง แทนที่สี Primary ม่วง/ชมพูที่เคย PROPOSED ไว้) ห้ามใช้ Liquid Glass ห้ามลอก Layout ของ Instagram/TikTok โดยตรง ดูรายละเอียดที่ `.wyn/docs/design/design-principles.md`
- **Design Principles**: PROPOSED โดย AI Design — Mobile-first, Gen Z-friendly, รองรับ Light/Dark mode, accessibility AA เป็นค่าเริ่มต้น ดูรายละเอียดเต็มที่ `.wyn/docs/design/design-principles.md` (ยังไม่มี brand guideline จริงจาก Founder)
- **Technology Stack**: Mobile-first — **Flutter (Dart)** สำหรับ frontend (แทนที่ React Native เดิม); Supabase (PostgreSQL + Auth + Storage + Realtime + Edge Functions) สำหรับ backend ใช้ผ่าน `supabase_flutter` package (ระบุโดย Founder ตรงเมื่อ 2026-08-13 ดู `.wyn/company/DECISIONS.md`)
- **Architecture**: Mobile app (Flutter, ภาษา Dart) เรียกใช้ Supabase โดยตรงสำหรับ Auth/Database/Storage/Realtime ผ่าน `supabase_flutter` และใช้ Supabase Edge Functions สำหรับ backend logic ที่ต้องการความปลอดภัยเพิ่มเติม เว็บแอปเป็นเฟสถัดไป (ยังไม่กำหนดรายละเอียด)
- **Business Rules**: UNKNOWN

## หมายเหตุ

ฟิลด์ที่ยังเป็น `UNKNOWN` จะถูกอัปเดตเป็นค่าจริงเมื่อ Founder ให้ข้อมูลหรืออนุมัติ ห้ามเดาหรือสมมติข้อมูลที่ยังไม่ได้รับการยืนยัน — ให้คงสถานะ `UNKNOWN` ไว้จนกว่าจะมีการยืนยันจริง

เมื่อ Founder ให้ข้อมูลเกี่ยวกับหัวข้อใดด้านบน ให้ AI Product Manager เป็นผู้ปรับปรุงไฟล์นี้ และบันทึกการตัดสินใจถาวรที่เกี่ยวข้องไว้ที่ `.wyn/company/DECISIONS.md` ด้วย
