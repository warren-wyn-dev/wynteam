# Mistakes Log

บันทึกข้อผิดพลาดที่เกิดขึ้น เพื่อป้องกัน QA และทีมไม่ให้พลาดซ้ำ

## รูปแบบ

```
### [YYYY-MM-DD] Task WYN-XXX
- ข้อผิดพลาด:
- ผลกระทบ:
- วิธีป้องกันในอนาคต:
- Regression test ที่เพิ่ม (ถ้ามี):
```

## รายการ

### [2026-09-02] Task WYN-081 (QA รอบ 2 → Debug — `ExploreClubsScreen._reload()` ยัง arrow-body `setState` ที่ return Future อยู่ ทั้งที่ commit เดียวกันอ้างว่า "แก้ครบ 5 จุดแล้ว")
- ข้อผิดพลาด: WYN-081's Coding Output เอง (commit `563049e`) พบและแก้บั๊ก `setState(() => x = asyncCall())` (arrow-body closure ที่ return Future แทน void) ใน 5 จุดทั่วโปรเจกต์ แล้วอ้างว่า "แก้ทั้ง 5 จุดเดิม + 2 จุดใหม่...ทุกจุด" — แต่ `ExploreClubsScreen._reload()` (ไฟล์เดียวกันที่ commit นี้กำลังแก้ไขอยู่พอดี) ยังเหลือ pattern เดียวกันอยู่ ไม่ถูกแก้ เป็น pre-existing method ที่อยู่ติดกับ `_onRefresh()` ตัวใหม่ที่เพิ่งเขียนถูกในงานเดียวกัน
- ผลกระทบ: กด "เข้าร่วม Club" สำเร็จจริง (`joinClub()` resolve แล้ว) แต่ผู้ใช้เห็น snackbar "เข้าร่วม Club ไม่สำเร็จ ลองใหม่อีกครั้ง" ผิดๆ ทันที เพราะ `_reload()` โยน `FlutterError` ที่ถูก `_join()`'s catch block กลืนแล้วตีความผิดว่า join ทั้งกระบวนการล้มเหลว — misleading error บนฟีเจอร์หลักที่ใช้บ่อย
- วิธีป้องกันในอนาคต: เมื่อแก้บั๊ก pattern ที่เกิดซ้ำหลายจุด (เช่น grep แล้วพบ N จุด) ต้อง grep **หลังแก้เสร็จ** อีกรอบด้วย pattern เดียวกัน ไม่ใช่เชื่อรายการที่ list ไว้ตอนเริ่มงานว่าครบแล้ว — โดยเฉพาะไฟล์ที่กำลังแก้ไขอยู่พอดี (สุ่มเสี่ยงสูงสุดที่จะมี method เดิมข้างๆ method ใหม่ที่เพิ่งเขียนถูก แต่สายตาข้ามไปเพราะโฟกัสที่โค้ดใหม่ที่กำลังเขียน) — AI QA & Security ควรทำ adversarial re-grep แบบนี้เป็นมาตรฐานทุกครั้งที่ Coding Output อ้างว่า "แก้ครบทุกจุดของ pattern X" ไม่ใช่เชื่อคำอ้างเฉยๆ
- Regression test ที่เพิ่ม: `app/test/explore_clubs_screen_test.dart` — `'QA (WYN-081): a successful Join does not show the "เข้าร่วม Club ไม่สำเร็จ" failure snackbar'` (เพิ่มโดย QA, พิสูจน์ red แล้ว ก่อน Debug แก้ให้ green)

### [2026-09-02] Task WYN-102 (QA รอบ 2 → Debug — `push_notification_service.dart`'s `_openPop()` ไม่ถูกแก้คู่กับ `notification_list_screen.dart`'s `_openPop()` ทั้งที่ comment ของไฟล์บอกตรงๆ ว่า "mirrors...exactly")
- ข้อผิดพลาด: WYN-102 แก้ `notification_list_screen.dart`'s `_openPop()` ให้แสดง SnackBar แทนการ fetch+navigate ไป Pop จริง แต่ไม่ได้แตะ `push_notification_service.dart`'s `_openPop()` เลย ทั้งที่ไฟล์นี้เขียน doc comment ของตัวเองไว้ตรงๆ ว่า "mirrors `NotificationListScreen._openNotification`'s switch exactly" — เป็น 2 switch statement คนละไฟล์ที่ควรถูกแก้คู่กันเสมอเมื่อแก้ notification-driven navigation path ใดๆ แต่ grep/review รอบแรกไม่ได้ตามไปดูไฟล์คู่แฝดนี้
- ผลกระทบ: ผู้ใช้แตะ push notification ประเภท `like_pop`/`comment_pop` (ยังส่งได้จริงเพราะ backend trigger ไม่ถูกบล็อกตามที่ WYN-102 ตั้งใจ) ยังเปิด `PopSingleClipScreen` เต็มรูปแบบได้ ขัดกับ Acceptance Criteria "หาทางเข้าถึงฟีเจอร์ Pop จากหน้า UI ไม่เจอแล้วทุกจุด" ตรงๆ — เป็น access point ที่สมบูรณ์ ไม่ใช่แค่ dead-link
- วิธีป้องกันในอนาคต: เมื่อไฟล์หนึ่งมี doc comment บอกว่า "mirrors ไฟล์ X exactly" ให้ถือเป็นสัญญาณบังคับว่าทุกครั้งที่แก้ไฟล์ X ต้อง grep หา "mirrors" ไฟล์นั้นแล้วตรวจไฟล์คู่แฝดด้วยเสมอ ไม่ใช่แค่แก้ไฟล์เดียวที่ QA/Coding บังเอิญมองเห็นก่อน — ควรเพิ่มเป็น checklist item ของ QA เวลาเจอ comment แบบนี้ในไฟล์ที่กำลังตรวจ
- Regression test ที่เพิ่ม: `app/test/push_notification_service_test.dart` — กลุ่มเทสใหม่ `'WYN-102: a push notification about a Pop no longer opens Pop content'` (2 เคส `like_pop`/`comment_pop`) พิสูจน์ red (ก่อนแก้: fetch ไป Supabase จริงแล้ว throw `PostgrestException`) → green (หลังแก้: เห็น SnackBar เท่านั้น) — ก่อนหน้านี้ไฟล์เทสนี้ไม่มี test case ใดครอบคลุม Pop-type push เลย


### [2026-08-24] Task WYN-044 (2 session ทำงานซ้ำซ้อนกันโดยไม่รู้ตัว — สาเหตุคือ CONTEXT.md ไม่ถูกอัปเดตต่อเนื่องตั้งแต่ WYN-040)
- ข้อผิดพลาด: ตั้งแต่ WYN-040 เป็นต้นมา (Phase 4/5/6 ทั้งหมด, 9 task) ไม่มี session ไหนอัปเดต `.wyn/company/CONTEXT.md` เลย ทั้งที่ AGENTS.md/COMPANY.md ระบุตรงๆ ว่าไฟล์นี้คือ source of truth ที่ทุก role ต้องอ่านก่อนเริ่มงานทุกครั้ง — บันทึกไว้แค่ใน task file รายตัว (`.wyn/tasks/approved/WYN-0XX-*.md`) เท่านั้น เป็นผลให้ session ใหม่ (`claude/phase-7-continuation-5s8by3`) อ่าน CONTEXT.md แล้วเข้าใจว่างานล่าสุดคือ WYN-039 (Phase 3 ปิด) จึงต้องไปเปิด `.wyn/docs/product/wyn-v1.0.0-roadmap.md` เองเพื่อหาว่า WYN-040 ถึง 043 เสร็จไปแล้วจริง (เจอจาก git log ไม่ใช่จาก CONTEXT.md) แล้วเริ่มทำ **WYN-044 เวอร์ชันของตัวเอง** (6 หมวด gate, ไม่มี `trending`) โดยไม่รู้เลยว่ามีอีก session หนึ่ง (`claude/wyn-044-0saj5u`) ทำ WYN-044 เวอร์ชันที่ต่างกัน (7 หมวดตาม Master Spec section 21 ตรงตัว) พร้อม WYN-045 ถึง WYN-048 (Phase 6 ทั้ง Phase) เสร็จและเปิด PR #161 ค้างไว้ตั้งแต่ก่อนหน้านั้นแล้ว
- ผลกระทบ: งานซ้ำซ้อนเต็มรูปแบบ 1 task (WYN-044 เวอร์ชันหลัง เขียน SQL/Flutter/test ครบชุดโดยไม่มีประโยชน์ เพราะต้องทิ้งทั้งหมด) — เปิด PR ชนกัน 2 ใบ (#161/#162) บน task ID เดียวกัน คนละ implementation คนละ mapping หมวดหมู่ — โชคดีที่ Founder ตรวจพบเองก่อนที่ทั้งสอง PR จะ merge (ถ้า merge ทั้งคู่จะชนกันที่ `notifications_settings`/`notification_settings` table + gate mapping ไม่ตรงกัน)
- วิธีป้องกันในอนาคต: (1) **ทุก role ต้องอัปเดต `.wyn/company/CONTEXT.md` ทันทีที่จบแต่ละ milestone ที่มีความหมาย** (Product spec เสร็จ, Design เสร็จ, Coding เสร็จ, QA PASS/FAIL, Deploy เสร็จ) ไม่ใช่แค่บันทึกใน task file ของตัวเองแล้วจบ — นี่คือกติกาที่ AGENTS.md/COMPANY.md ระบุไว้อยู่แล้วแต่ไม่มีการบังคับจริง (2) ก่อนเริ่ม Product spec ของ task ใหม่ทุกครั้ง ให้เช็ค `git log --all --oneline | grep -i "wyn-0XX"` (task ID ถัดไปตาม roadmap) และเช็ค open PR ด้วย `list_pull_requests` ก่อนเสมอ ไม่ใช่เชื่อ CONTEXT.md/roadmap doc เพียงอย่างเดียว โดยเฉพาะเมื่อพบว่า CONTEXT.md's ล่าสุดดูค้างนานผิดปกติเทียบกับ commit ล่าสุดบน `main` (3) เมื่อพบ PR ชนกัน ให้ diff ทั้งสองฝั่งและตรวจสอบ build/test อิสระก่อนตัดสินใจ merge ฝั่งไหน ไม่ใช่เชื่อ commit message/QA claim ของอีกฝั่งเฉยๆ (ตรวจสอบจริงในเคสนี้: pull branch มา รัน `flutter analyze`/`flutter test`/SQL suite ทั้ง 21 สคริปต์เองก่อน merge PR #161 จริง)
- Regression test ที่เพิ่ม: N/A (เป็น process/documentation gap ไม่ใช่บั๊กโค้ด) — แก้โดยเพิ่ม catch-up entry ใน CONTEXT.md (2026-08-24) สรุปสถานะ WYN-040 ถึง 048 ทั้งหมดให้ตรงกับ git history จริง

### [2026-08-22] Task WYN-029 (QA อิสระรอบ 1 → Debug — `notifications.actor_id` รั่วตัวตนผู้ดูแลที่รีวิว Warning/Remove Content ให้ target เห็นได้ตรงๆ)
- ข้อผิดพลาด: `apply_moderation_action()` (`supabase/schema.sql`, WYN-029) เขียน `insert into public.notifications (recipient_id, actor_id, type, reason) values (v_target_user, v_reviewer, ...)` สำหรับทั้ง Warning และ Remove Content — ใส่ `auth.uid()` ของผู้ดูแลจริงลงคอลัมน์ `actor_id` ทั้งที่ตั้งใจปกป้องตัวตนนี้ไว้แล้วอย่างถูกต้องใน `moderation_actions.reviewer_id` (ไม่มี SELECT policy ให้ client เข้าถึงเลย) — `notifications` มี RLS row-level ธรรมดา (`auth.uid() = recipient_id`) ที่ target อ่านแถวของตัวเองได้ปกติ ทำให้ตัวตนที่ป้องกันไว้แล้วรั่วออกไปทางคอลัมน์ที่สอง การป้องกันจริงถูกทำแค่ที่ Dart UI layer (`NotificationListScreen._hidesActorIdentity()` ซ่อนแค่การ render ของ 1 หน้าจอ) ไม่ใช่ data-access boundary — เป็น mistake class เดียวกับ WYN-027 (UI/client-side omission ไม่ใช่ data-access boundary) ที่เกิดซ้ำอีกครั้งในรูปแบบใหม่
- ผลกระทบ: target ของ Warning/Remove Content (ผู้ใช้ที่ถูก moderator ดำเนินการ) สามารถ re-issue query แบบเดียวกับที่แอปใช้ตรงๆ (หรือ raw REST call) แล้วเห็นชื่อผู้ใช้/ชื่อที่แสดงจริงของผู้ดูแลที่รีวิวเคสของตัวเอง — ขัดกับเจตนาการออกแบบที่ระบุไว้ตรงๆ ทั้งใน SQL comment และ Dart comment ("must never surface", มิเรอร์ทิศตรงข้ามของ WYN-026 ที่ปกป้องตัวตนผู้รายงาน) — เป็น Trust & Safety privacy leak ที่อาจทำให้ผู้ดูแลถูกคุกคามย้อนกลับจากผู้ใช้ที่ถูกลงโทษ
- วิธีป้องกันในอนาคต: (1) ทุกครั้งที่ feature ใหม่ denormalize identity/ข้อมูล sensitive จากตารางที่ป้องกันไว้แล้วไปยังตารางอื่นเพื่อความสะดวก ต้องเช็ค SELECT policy ของตารางปลายทางเทียบกับต้นทางเสมอ (บันทึกไว้ที่ `.wyn/learning/LESSONS_LEARNED.md` แล้ว) (2) เมื่อพบว่าคอลัมน์ที่ตั้งใจปกป้องต้องเป็น `NULL` แทนค่าจริงในบางเคส ต้อง grep ทั้ง schema.sql และ client code ก่อนเปลี่ยน `not null` constraint เพื่อประเมิน blast radius จริง ไม่ใช่เดาว่า "แค่ 2 จุดที่เกี่ยวข้อง" (3) UI-layer hide (เช่น `_hidesActorIdentity()`) ใช้เป็น defense-in-depth ได้ แต่ต้องมี data-layer guarantee รองรับเสมอ ไม่ใช่พึ่ง UI logic เป็นเกราะป้องกันเดียว
- Regression test ที่เพิ่ม: ขยาย `supabase/tests/wyn_029_moderation_queue_test.sh` เพิ่ม 4 checks ใหม่ (`CHECK7e`/`CHECK7f` สำหรับ Warning, `CHECK21b`/`CHECK21c` สำหรับ Remove Content) ยืนยันทั้ง `actor_id is null` โดยตรง และ re-run query join แบบเดียวกับที่แอปใช้เป็น target role ยืนยัน 0 rows (ไม่ใช่แค่ absent จาก column list ที่เลือกเอง) — พิสูจน์ red→green จริงด้วย `git stash`/`git stash pop` (4 checks fail ก่อนแก้ → 36/36 ผ่านหลังแก้) รวมถึง `app/test/notification_test.dart` ใหม่ (model-level `WynNotification.fromMap` null-actor tests) และเคสใหม่ใน `app/test/notification_list_screen_test.dart` (widget-level null-actor render, พิสูจน์ compile-fail จริงก่อนแก้ Dart model ด้วย `git stash` เช่นกัน) ดู `.wyn/tasks/bugs/WYN-029-moderation-actor-identity-leak.md` หัวข้อ "Debug Output"

### [2026-08-22] Task WYN-024/DS-009 (QA รอบ 2 → Debug รอบ 2) — Coding เบี่ยงเบนจาก Design spec เอง (ใส่ accent "ใน" label แทนที่จะเป็น "นอก" ตามที่ระบุไว้ตรงๆ) และมีบั๊กแฝงเก่าที่ไม่มีใครเคยเจอมาก่อน (label wrap หลายบรรทัดที่จอแคบ)
- ข้อผิดพลาด: Design spec (`ds-009-rainbow-accent.md` ข้อ 2) ระบุตรงๆ ว่า accent indicator ของ segment ที่ active ต้อง "วางนอก touch target เดิมของปุ่ม ไม่ทับตัวหนังสือ" — แต่ตอน Coding implement จริง ใส่จุด Rainbow เป็น `Row` **ข้างใน** label เดียวกับตัวหนังสือ (แย่งพื้นที่แนวนอนกันตรงๆ) ไม่ตรงกับ spec เลย — เป็นต้นเหตุของทั้งบั๊ก overflow รอบ 1 และบั๊ก label หายเกือบหมดรอบ 2 (การแก้แบบผิวเผินด้วย `Flexible`+ellipsis ในรอบ 1 ของ Debug แก้แค่ "ไม่ crash" แต่ไม่ได้แก้ที่ต้นตอ) — ระหว่างที่ Debug Engineer แก้ตาม spec จริง (ย้าย accent ออกมาเป็นแถบใต้ปุ่มแยกต่างหาก) ก็ยังพบว่าตอนใช้ `Text` ธรรมดาไม่มี `maxLines` เลย (ย้อนกลับไปเหมือนโค้ดดั้งเดิมของ WYN-015) ข้อความ "จาก Club ของคุณ" **wrap ขึ้นหลายบรรทัด** (สูงถึง ~160px จาก ~20px ปกติ) ที่ความกว้างจอมือถือจริง — เป็นบั๊กที่ **มีอยู่แล้วตั้งแต่ WYN-015** (ก่อน DS-009 จะแตะไฟล์นี้ด้วยซ้ำ) เพราะไม่มีใครเคยทดสอบ segment ที่ยาวที่สุดที่ความกว้างจอแคบมาก่อนเลย
- ผลกระทบ: บั๊ก overflow/text-disappear ที่เจอในรอบ 1-2 ทั้งหมดสืบมาจากจุดเดียว (ไม่ทำตาม spec) — และการไล่แก้ให้ตรง spec เผยบั๊กที่สองซึ่งซ่อนอยู่นานกว่าและกว้างกว่า (ทุกครั้งที่ "จาก Club ของคุณ" active บนจอแคบ ทั้งแถว SegmentedButton จะสูงขึ้นผิดปกติ) — ยืนยันด้วย screenshot จริง (`RenderRepaintBoundary.toImage`) ไม่ใช่แค่ตัวเลข
- วิธีป้องกันในอนาคต: (1) เมื่อ implement ตาม Design spec ที่ระบุตำแหน่ง/ความสัมพันธ์ของ element ไว้ชัดเจน (เช่น "นอกตัว X" "ไม่ทับ Y") ต้องอ่านและทำตามตรงตัว ไม่ใช่ตีความแบบง่ายกว่า (inline dot ง่ายกว่า separate indicator แต่ผิด spec) — เบี่ยงเบนจาก spec แม้เพื่อความง่าย ต้องบันทึกเหตุผลไว้ให้ QA/Design ตรวจสอบ ไม่ใช่ทำเงียบๆ (2) `Text` ใหม่ทุกจุดที่อาจไม่พอที่ (โดยเฉพาะใน `SegmentedButton`/`Row`/`Chip` ที่มีพื้นที่จำกัด) ต้องกำหนด `maxLines: 1` + `overflow: TextOverflow.ellipsis` เป็นค่าเริ่มต้นเสมอ ไม่ใช่ปล่อยให้ wrap โดยไม่ตั้งใจ — การไม่ crash ไม่ได้แปลว่า layout ถูกต้อง (wrap ที่ทำให้ row สูงผิดปกติก็เป็นบั๊กเหมือนกัน แค่ไม่ throw exception)
- Regression test ที่เพิ่ม: `app/test/home_feed_screen_test.dart` — เทสต์ใหม่ "the widest segment label ... stays single-line at real phone widths" ที่ 360px ยืนยันด้วย `RenderParagraph.size.height < 25` (พิสูจน์ red→green จริง: revert โค้ดกลับไปเวอร์ชันที่มี `Flexible`+ellipsis inline ของรอบ 1 ของ Debug แล้วรันเทสต์เดิม — ผ่านเหมือนกัน เพราะเทสต์นี้จับเฉพาะปัญหา "wrap หลายบรรทัด" ไม่ใช่ปัญหา "แคบเกินอ่านไม่ออก" ซึ่งเป็นข้อจำกัดที่ยอมรับและบันทึกไว้ในรายงานแล้วว่าเป็นปัญหาคนละมิติที่ลึกกว่าและกว้างกว่าของ SegmentedButton 4 ช่องกับ label ภาษาไทยยาว — ยังไม่แก้เต็มรูปแบบ เสนอให้ Product/Design พิจารณาย่อข้อความ "จาก Club ของคุณ" เป็นทางแก้ระยะยาวถ้า QA รอบ 3 ยังเห็นว่าไม่พอ)

### [2026-08-22] Task WYN-024/DS-009 (Bottom Nav V1.0.0 Restructure + Rainbow Accent) — ส่งมอบโดยไม่เคยรัน `flutter analyze`/`flutter test` เลยสักครั้ง เพราะเข้าใจผิดว่า Flutter SDK ดาวน์โหลดไม่ได้ในเซสชันนั้น
- ข้อผิดพลาด: (1) `root_shell.dart` ใช้ `const Semantics(...)` ทั้งที่ `Semantics`' constructor ไม่ใช่ const บน Flutter SDK เวอร์ชันนี้ — compile error พื้นฐานที่ analyzer จับได้ทันที (2) `home_feed_screen.dart`'s `_segment()` (Rainbow accent dot ใหม่จาก DS-009) ห่อ dot+spacing+`Text(label)` ด้วย `Row(mainAxisSize: min)` เฉยๆ โดยไม่มี `Flexible`/`Expanded` ครอบ `Text` — ทำให้ overflow 11px ทันทีที่ segment นั้น active (รวมถึง "สำหรับคุณ" ที่เป็นค่าเริ่มต้นตอนเปิด Home) (3) test file ใหม่ `root_shell_test.dart` สร้าง `RecordingXRepository` (และ `SupabaseClient` auto-refresh Timer ข้างใน) ใหม่ทุกครั้งที่ `buildShell()` ถูกเรียก (คือทุก test) แทนที่จะสร้างครั้งเดียวใน `setUpAll` — เป็น anti-pattern ที่บันทึกไว้แล้วใน `.wyn/learning/PATTERNS.md`/`LESSONS_LEARNED.md` จาก WYN-005 แต่ไม่ได้ตรวจสอบก่อนเขียนไฟล์ใหม่ — ทั้งหมดนี้ไม่มีใครจับได้เพราะ **Coding ไม่มี Flutter SDK ในเซสชันนั้นและเข้าใจว่าดาวน์โหลดไม่ได้ (ลองแค่ `github.com` แล้วเจอ 403 สรุปว่า network ถูกบล็อกทั้งหมด) จึงส่งมอบโดยไม่เคย compile/test เลยสักครั้ง**
- ผลกระทบ: แอปคอมไพล์ไม่ผ่านทั้งแอป (ไม่ใช่แค่ feature เดียว) + Home ซึ่งเป็นหน้าจอหลักของแอป overflow ทันทีที่เปิดโดย default + test ใหม่ทั้งไฟล์ (6 เทสต์) fail หมดด้วยเหตุผลที่ไม่เกี่ยวกับ logic จริงเลย — ถ้าไม่มี QA รอบนี้บังเอิญพบว่า `storage.googleapis.com` เข้าถึงได้ (ต่างจาก `github.com`) แล้วติดตั้ง Flutter เองสำเร็จ บั๊กทั้ง 3 จุดนี้จะไม่ถูกจับได้เลยจนกว่าจะมีคนรันแอปจริง
- วิธีป้องกันในอนาคต: (1) ก่อนสรุปว่า "ไม่มี Flutter SDK และดาวน์โหลดไม่ได้" ให้ลอง `https://storage.googleapis.com/flutter_infra_release/releases/releases_linux.json` ก่อนเสมอ (บันทึกขั้นตอนละเอียดไว้ที่ `.wyn/learning/LESSONS_LEARNED.md` แล้ว) — "ไม่มี SDK" ไม่ใช่เหตุผลที่ยอมรับได้สำหรับการส่งมอบโดยไม่ทดสอบ ถ้ายังไม่ได้ลองดาวน์โหลดจริงก่อน (2) `Row` ใหม่ที่ผสม fixed-size widget (icon/dot) กับ `Text` ที่ไม่มี cap ความยาว (โดยเฉพาะข้อความภาษาไทยที่ยาวกว่าอังกฤษ) ต้องครอบ `Text` ด้วย `Flexible`/`Expanded` เสมอเป็นค่าเริ่มต้น — เป็นบทเรียนเดิมจาก ZOKY-004 (ด้านล่าง) ที่เกิดซ้ำอีกครั้งในบริบทใหม่ (SegmentedButton label แทน StoreScreen header) (3) ทุกครั้งที่เขียน test file ใหม่ที่มี repository ที่ extends จาก class ที่ wrap `SupabaseClient` ต้องเช็ค `.wyn/learning/PATTERNS.md` เรื่อง "setUpAll เท่านั้น" ก่อนเขียน ไม่ใช่แค่ตอนแก้ไฟล์เดิมที่มี pattern ให้เห็นอยู่แล้ว
- Regression test ที่เพิ่ม: ไม่ต้องเพิ่มใหม่ — test ที่ QA เขียนไว้แล้ว (`home_feed_screen_test.dart`'s ขยาย "ติดตาม"/Rainbow-dot tests, `root_shell_test.dart` ทั้งไฟล์) ทำหน้าที่เป็น regression test ของทั้ง 3 บั๊กอยู่แล้วในตัว หลังแก้ไข: `flutter analyze` 0 error ทั้ง `app/`/`seller_app/`, `flutter test` 357/357 ผ่านใน `app/` (พิสูจน์ red→green จริง: revert การแก้ทั้ง 2 จุดใน `app/lib` ชั่วคราว → กลับไป error/4 failing เหมือนเดิม → restore fix → 0 error/0 failing) — ระหว่างแก้ Bug 3 พบบั๊กที่ 4 เพิ่มเติมใน `root_shell_test.dart` เอง (ไม่ใช่บั๊กแอป): `tester.pageBack()` หา `CreateDropScreen`'s close button ไม่เจอเพราะ AppBar ใช้ custom `leading` (ไอคอน X) แทน default back button — แก้โดยเปลี่ยนไป `tester.tap(find.byIcon(Icons.close))` ตรงๆ

### [2026-08-16] Task WYN-021 (QA อิสระรอบ 1 — `club_post_mentions`'s select RLS policy รั่ว, ครั้งแรกที่ QA อิสระของชุด WYN-017–022 พบ Major security finding)
- ข้อผิดพลาด: `supabase/schema.sql`'s `club_post_mentions`'s `select` policy (`"Club post mentions are viewable by authenticated users"`) เขียนเป็น `using (true)` แทนที่จะ gate ด้วย `public.club_role(cp.club_id, auth.uid()) is not null` เหมือน sibling table 3 ตัวในไฟล์เดียวกัน (`club_posts`/`club_post_likes`/`club_post_comments`) — เป็นการมิเรอร์ผิดตัวจาก `drop_mentions` (ที่ `using (true)` ถูกต้องเพราะ `drops` เป็น global-public ไม่มี privacy boundary) แทนที่จะมิเรอร์จาก `club_post_likes`/`club_post_comments` ที่เป็น sibling ที่ถูกต้องของฟีเจอร์ Club — ไม่มีการบันทึกไว้เป็น documented/accepted tradeoff ใน Design spec เลย
- ผลกระทบ: authenticated user คนไหนก็ตาม (แม้ไม่เคยเป็นสมาชิก แม้แต่ pending ก็ไม่มี) อ่าน `club_post_mentions` ของ Club แบบ private ได้ตรง ๆ ผ่าน raw Supabase/PostgREST API (`GET /rest/v1/club_post_mentions?select=*`) เห็นทั้งว่ามีโพสต์ private นั้นอยู่จริง (`club_post_id`) และใครถูกแท็กในโพสต์นั้น (`mentioned_user_id`) — exploitable โดยไม่ต้องผ่านแอป Flutter เลย ขัดกับ invariant "club posts are members-only-visible at the DB layer" ที่ WYN-014 วางไว้ตรง ๆ
- วิธีป้องกันในอนาคต: ทุกครั้งที่ตารางใหม่ถูกสร้างในบริบทที่มี sibling table ที่แชร์ parent entity/privacy invariant เดียวกัน ต้อง diff policy shape เทียบกับ sibling ทุกตัวโดยเจาะจง ไม่ใช่แค่อ่านตารางใหม่ตัวเดียวแล้วตัดสินว่า logic สมเหตุสมผล (บันทึกไว้ที่ `.wyn/learning/LESSONS_LEARNED.md` แล้ว) — เมื่อมี live Postgres ให้ทดสอบ (sandbox นี้มี) ให้พิสูจน์ RLS ของตารางใหม่ด้วยการรันจริงเสมอเมื่อทำได้ ไม่ใช่อ่าน SQL อย่างเดียว
- Regression test ที่เพิ่ม: `supabase/tests/wyn_021_club_post_mentions_rls_test.sh` — สคริปต์ regression test ที่รันกับ live Postgres จริงตัวแรกของโปรเจกต์ที่ถูก persist ไว้ให้รันซ้ำได้ (ก่อนหน้านี้การตรวจ RLS กับ Postgres จริงเป็นแค่ manual step ที่ทำครั้งเดียวแล้วทิ้ง เช่น ตอน SCHEMA-001/`club_post_mentions` QA รอบ 1) — stub `auth`/`storage` เอง, seed private-Club fixture (outsider ไม่มีแม้แต่ pending membership), assert 5 เคส พิสูจน์ red→green จริงด้วยสคริปต์เดียวกัน (`git stash` ก่อนแก้ → `CHECK2_outsider_club_post_mentions` FAIL ตรงตามที่คาด → `git stash pop` คืน fix → ALL CHECKS PASSED) ดู `.wyn/tasks/bugs/WYN-021-club-post-mentions-rls-gap.md` หัวข้อ "Debug Output"

### [2026-08-16] Task DS-002 (dart format เผลอ reformat ไฟล์ที่ไม่เกี่ยวข้องกว่า 90 ไฟล์)
- ข้อผิดพลาด: หลังกวาดแทนที่ literal spacing/radius ด้วย `WynSpacing` token (84 ไฟล์ที่ตั้งใจแก้จริง) รัน `dart format --line-length 100` เพื่อ "จัดฟอร์แมตให้สวย" โดยไม่ตรวจสอบก่อนว่าโปรเจกต์นี้มี convention line-length เท่าไหร่ (ไม่มี config บังคับใน `analysis_options.yaml` และโค้ดเดิมมีบรรทัดยาวถึง 180 ตัวอักษร แปลว่าไม่เคยผ่าน `dart format` มาตั้งแต่ต้น) ผลคือคำสั่งนี้ไป reformat ไฟล์ที่ไม่เกี่ยวข้องกับ DS-002 เลยกว่า 90 ไฟล์ **รวมไฟล์ใน `data/` layer** (เช่น `zoky_repository.dart`, `product_variant.dart`) ที่ DS-002's Acceptance Criteria ห้ามแตะโดยตรง
- ผลกระทบ: ยังไม่ถูก commit (จับได้ก่อนจาก `git diff --stat` ที่แสดงไฟล์เกินคาดจำนวนมาก) — revert ด้วย `git checkout` ทันทีที่พบ ไม่มีผลกระทบจริงต่อ repository
- วิธีป้องกันในอนาคต: ก่อนรัน formatter บนโค้ดที่มีอยู่แล้ว (ไม่ใช่ไฟล์ใหม่) ต้องตรวจสอบ convention เดิมของโปรเจกต์ก่อนเสมอ (เช่น เช็คความยาวบรรทัดสูงสุดที่มีอยู่จริง, หา config ไฟล์) — ถ้าโค้ดเดิมไม่เคยผ่าน formatter มาก่อน ห้ามรัน formatter แบบ blanket ทับทั้งโฟลเดอร์ ให้จำกัดเฉพาะไฟล์ที่ตั้งใจแก้จริงเท่านั้น (`dart format <file1> <file2> ...` แทน `dart format <directory>/`) และตรวจ `git diff --stat` ทุกครั้งหลังรันคำสั่งที่มีขอบเขตกว้างก่อน commit เสมอ
- Regression test ที่เพิ่ม (ถ้ามี): ไม่มี (เป็นกระบวนการทำงานของ AI ไม่ใช่บั๊กของแอป)

### [2026-08-15] Task ZOKY-004 (rating Row overflow, พบระหว่าง SELLER-004 QA รอบ 2)
- ข้อผิดพลาด: `StoreScreen`'s `_buildHeader` rating `Row` (`StarRatingDisplay` + `Text('rating · ผู้ติดตาม · productCount สินค้า')`) ไม่มี `Expanded`/`Flexible` ครอบ `Text` เลย ตั้งแต่ ZOKY-004 (commit `135af7a`) — บนจอมือถือจริงทุกขนาด overflow 165–322px ทุกครั้งที่ร้านมีรีวิว ≥1 ทั้งที่ `Row` เดียวกันในไฟล์นี้ (`_buildStoreInfoSection`'s address/business-hours) ใช้ `Expanded` ถูกต้องอยู่แล้วเป็นตัวอย่างในบรรทัดถัดมา
- ผลกระทบ: กระทบทุกร้านที่มีรีวิว ≥1 บนทุกขนาดจอมือถือ — release build จะ clip ข้อความเงียบ ๆ ซ่อนบางส่วนของ "X ผู้ติดตาม · Y สินค้า" จากลูกค้า ไม่ block SELLER-004 (Row คนละตัวกับที่ SELLER-004 แก้) แต่กระทบหน้าเดียวกันที่ลูกค้าเห็นอยู่ตอนนี้
- วิธีป้องกันในอนาคต: `Row` ใหม่ที่ผสม fixed-size widget กับ `Text` ที่ประกอบจากข้อมูล runtime (ตัวเลขนับไม่จำกัดหลัก) ต้องครอบ `Text` ด้วย `Expanded`/`Flexible` เป็นค่าเริ่มต้นเสมอ — QA ต้องตั้งฟิลด์ตัวเลข/นับจำนวนให้ไม่เป็นศูนย์ในทุก device-matrix test ของ Row ลักษณะนี้ (ไม่ใช่ปล่อย default 0/null ตลอดแบบที่เกิดใน `_viewportCases` เดิมของ SELLER-004 BUG-1 ซึ่งไม่มีเคสไหนตั้ง `storeRating` เลย)
- Regression test ที่เพิ่ม: `app/test/store_screen_test.dart` — เพิ่ม `_ratedViewportCases` (360x640, 375x667, 390x844@1.3, 430x932@1.3) + `ratedViewportRepos` (storeRating: (4.5, 1)) และ test loop ใหม่ 4 เคส พิสูจน์ red (overflow 220–322px) ก่อนแก้ / green (ไม่ overflow, ข้อมูล rating ยังแสดงครบ) หลังแก้ — ดู `.wyn/tasks/bugs/ZOKY-004-store-header-rating-row-overflow.md` หัวข้อ "Debug Output"

### [2026-08-13] Task WYN-002
- ข้อผิดพลาด: (1) `AuthGate` ฟังแค่ Supabase auth-state event แต่หน้า Welcome/AuthMethod/Phone/OTP ถูก push ทับไว้ด้านบนโดยไม่มีจุด pop กลับ ทำให้ผู้ใช้ค้างหน้าเดิมหลัง sign-in สำเร็จทุกเส้นทาง (Google/Apple/Phone) ไม่ใช่แค่ตอนตั้ง username ตามที่ QA รายงานไว้ในตอนแรก (2) `setUsername()` ไม่ catch unique-constraint violation จริงจาก race condition (3) OTP input ใช้ช่องเดียวแทนที่จะเป็น 6 ช่องแยกตาม design spec
- ผลกระทบ: ผู้ใช้ทุกคนที่ sign-in สำเร็จ (ไม่ว่าวิธีไหน) จะไม่เห็นหน้าจอเปลี่ยนอัตโนมัติ ต้อง force-restart แอป — เป็น blocker ที่ทำให้ onboarding ใช้งานไม่ได้เลยถ้าไม่แก้
- วิธีป้องกันในอนาคต: เมื่อออกแบบ flow ที่มี auth-state gate ผสมกับหลายหน้า push ต่อกัน ให้ระบุจุด pop-back ชัดเจนตั้งแต่ตอน Design/Coding ไม่ใช่รอ QA มาเจอ และ AI Coding ควร inject dependency (เช่น `AuthRepository`) แบบ testable มากกว่านี้ เพื่อให้เขียน regression test ครอบคลุม navigation behavior ได้ (ตอนนี้ `AuthGate` สร้าง `AuthRepository` เองภายใน ทำให้ยังเขียน widget test สำหรับ pop-back behavior ไม่ได้)
- Regression test ที่เพิ่ม: `app/test/otp_box_input_test.dart` (4 เคส ครอบคลุมปัญหา #3 เต็มรูปแบบ) — ปัญหา #1 (pop-back) และ #2 (race condition) ยังไม่มี automated regression test เพราะสถาปัตยกรรมปัจจุบันไม่รองรับการ inject fake backend ได้ง่าย (บันทึกเป็นข้อเสนอปรับปรุงใน `.wyn/learning/IMPROVEMENTS.md`)

### [2026-08-13] Task WYN-004
- ข้อผิดพลาด: `FeedScreen._toggleLike(Post post)` รับ `post` เป็น parameter ที่ถูก capture ไว้ตอน build ล่าสุด แล้วใช้ค่านั้นตัดสินใจ insert/delete ใน `likes` table — ไม่ได้อ่าน `_posts[index]` สดใหม่ในตัว method เอง (ต่างจาก `PostDetailScreen._toggleLike()` ที่เขียนถูกต้องอยู่แล้วในไฟล์ชุดเดียวกัน) ผลคือกดปุ่ม Like ซ้ำเร็ว ๆ ก่อนหน้าจอ rebuild ทำให้ทั้งสอง network call ใช้ค่าเดิมผิด ๆ ซ้ำกัน เสี่ยง duplicate insert ที่ violate primary key แล้ว rollback UI กลับไปสถานะผิด แบบเงียบ ๆ (ไม่แสดง error) — จุดเดียวกันนี้เกิดซ้ำในรูปแบบอื่นที่ `CreatePostScreen._post()` ซึ่งไม่มี `if (_isPosting) return;` guard เลย เปิดช่องให้กดปุ่ม "โพสต์" ซ้ำสร้างโพสต์ซ้ำได้
- ผลกระทบ: กระทบ Acceptance Criteria หลักของ WYN-004 โดยตรง ("กดไลก์แล้วเห็นจำนวนเพิ่มทันที กดซ้ำเพื่อเลิกไลก์ได้") — เป็นบั๊กที่เกิดได้จริงบนมือถือ (ผู้ใช้ใจร้อน/เครื่อง lag) ไม่ใช่แค่ทฤษฎี
- วิธีป้องกันในอนาคต: เมื่อ widget เดียวกันมี async button handler มากกว่า 1 จุดที่ทำงานคล้ายกัน (เช่น toggle like ทั้งใน Feed และ Post Detail) ให้ AI Coding ใช้ pattern เดียวกันทุกจุดตั้งแต่แรก (อ่าน mutable state สดใหม่ในตัว method เสมอ ไม่รับเป็น parameter/closure ที่ capture ไว้ตอน build) และทุกปุ่มที่เรียก async operation ที่ไม่ควรถูกเรียกซ้อนกัน ต้องมี explicit guard (`if (_isXxx) return;`) เป็นบรรทัดแรกของ handler เสมอ ไม่ใช่พึ่งแค่ disabled state ของปุ่มซึ่ง lag ไปหนึ่ง frame เสมอ
- Regression test ที่เพิ่ม: `app/test/feed_screen_test.dart`, `app/test/create_post_screen_test.dart` — พิสูจน์ทั้งสองจุดได้จริงแบบ dynamic โดยเรียก widget's `onPressed` callback สองครั้งติดกันแบบ synchronous (แม่นยำกว่า `tester.tap()` สองครั้งซึ่งเป็น async และปล่อยให้ call แรกทำงานจบก่อนได้) ใช้เทคนิคใหม่ 2 อย่างที่ทำให้ testable: (1) `RecordingPostRepository` — subclass ของ `PostRepository` ที่ override method แทนการเพิ่ม interface ใหม่ (2) `fake_supabase_session.dart` — ปลอม signed-in session แบบ local-only (ไม่ยิง network จริง) ให้ widget ที่พึ่ง `Supabase.instance.client.auth.currentUser` ตรง ๆ (เช่น `FeedScreen`) pump ได้ในการทดสอบ — บันทึกเป็น pattern ใหม่ใน `.wyn/learning/PATTERNS.md`

### [2026-08-14] Task WYN-005
- ข้อผิดพลาด: ทั้ง Product spec และ Design spec ของ WYN-005 ระบุ "Like Comment" ไว้ตรงกันชัดเจน (Product's Requirements: "Like Comment ได้"; Design's Screen 3 Components: "ปุ่ม Like เล็ก ๆ ข้างคอมเมนต์") แต่ AI Coding implement ข้ามฟีเจอร์นี้ไปทั้งระบบ — ไม่มีทั้งตาราง DB, repository method, และปุ่ม UI เลยแม้แต่จุดเดียว โดยไม่ได้บันทึกไว้เป็น known/deferred scope เหมือนจุดอื่นที่ตัดออกอย่างตั้งใจ (เช่น hashtag click-through ที่ Founder ยืนยันแล้วว่าทำทีหลัง) — แสดงว่าเป็นการมองข้ามจริง ไม่ใช่การตัดขอบเขต
- ผลกระทบ: กระทบ requirement ที่ระบุไว้ตรงกันในสองเอกสาร (Product + Design) — ถ้า QA ไม่ได้เทียบ requirement/component list ทีละบรรทัดกับโค้ดจริง จะไม่มีทางจับได้เลยเพราะ flow หลัก (โพสต์, ไลก์ Drop, คอมเมนต์, Save) ทำงานถูกต้องปกติทุกอย่าง มีแค่ปุ่มเดียวที่หายไปเงียบ ๆ
- วิธีป้องกันในอนาคต: AI Coding ควรไล่ checklist ทุกบรรทัดของทั้ง Product Requirements และ Design Components ก่อนส่งงาน (ไม่ใช่แค่ implement ตามความเข้าใจคร่าว ๆ ของ flow หลัก) และ AI QA & Security ควรเทียบ requirement/component list ทีละบรรทัดกับโค้ดจริงเสมอเป็นขั้นตอนบังคับ ไม่ใช่แค่ทดสอบ flow หลักแล้วผ่าน
- Regression test ที่เพิ่ม: `app/test/drop_comment_like_test.dart` (double-tap safety ของปุ่ม Like Comment ใหม่ พิสูจน์แล้วว่า fail จริงก่อนแก้/pass หลังแก้), `app/test/drop_comment_test.dart` (ขยายให้ครอบคลุม `toggledLike`/`likeCount` field ใหม่) — ระหว่างเขียน regression test เจอ gotcha ของ Flutter testing เพิ่มเติม (ไม่ใช่บั๊กจริงในโค้ด) บันทึกไว้ที่ `.wyn/learning/PATTERNS.md`: `find.byType()`/`find.text()` มองไม่เห็น widget ที่อยู่นอก viewport ใน `ListView`/`Sliver` ต้อง `scrollUntilVisible` ก่อนเสมอ

### [2026-08-14] Task WYN-005 (QA รอบ 2)
- ข้อผิดพลาด: หลัง Debug Engineer แก้บั๊ก "Like Comment" หายจากรอบ 1 แล้ว QA รอบ 2 ไล่ตรวจ Acceptance Criteria ทุกบรรทัดใหม่ทั้งหมด (ไม่ใช่แค่จุดที่เพิ่งแก้) พบว่า **"ลบ Comment ของตัวเองได้" ก็หายไปทั้งระบบเช่นกัน** — Product spec ระบุไว้ตรง ๆ ทั้งใน Requirements ("ลบ Comment ของตัวเองได้") และ Acceptance Criteria ("ลบคอมเมนต์ของตัวเองได้") แต่ `DropRepository` ไม่มี `deleteComment` เลย และ `DropDetailScreen` ไม่มีปุ่มลบหรือแม้แต่การเทียบ `comment.authorId == currentUserId` เลยสักจุด (ต่างจาก `isOwnDrop` ที่เทียบไว้ถูกต้องสำหรับตัว Drop เอง) — DB level มี RLS delete policy รองรับไว้แล้วตั้งแต่รอบ Coding แรก แต่ไม่มี client path ไปถึงมันเลย เหมือนกับกรณี Like Comment ในรอบ 1 เป๊ะ ตรวจสอบแล้วว่าไม่ใช่ gap ที่สืบทอดจาก WYN-004 (WYN-004 มี requirement แค่ "ลบโพสต์" ไม่เคยมี "ลบ comment") และ Design spec เองก็ไม่ได้ระบุปุ่มลบคอมเมนต์ไว้ใน Component list เช่นกัน (Design พลาดจุดนี้ตั้งแต่ต้น ไม่ใช่แค่ Coding)
- ผลกระทบ: กระทบ Acceptance Criteria ที่ระบุไว้ตรง ๆ ใน Product spec — Comment มี 3 ความสามารถ (เพิ่ม/ลบ/Like) แต่ทำงานจริงแค่ 2/3 แม้จะผ่าน Debug รอบ 1 มาแล้ว
- วิธีป้องกันในอนาคต: QA ต้องไล่ **ทุกหัวข้อ** ของ spec แยกกัน (Requirements, Design Component list, **และ** Acceptance Criteria) ทีละบรรทัดเสมอ ไม่ใช่แค่หัวข้อเดียว เพราะแต่ละหัวข้ออาจระบุรายละเอียดที่หัวข้ออื่นไม่ได้พูดถึง และ QA รอบถัดไปของ task ที่เคย FAIL ต้องไล่ checklist แบบครบทุกหัวข้อใหม่ทั้งหมดเสมอ ไม่ใช่แค่ตรวจจุดที่ Debug เพิ่งแก้เท่านั้น — Design ก็ควรไล่ checklist ทุกบรรทัดของ Product Requirements ก่อนออกแบบ Component list เช่นกัน ไม่ใช่แค่ Coding
- Regression test ที่เพิ่ม: `app/test/drop_comment_delete_test.dart` (ปุ่มลบแสดงเฉพาะเจ้าของคอมเมนต์เท่านั้น, ลบแล้วหายจาก list จริง + commentCount ลดลงจริง — พิสูจน์แล้วว่า fail จริงก่อนแก้/pass หลังแก้), `app/test/drop_test.dart` (เพิ่ม `withRemovedComment` unit test) — แก้แล้วในรอบ Debug ที่ 2 (ดู `.wyn/tasks/bugs/WYN-005-drop-post-image.md` หัวข้อ "Debug Engineer Report — รอบ 2")

### [2026-08-14] Task WYN-007 (QA รอบ 1)
- ข้อผิดพลาด: Design spec (`wyn-007-home.md`) ระบุ Component list ของการ์ด Home ไว้ตรงกันทั้งการ์ด Drop และการ์ด Pop ว่าแถวปฏิสัมพันธ์ต้องมี "Like/Comment/Share/Save" ครบ 4 อย่าง แต่ AI Coding ส่งมอบ `HomeDropCard`/`HomePopCard` โดยไม่มีปุ่ม Share เลยแม้แต่จุดเดียว และไอคอน Comment เป็น `const Icon(...)` เฉย ๆ ไม่ได้ห่อด้วย widget ที่กดได้ (ไม่มี `onTap`/`onPressed`) — ไม่ได้บันทึกไว้เป็น known/deferred scope cut เหมือนจุด Search bar placeholder ที่ Coding Output บันทึกไว้ชัดเจนว่าตั้งใจตัด แสดงว่าเป็นการมองข้ามจริง ไม่ใช่การตัดขอบเขตอย่างมีเหตุผล
- ผลกระทบ: กระทบ Design Component list โดยตรงทั้งสองการ์ด — ผู้ใช้ยังกด Share/Comment ได้จากหน้ารายละเอียด (`DropDetailScreen`/`PopSingleClipScreen`) ที่เปิดจากการแตะการ์ด จึงไม่ใช่ capability หายทั้งหมดแบบ WYN-005 แต่ก็ไม่ตรงตามที่ Design ตั้งใจให้เป็น quick action ระดับการ์ด — **นี่คือครั้งที่ 3 ในโปรเจกต์นี้** ที่เกิด pattern เดียวกัน: spec ระบุ component ไว้ชัดเจน แต่ Coding ข้ามไปเงียบ ๆ โดยไม่บันทึกเหตุผล (ครั้งที่ 1: WYN-005 รอบ 1 — Like Comment หาย; ครั้งที่ 2: WYN-005 รอบ 2 — Delete Comment หาย; ครั้งที่ 3: WYN-007 รอบ 1 — Share/Comment tap หายจากการ์ด Home)
- วิธีป้องกันในอนาคต: เมื่อ Coding สร้าง widget/การ์ดใหม่ที่อ้างอิง Component list จาก Design spec ต้องไล่ checklist รายการ interaction ทุกอย่างในหัวข้อนั้นทีละบรรทัดก่อนส่งงานเสมอ (ไม่ใช่แค่ทำตามความเข้าใจคร่าว ๆ ว่า "มีปุ่ม Like/Save ก็พอ") และถ้าตัดสินใจไม่ทำจุดใดจุดหนึ่งในรอบนี้ ต้องบันทึกเป็น known/deferred item ใน Coding Output เสมอ ไม่ใช่ปล่อยให้ QA ต้องมาจับเอง — เนื่องจากนี่เป็นครั้งที่ 3 ของ bug class เดียวกัน ควรพิจารณาเพิ่มขั้นตอน self-checklist บังคับก่อนส่งงานทุกครั้งที่มีการอ้างอิง Design Component list (บันทึกเป็นข้อเสนอใน `.wyn/learning/IMPROVEMENTS.md`)
- Regression test ที่เพิ่ม: แก้แล้วในรอบ Debug ที่ 1 — เพิ่ม Share `IconButton`/ทำ Comment icon เป็น `IconButton` ในทั้ง `home_drop_card.dart`/`home_pop_card.dart` ขยาย `app/test/home_feed_screen_test.dart` ด้วย 2 เทสต์ใหม่ (Comment icon เปิด `DropDetailScreen`/`PopSingleClipScreen` ได้จริงบนทั้งสองการ์ด) และเพิ่ม assertion ยืนยันปุ่ม Share ปรากฏบนทั้งสองการ์ดในเทสต์ "renders a mix" เดิม — พิสูจน์แล้วว่าทั้ง 3 จุดที่แก้ fail จริงก่อนแก้ (revert กลับไปโค้ดเดิมชั่วคราวแล้วรัน `flutter test test/home_feed_screen_test.dart` พบ FAIL 3 เทสต์) และ pass ทั้ง 83 เทสต์หลังแก้ (ดู `.wyn/tasks/bugs/WYN-007-home-feed.md` หัวข้อ "Debug Engineer Report — รอบ 1")

### [2026-08-15] Task ZOKY-004 (QA รอบ 1) — ครั้งแรกที่พบช่องโหว่ security ระดับ Critical ในโปรเจกต์นี้
- ข้อผิดพลาด: `reviews` table's `insert` RLS policy ตรวจ delivered-order-ownership gate ถูกต้องครบถ้วนผ่าน `exists` subquery (join `order_items`→`orders` ตรวจ `buyer_id`/`status = 'delivered'`/`product_id` ตรงกัน) แต่ `update` policy เขียนแค่ `using (auth.uid() = user_id)` โดยไม่มี `with check` — Coding/Design เข้าใจผิดว่า "order status ไม่มีทาง regress กลับจาก delivered ได้ จึงไม่ต้องตรวจซ้ำตอนแก้ไข" แต่ลืมพิจารณาว่า `order_item_id`/`product_id` **ของแถว reviews เอง** เปลี่ยนแปลงได้อิสระผ่าน update โดยไม่มี gate ป้องกันเลย เป็นคนละเรื่องกับ order status regression — Postgres RLS เมื่อไม่มี `with check` สำหรับ `update` จะ fallback ใช้ `using` เดิมเป็นตัวตรวจแถวใหม่ด้วย ซึ่งในที่นี้แคบกว่า insert's gate มาก
- ผลกระทบ: ผู้ใช้แก้ไขรีวิวของตัวเอง (ที่เขียนถูกต้องมาแล้วครั้งหนึ่ง) ให้ retarget `product_id`/`order_item_id` ไปยังสินค้าใดก็ได้ในระบบ รวมถึงสินค้าที่ไม่เคยซื้อเลย — ขัดกับ core security guarantee ของทั้งฟีเจอร์ ("ป้องกัน fake review จากคนที่ไม่เคยซื้อจริง") ตรง ๆ เป็นช่องโหว่ระดับ Critical/blocking ครั้งแรกที่พบในโปรเจกต์นี้ (ต่างจาก Minor/Major ที่เจอมาก่อนหน้าทั้งหมดซึ่งเป็นเรื่อง UI/UX ไม่ใช่ security)
- วิธีป้องกันในอนาคต: ทุกครั้งที่ตารางใหม่มี `insert` policy ที่ซับซ้อน (มี multi-table `exists`/join gate) ต้องตรวจ `update` policy คู่กันเสมอว่ามี `with check` ที่ตรวจ gate เดียวกันหรือไม่ — เขียนไว้ชัดเจนใน `.wyn/learning/LESSONS_LEARNED.md` แล้วว่า "update/delete policy ไม่มี with check ไม่ได้แปลว่าปลอดภัยเท่ากับ insert เสมอไป"
- Regression test ที่เพิ่ม: ไม่มี Flutter automated test ที่ทดสอบ live RLS ได้ (ไม่มี Supabase project จริง deploy อยู่) — แก้ด้วยการเพิ่ม `with check` mirror ของ insert policy เข้าไปใน `update` policy โดยตรง (ดู `.wyn/tasks/bugs/ZOKY-004-review-update-rls-gap.md` หัวข้อ "Debug Output") ยืนยันด้วยการอ่าน Postgres RLS semantics และไล่ query path ของ `ZokyRepository.editReview` ว่าไม่มี behavior ใดที่ app ใช้งานจริงจะถูก policy ที่เข้มขึ้นนี้ปฏิเสธ

### [2026-08-15] Task SCHEMA-001 — `schema.sql` พังทั้งไฟล์เมื่อรันกับ Supabase project ว่างเปล่าจริงเป็นครั้งแรก
- ข้อผิดพลาด: เมื่อ WYN-015 เพิ่ม notification type ของ Club (`club_join_request`/`club_join_approved`/`club_post_like`/`club_post_comment`) AI Coding แก้ `club_id`/`club_post_id` เข้าไปใน `create table public.notifications` เดิม (ตำแหน่งเดิมของ WYN-012 ที่อยู่บรรทัด ~589 ต้นไฟล์) แทนที่จะเพิ่มเป็น `alter table ... add column` แยกต่างหากวางไว้หลัง `public.clubs`/`public.club_posts` ถูกสร้าง (บรรทัด 774/1177) — ผลคือ `notifications` อ้างอิงตารางที่ยังไม่ถูกสร้างเลยในไฟล์ ณ จุดนั้น เป็น forward reference ที่ทำให้ Postgres/Supabase SQL Editor รัน sequential top-to-bottom แล้วพังกลางทางด้วย `relation "public.clubs" does not exist`
- ผลกระทบ: ทุก statement หลังจากจุดพัง (RLS/RPC/ตารางของ Club, ZOKY Marketplace ทั้ง 4 task, SELLER ทั้ง 4 task — รวมหลายร้อย statement) ไม่เคยถูกรันเลยเมื่อ paste ไฟล์ทั้งก้อนใส่ Supabase SQL Editor ของ project ใหม่ ทำให้ deploy จริงครั้งแรกเป็นไปไม่ได้เลยจนกว่าจะแก้ — เป็นบั๊กที่ "ซ่อน" อยู่ตั้งแต่ WYN-015 merge (หลายสิบ PR ก่อนหน้าที่ merge ทับไปแล้ว) เพราะไม่เคยมีการรันจริงกับ DB ว่างเปล่ามาก่อนเลยตลอดทั้งโปรเจกต์ — ทุก QA รอบก่อนหน้าตรวจแค่ SQL semantics (RLS/RPC ถูกต้องไหม) ไม่เคยตรวจ statement ordering/forward-reference เพราะไม่มี live project ให้ทดสอบ
- วิธีป้องกันในอนาคต: ทุกครั้งที่แก้ `supabase/schema.sql` โดยเฉพาะการเพิ่มคอลัมน์ให้ตารางเก่าที่ต้องอ้างอิงตารางใหม่ (feature ใหม่ต่อยอด table เดิม เช่น notification type ใหม่, tag/category ใหม่) ต้อง **เพิ่มคอลัมน์ผ่าน `alter table ... add column if not exists` แยกต่างหาก** วางตำแหน่งหลังตารางที่ถูกอ้างอิงถูกสร้างแล้วเท่านั้น ห้ามแก้ไข `create table` เดิมของตารางเก่าให้อ้างอิงตารางที่ยังไม่มีเลย — และต้องรัน `supabase/check_schema_ordering.py` (สคริปต์ regression test ใหม่ที่เพิ่มใน PR นี้) ทุกครั้งก่อนส่ง PR ที่แตะ `schema.sql`
- Regression test ที่เพิ่ม: `supabase/check_schema_ordering.py` — สคริปต์ Python มาตรฐาน (ไม่ต้องมี live DB) ไล่เช็คทุก `create table`/`alter table ... references public.X` ในไฟล์ทั้งหมดว่า `public.X` ถูกสร้างไปแล้วก่อนบรรทัดนั้นจริงหรือไม่ ยืนยันแล้วว่า FAIL (เจอ 2 จุด: `club_id`→`clubs`, `club_post_id`→`club_posts`) ก่อนแก้ และ PASS (0 จุด) หลังแก้ — ไล่ตรวจทั้งไฟล์ (`create table` ทุกตัว 26 ตาราง) เพิ่มเติมเชิงระบบแล้วไม่พบ forward-reference จุดอื่นอีก มีแค่จุดเดียวนี้ในทั้งไฟล์

### [2026-08-22] Task WYN-024 / DS-009 (QA รอบ 2→3) — แก้บั๊กแค่ segment ที่ถูก report มา ไม่ได้ไล่ตรวจทั้ง 4 segment เหมือนกัน
- ข้อผิดพลาด: QA รอบ 2 พบว่า label "จาก Club ของคุณ" (segment ที่ยาวที่สุด) ถูกบีบจนอ่านไม่ออกเมื่อ active และ Debug Engineer แก้ด้วยการเพิ่ม `maxLines: 1` + ellipsis ให้ label **ทั้ง 4 segment** (ถูกต้องแล้วในแง่ป้องกันการ wrap) แต่ทั้ง Debug และ QA รอบ 2 ไม่ได้กลับไปวัดขนาด render จริงของอีก 3 segment ที่เหลือหลังแก้ — สมมติไปเองว่าเป็นปัญหาเฉพาะ label ที่ยาวที่สุดเท่านั้น ("จาก Club ของคุณ" 15 ตัวอักษร) ทั้งที่ SegmentedButton แบ่งความกว้างเท่ากันทั้ง 4 ช่องอยู่แล้ว (ยืนยันจาก BoxConstraints trace ของ QA รอบ 1) บวกกับ checkmark icon ที่กิน fixed width เมื่อ segment ไหนก็ตาม active — ทำให้ทุก segment ที่ active ถูกบีบเท่ากันหมด (~30px) ไม่ว่า label จะสั้นหรือยาว รวมถึง "สำหรับคุณ" ซึ่งเป็น **default segment ที่ active ตั้งแต่เปิดแอปครั้งแรกโดยไม่ต้องแตะอะไรเลย**
- ผลกระทบ: QA รอบ 3 วัดด้วย `RenderParagraph` + `TextPainter` (unconstrained) เทียบ estimate ตัวอักษรที่มองเห็นได้จริงของทั้ง 4 segment พบว่า "สำหรับคุณ" (9 ตัวอักษร) เหลือมองเห็นแค่ ~1 ตัวอักษร, "ติดตาม"/"ล่าสุด" (6 ตัวอักษร) เหลือ ~2 ตัวอักษร, "จาก Club ของคุณ" (15 ตัวอักษร) เหลือ ~2-3 ตัวอักษร — ทุก segment พังเท่ากันเมื่อ active ไม่ใช่แค่ตัวที่ยาวที่สุด และที่ร้ายแรงกว่าคือกระทบ **default state ของ Home ทันทีที่เปิดแอป** ซึ่งกว้างกว่าที่ทั้ง Debug report และ QA รอบ 2 ประเมินไว้มาก (ประเมินไว้แค่ "residual, ไม่ block")
- วิธีป้องกันในอนาคต: เมื่อบั๊กเกิดจาก layout constraint ที่ใช้ร่วมกันระหว่างหลาย element (เช่น segment ทั้ง 4 ใน control เดียวกัน แบ่ง width เท่ากัน) ห้ามสรุป root cause ว่าเป็นปัญหาเฉพาะจุดที่ถูก report มาก่อนไล่ตรวจ element อื่นที่ใช้ constraint เดียวกันทุกตัวเสมอ — โดยเฉพาะเมื่อ fix ที่ใช้ (`maxLines`/ellipsis) เป็น fix แบบเดียวกันที่ apply ให้ทุก element พร้อมกันอยู่แล้ว นั่นคือสัญญาณว่าควรวัดผลทุกจุดที่ apply ไปด้วย ไม่ใช่แค่จุดเดิมที่ถูก report
- Regression test ที่เพิ่ม: ยังไม่ได้เพิ่ม (รอ fix รอบถัดไปจาก Debug Engineer ก่อน — ดู `.wyn/tasks/bugs/WYN-024-segmented-button-active-label-illegible-all-segments.md`)

### [2026-08-22] WYN-027 (Block system) — สร้าง SECURITY DEFINER helper function ใน `public` โดยไม่ grant/revoke ชัดเจน เข้าใจผิดว่า "ไม่ grant = ปิด"
- ข้อผิดพลาด: ตอน implement `is_blocked_either_way(a uuid, b uuid)` (และ 4 sibling author-lookup function) ผู้เขียนโค้ด (self-QA เดียวกับ Coding session) ตั้งใจให้เป็น internal-only helper สำหรับ RLS policy เท่านั้น แต่ไม่ได้เขียน `grant`/`revoke` ใด ๆ กำกับไว้เลย ต่างจาก `block_relationship`/`block_user`/`unblock_user` ที่ตั้งใจเปิดเป็น client RPC และมี `grant execute ... to authenticated;` ชัดเจนทุกตัว — สมมติไปเอง (ผิด) ว่าการไม่เขียน grant statement ใด ๆ จะทำให้ function "ไม่เปิด" โดย default ทั้งที่ Postgres grant `EXECUTE` ให้ `PUBLIC` เป็นค่าเริ่มต้นเสมอตอนสร้าง function ใหม่ และ PostgREST auto-expose ทุก function ใน `public` เป็น RPC endpoint โดยดูแค่ ACL ไม่ใช่ "มี grant statement บอกไว้หรือเปล่า"
- ผลกระทบ: `is_blocked_either_way` รับ 2 พารามิเตอร์เป็น user id ใครก็ได้ (ต่างจาก `block_relationship` ที่ resolve จาก `auth.uid()` เสมอ) ทำให้ authenticated user คนไหนก็เรียก `POST /rest/v1/rpc/is_blocked_either_way` ถามความสัมพันธ์ block ของผู้ใช้อีกสองคนที่ไม่เกี่ยวกับตัวเองได้เลย ทั้งที่ตาราง `blocks` มี RLS `select` policy จำกัดไว้แน่นหนาว่า `auth.uid() = blocker_id` — bypass การป้องกันความเป็นส่วนตัวที่ table เดียวกันตั้งใจสร้างไว้โดยสมบูรณ์ QA อิสระรอบ 1 (session แยกจาก Coding) เป็นคนจับได้ ไม่ใช่ self-QA รอบแรก
- วิธีป้องกันในอนาคต: ทุกครั้งที่สร้าง `security definer` function ใหม่ที่ตั้งใจให้เป็น internal-only (ไม่ใช่ client RPC) ต้องสร้างใน schema ที่ไม่อยู่ใน PostgREST exposed-schema list ตั้งแต่ต้น (โปรเจกต์นี้ตอนนี้มี schema `internal` แล้วจาก WYN-027 fix — ใช้ schema นี้ต่อไปสำหรับ helper function แบบเดียวกันในอนาคต) ห้ามสร้างใน `public` แล้วหวังว่า "ไม่ grant = ปิด" เพราะไม่จริง — AI Coding/AI QA & Security ต้องเช็คทุก `security definer` function ใหม่ด้วยคำถาม 2 ข้อเสมอ: (1) ฟังก์ชันนี้ตั้งใจเป็น client RPC หรือ internal-only? (2) ถ้า internal-only อยู่ใน schema ที่ PostgREST expose ไหม (ถ้าใช่ ต้องย้ายออก ไม่ใช่แค่ไม่ grant)

### [2026-08-22] WYN-030 (Appeal System) — SQL verification รอบแรกรันผ่าน Postgres superuser ซึ่ง bypass RLS เสมอ ผลลัพธ์ "PASS" ไม่มีความหมายด้าน security จริง
- ข้อผิดพลาด: ตอน implement เขียน ad hoc SQL script ยืนยัน `submit_appeal()`/`decide_appeal()` โดยเชื่อมต่อฐานข้อมูลผ่าน `sudo -u postgres psql` แล้วจำลอง `auth.uid()` ด้วย `current_setting('app.current_user_id')` เฉยๆ — สคริปต์รันผ่านทุกเคสรวมถึงเคสที่ตั้งใจพิสูจน์ RLS (เช่น "ผู้ใช้อื่นเห็น appeal ของคนอื่นไหม") แต่ไม่ได้ตระหนักว่า `postgres` เป็น role ที่มี `rolbypassrls = true` โดย default เสมอ — การ query ใดๆ ผ่าน role นี้จะข้าม RLS policy ทั้งหมดเสมอไม่ว่า policy จะเขียนถูกหรือผิด ทำให้เคสที่ "ดูเหมือนผ่าน" ไม่ได้พิสูจน์อะไรเกี่ยวกับ RLS เลยจริงๆ
- ผลกระทบ: หากไม่ตรวจพบเอง (ระหว่างทำ QA อิสระต่อทันทีในเซสชันเดียวกัน — เช็ค `pg_roles.rolbypassrls` แล้วพบว่า `postgres = true`) จะรายงาน SQL verification ว่า "PASS" ทั้งที่ RLS policy ของ `appeals`/`appeal-evidence` storage bucket อาจมีช่องโหว่จริงที่ไม่เคยถูกทดสอบเลย เป็นความเสี่ยงเดียวกับที่ WYN-027/WYN-029 เจอบั๊ก privacy leak จริงมาก่อน เพียงแต่ครั้งนี้ตัว verification เองเป็นจุดที่พลาด ไม่ใช่ตัวโค้ด — โชคดีที่แก้ทันก่อนส่ง QA PASS จริง เขียน `supabase/tests/wyn_030_appeal_system_test.sh` ใหม่ให้รันผ่าน role `authenticated` จริงตาม convention ของ `wyn_029_moderation_queue_test.sh` แล้วพบว่าโค้ดจริงผ่านทุกเคสอยู่ดี (ไม่มีบั๊กใหม่จากจุดนี้) แต่เป็นความเสี่ยงที่ควรป้องกันไว้ล่วงหน้า ไม่ใช่ต้องมาจับเองทุกครั้ง
- วิธีป้องกันในอนาคต: SQL verification/regression script ใดๆ ที่มีจุดประสงค์พิสูจน์ RLS (ต่างจากการพิสูจน์ตรรกะภายใน RPC/trigger ที่ `security definer` ครอบอยู่แล้วซึ่งไม่ขึ้นกับ RLS ของผู้เรียก) **ต้องรันผ่าน `set role authenticated` เสมอ ห้ามรันผ่าน superuser (`postgres`) แล้วสรุปว่าพิสูจน์ RLS แล้ว** — ก่อนเขียน SQL verification ใหม่ทุกครั้ง ให้ถามตัวเองก่อนว่า "เคสนี้ต้องพึ่ง RLS policy หรือแค่ logic ภายใน function?" ถ้าเป็นอย่างแรก ต้องใช้ role `authenticated` + จำลอง JWT claim ผ่าน `current_setting('request.jwt.claim.sub', true)` (ตรงกับที่ `auth.uid()`'s stub ใช้จริงในทุก `supabase/tests/*.sh`) ไม่ใช่ตัวแปร custom เช่น `app.current_user_id` ที่ใช้ได้แค่กับ stub เฉพาะกิจของตัวเอง

### [2026-08-23] WYN-039 (Private Account + Follow Request) — Requirement 3 ("Remove Follower") หายไปทั้งข้อ ตั้งแต่ Design จนถึง Coding แม้จะระบุไว้ชัดเจนทั้งใน Requirements และ Acceptance Criteria ของ Product spec
- ข้อผิดพลาด: Product spec (`WYN-039-private-account-follow-request.md`) เขียน Requirement 3 ไว้ตรงๆ เป็นหัวข้อแยกต่างหาก ("เจ้าของบัญชี... เอาผู้ติดตามคนใดคนหนึ่งออกจากรายชื่อ Followers ได้") พร้อม Acceptance Criteria ของตัวเอง และยังชี้เฉพาะเจาะจงด้วยว่า `follows`' DELETE policy เดิมของ WYN-008 ไม่รองรับ (`auth.uid() = follower_id` เท่านั้น ไม่มีทางให้ `following_id` ลบได้) — แต่ Design doc ที่เขียนต่อจากนั้นทันที **ไม่มี Screen ไหนพูดถึง Remove Follower เลยแม้แต่บรรทัดเดียว** (ครอบแค่ Requirement 1-2 กับ 4-6 เท่านั้น) และ Coding ก็ implement ตาม Design doc ตรงๆ จึงพลาดไปด้วยเป็นธรรมชาติ — เป็น pattern เดียวกับ WYN-005 รอบ 1/2 และ WYN-007 รอบ 1 (component/requirement ที่ระบุไว้ชัดเจนในเอกสารแต่ถูกข้ามไปเงียบๆ ระหว่างทาง) เพียงแต่ครั้งนี้จุดที่พลาดคือ **Design ไม่ยกมาจาก Product ทั้งที่ Product เขียนไว้ชัดกว่าปกติด้วยซ้ำ** (ระบุ schema-level detail ของปัญหาไว้ให้เสร็จแล้ว)
- ผลกระทบ: ผู้ใช้ไม่มีทางเอาผู้ติดตามที่ไม่ต้องการออกได้เลยนอกจาก Block (ซึ่งรุนแรงเกินไปสำหรับ intent จริง) — กระทบ Acceptance Criteria ข้อหนึ่งของ Product โดยตรง แต่ไม่ใช่ security/privacy leak (แยกจาก 2 จุดที่ Coding พบเองระหว่างเขียนโค้ดในรอบเดียวกัน คือ `follows` INSERT policy ไม่เคยเช็ค privacy ของ target และ `get_poll_results()` bypass RLS ของ `drops`) — จับได้ตอน QA อิสระไล่เทียบ Acceptance Criteria ทุกข้อของ Product กับโค้ดจริงทีละข้อ ไม่ใช่แค่เชื่อ Coding Output ที่อ้างว่า "ไล่ตรวจครบทุกข้อ" (ซึ่งไม่จริง)
- วิธีป้องกันในอนาคต: เมื่อ Product spec มี Requirement หลายข้อพร้อมหมายเลข (Requirement 1, 2, 3, ...) **AI Design ต้องไล่ตอบให้ครบทุกหมายเลขก่อนส่งต่อ Coding** — เขียน checklist ในหัว Design doc เองก็ได้ว่า Requirement ไหนถูก cover ที่ Screen ไหน เพื่อให้เห็นชัดถ้ามีข้อไหนตกหล่น (มิเรอร์ข้อเสนอเดิมจาก WYN-005/WYN-007 ที่ยังไม่เคยถูกนำไปทำเป็น checklist บังคับจริงจัง) — AI QA & Security ต้องไล่เทียบ **Product's Requirements ทุกข้อที่มีหมายเลข + ทุกบรรทัดของ Acceptance Criteria** กับโค้ดจริงเสมอ ไม่ใช่แค่เทียบกับ Coding Output's สรุปเอง (Coding Output สรุปผิดได้ เหมือนที่เกิดในรอบนี้)
- Regression test ที่เพิ่ม: SQL policy ใหม่ (`"Users can remove a follower from their own followers list"`, permissive เพิ่มเติมไม่แตะ policy เดิมของ WYN-008) + `FollowRepository.removeFollower()` + ปุ่ม "ลบ" ใหม่ใน `follow_list_screen.dart` (เฉพาะดู Followers list ของตัวเอง) — พิสูจน์ด้วย SQL CHECK19a-c ใหม่ใน `wyn_039_private_account_test.sh` (บุคคลที่สามลบ follower ของคนอื่นไม่ได้, เจ้าของบัญชีลบได้จริง, follower เดิม unfollow ตัวเองได้ปกติ) และ 6 เคสใหม่ใน `follow_list_screen_test.dart` (แสดงปุ่มเฉพาะ own-followers-list, confirm dialog, optimistic remove, revert-on-error) — `flutter test` 632/632 ผ่านหลังแก้ (626 ก่อนแก้ + 6 ใหม่), SQL 13/13 สคริปต์ผ่านหมด

### [2026-09-01] WYN-072: AI Coding เขียน widget test ที่ mount `AuthGate` ไปจนถึง `RootShell` จริงโดยไม่รู้ว่าจะโดน Realtime Timer leak

- ผิดพลาด: AI Coding (รอบ implement WYN-072) เพิ่ม test ใหม่ที่ `pumpWidget(AuthGate(...))` แล้ว `pumpAndSettle()` จนถึง `RootShell` จริง (all-default repositories) เพื่อพิสูจน์ guest-bypass logic — ไม่รู้ว่า test อื่นในไฟล์เดียวกัน (`auth_gate_test.dart`) มี comment เตือนไว้แล้วว่า "RootShell mounts every Bottom Nav tab at once (IndexedStack)...ซึ่งคาสเคดไปเป็น real Supabase call จากหลาย initState" และตั้งใจ**หลีกเลี่ยง**การ mount RootShell เต็มรูปแบบมาโดยตลอดด้วยเหตุผลนี้ (ใช้ `hasUsernameResult: false` ให้จบที่ `UsernameSetupScreen` แทน) — AI Coding เขียน test ใหม่โดยไม่อ่าน/ไม่จับ pattern นี้จากไฟล์เดิมที่ตัวเองกำลังแก้ ทำให้เขียน test ที่ตัวเองก็ไม่ได้รันจริง (ไม่มี Flutter SDK ในสภาพแวดล้อมตอนนั้น) ส่งต่อมาแบบไม่รู้ว่าพัง
- ผลกระทบ: QA รอบ 1 เจอ 1/877 test แดง (teardown-only, ไม่ใช่ production bug) ต้องเสียรอบ QA→Debug Engineer→QA เพิ่มหนึ่งรอบเต็ม เพื่อแก้ test ที่ไม่จำเป็นต้องพังตั้งแต่แรกถ้าอ่าน comment ในไฟล์เดียวกันก่อนเขียน
- **บทเรียนสำคัญ**: เวลาจะเขียน test ใหม่ในไฟล์ที่มี test อื่นอยู่แล้ว **ต้องอ่าน pattern/comment ของ test ข้างเคียงในไฟล์เดียวกันก่อนเสมอ** โดยเฉพาะ comment ที่อธิบายว่า "ทำไมถึงไม่ทำแบบ A" (เช่น "ทำไมไม่ mount RootShell เต็มๆ") เพราะมักบอกใบ้ข้อจำกัดของ test infrastructure ที่ test ใหม่ก็จะไปชนเหมือนกันถ้าไม่ระวัง — ไม่ใช่แค่ copy pattern การ setup/pumpWidget มาเฉยๆ โดยไม่อ่าน "เหตุผลที่ทำไม่ทำ" ที่ comment ข้างๆ อธิบายไว้แล้ว
- เมื่อไม่มี Flutter SDK ให้รันจริงตอนเขียนโค้ด (เหมือนสภาพแวดล้อมตอนนั้น) ความเสี่ยงแบบนี้สูงขึ้นมาก — AI Coding ควรระบุความเสี่ยงนี้ชัดเจนใน "Known Issues" เสมอเมื่อเขียน test ใหม่ที่ mount widget tree ลึก/ซับซ้อนโดยไม่ได้รันจริง (ไม่ใช่แค่ "ไม่ verify compile" แต่ "ไม่ verify ว่า test เองจะผ่านหรือเปล่าเลย")
