# Team Performance Metrics

> ห้ามสร้างข้อมูลเทียม (fabricate) — ค่าที่ยังไม่มีข้อมูลจริงให้ระบุ UNKNOWN

- Tasks completed: 18 (WYN-001 — Vision & Tech Stack, WYN-002 — Authentication & Onboarding ผ่าน QA รอบ 3, WYN-003 — User Profile ผ่าน QA รอบ 2, WYN-004 — Feed & Post ผ่าน QA รอบ 2, WYN-005 — Drop ผ่าน QA รอบ 3, WYN-006 — Pop ผ่าน QA รอบ 1, WYN-007 — Home ผ่าน QA รอบ 2, WYN-008 — Follow system ผ่าน QA รอบ 1, WYN-013 — Profile V2 ผ่าน QA รอบ 1, WYN-009 — Search ผ่าน QA รอบ 1, WYN-012 — Notification ผ่าน QA รอบ 1, WYN-014 — Club Core ผ่าน QA รอบ 1, WYN-015 — Club Discovery & Integration ผ่าน QA รอบ 1, ZOKY-001 — Marketplace Foundation ผ่าน QA รอบ 1, ZOKY-002 — Search & Filter ผ่าน QA รอบ 1, ZOKY-003 — Cart & Checkout & Order ผ่าน QA รอบ 1, ZOKY-004 — Review ผ่าน QA รอบ 2, WYN-010 — Share formalization ผ่าน QA รอบ 1)
- QA failures: 10 (WYN-002 รอบที่ 1, รอบที่ 2 — FAIL; WYN-003 รอบที่ 1 — FAIL; WYN-004 รอบที่ 1 — FAIL; WYN-005 รอบที่ 1, รอบที่ 2 — FAIL; WYN-007 รอบที่ 1 — FAIL; ZOKY-004 รอบที่ 1 — FAIL); QA passes: 17 (WYN-002 รอบที่ 3, WYN-003 รอบที่ 2, WYN-004 รอบที่ 2, WYN-005 รอบที่ 3, WYN-006 รอบที่ 1, WYN-007 รอบที่ 2, WYN-008 รอบที่ 1, WYN-013 รอบที่ 1, WYN-009 รอบที่ 1, WYN-012 รอบที่ 1, WYN-014 รอบที่ 1, WYN-015 รอบที่ 1, ZOKY-001 รอบที่ 1, ZOKY-002 รอบที่ 1, ZOKY-003 รอบที่ 1, ZOKY-004 รอบที่ 2, WYN-010 รอบที่ 1 — PASS)
- Bugs discovered: 15 (WYN-002: 1 Critical + 2 Medium จากรอบ 1, อีก 1 Critical เป็น regression จากรอบ 2 — แก้ครบแล้ว; WYN-003: 1 Critical จากรอบ 1 — แก้แล้ว; WYN-004: 1 Major (2 จุดเกิดจาก root cause เดียวกัน) จากรอบ 1 — แก้แล้ว ยืนยันด้วย regression test ที่พิสูจน์ว่าจับบั๊กได้จริง (fail ก่อนแก้, pass หลังแก้); WYN-005: 1 Major (ฟีเจอร์ "Like Comment" หายไปทั้งระบบ) จากรอบ 1 — แก้แล้ว, อีก 1 Major (ไม่มีทางลบ Comment ของตัวเองได้เลย) จากรอบ 2 — แก้แล้ว ทั้งสองยืนยันด้วย red→green regression test; อีก 1 Minor (ไม่แสดงเวลาที่โพสต์แบบ relative time ทั้ง Drop header และ comment) จากรอบ 3 — ไม่ block การอนุมัติ บันทึกเป็นรายการปรับปรุงรอบถัดไป; WYN-006: 1 double-tap Like bug ที่ Coding เจอและแก้เองระหว่างพัฒนาก่อนส่ง QA (ไม่นับเป็น QA failure เพราะไม่เคยไปถึง QA ในสภาพที่มีบั๊ก) + 2 Minor (Follow ปุ่มขาด Semantics label, view-count RPC ไม่มี rate-limit) จากรอบ 1 — ไม่ block การอนุมัติ; WYN-007: 1 Major (การ์ด Home ทั้ง Drop และ Pop ไม่มีปุ่ม Share เลย และไอคอน Comment กดไม่ได้ ทั้งที่ Design spec ระบุไว้ตรงกันทั้งสองการ์ด) จากรอบ 1 — แก้แล้วในรอบ Debug ที่ 1 ยืนยันด้วย red→green regression test (ทั้ง Debug และ QA รอบ 2 ทำซ้ำอิสระทั้งคู่) + 1 Minor (กด Comment บนการ์ด Pop เปิดหน้าคลิปเดี่ยวก่อนแทนที่จะเปิด comment sheet ตรง ๆ) จากรอบ 2 — ไม่ block การอนุมัติ บันทึกเป็น fast-follow item; WYN-008: 2 gap ที่ Coding เจอและแก้เองระหว่างพัฒนาก่อนส่ง QA (`IndexedStack` reload sync, testing gotcha ของ `pumpWidget`) ไม่นับเป็น QA failure + 1 Minor (schema.sql comment อ้างอิง path `approved/` ล่วงหน้า) จากรอบ 1 — ไม่ block การอนุมัติ; WYN-013: 1 Minor (จุด tap-to-profile บนการ์ด Home — จุดเสี่ยงชนกันมากที่สุดตาม Design spec — ไม่มี regression test ถาวรใน `home_feed_screen_test.dart` แม้ฟีเจอร์ทำงานถูกต้องจริง ยืนยันด้วย widget test ชั่วคราวที่ QA เขียนเองแล้วลบทิ้ง) จากรอบ 1 — ไม่ block การอนุมัติ เสนอเป็น fast-follow; WYN-009: Coding เจอและแก้ testing gap เอง 1 จุดก่อนส่ง QA — test debounce-cancel แรกที่เขียนไว้ไม่จับบั๊กได้จริง (pump เดียวยาว coalesce หลาย timer callback ให้เหลือ rebuild เดียว) เขียนใหม่แยก pump เป็นช่วง ๆ แล้วพิสูจน์ red→green ได้จริง ไม่นับเป็น QA failure เพราะไม่เคยไปถึง QA ในสภาพที่มีบั๊ก; WYN-012: 1 Minor (ไม่มี regression test ถาวรครอบคลุมวงจร badge-refresh-after-return เต็มรูปแบบ แม้ฟีเจอร์ทำงานถูกต้องจริง ยืนยันด้วย widget test ชั่วคราวที่ QA เขียนเองแล้วลบทิ้ง) จากรอบ 1 — ไม่ block การอนุมัติ เสนอเป็น fast-follow; WYN-014: Coding เจอและแก้เอง 2 จุดก่อนส่ง QA (layout overflow ของ Cover ใน `ClubPage`, path อัปโหลดรูปโพสต์หลายรูปที่ชน CHECK constraint) ไม่นับเป็น QA failure + 1 Minor (ผู้ใช้ที่ถูกแบน join ซ้ำเห็น error message ทั่วไปแทนข้อความเฉพาะ — ปลอดภัย ไม่ crash) จากรอบ 1 — ไม่ block การอนุมัติ เสนอเป็น fast-follow; WYN-015: Coding เจอและแก้เอง 1 จุดก่อนส่ง QA (RenderFlex overflow ใน `ViewProfileScreen`'s My Clubs section) ไม่นับเป็น QA failure + 1 Minor (`FromYourClubsFeed`'s empty state ขาดปุ่ม "สำรวจ Club" ตามที่ Design spec ระบุไว้ตรงๆ — เป็น pattern เดิมของ WYN-005/007 "spec ระบุ component ชัดเจน Coding มองข้าม" ที่กลับมาเป็นครั้งที่ 4 แต่ครั้งนี้ QA จับได้เองในรอบเดียวไม่ต้องรอ Founder หรือรอบถัดไป และไม่ block เพราะมีทางออกอื่นบนหน้าเดียวกันอยู่แล้ว) จากรอบ 1 — ไม่ block การอนุมัติ เสนอเป็น fast-follow; ZOKY-001: Coding เจอและแก้เอง 2 จุดก่อนส่ง QA (ZOKY tab แทรกผิดตำแหน่ง, `FutureBuilder<Store?>.hasData` bug จริงที่ทำให้ spinner หมุนไม่จบเมื่อร้านไม่พบ) ไม่นับเป็น QA failure + 1 Minor (`ProductDetailScreen`/`StoreScreen` ไม่แสดง "Rating" เลยทั้งที่ Product Requirements และ Design Components ระบุไว้ตรงๆ ทั้งคู่ — pattern เดิมกลับมาเป็นครั้งที่ 5 คราวนี้ไม่มีการบันทึกเหตุผลไว้เลยว่าทำไมตัดออก ต่างจาก "แนะนำสำหรับคุณ"/"ขายดี" ที่บันทึกเหตุผลไว้ชัดเจน) จากรอบ 1 — ไม่ block เพราะไม่มี AC ทดสอบตรงและไม่มี data source ให้ rating จริงรอบนี้ เสนอเป็น fast-follow; ZOKY-002: Coding เจอและแก้เอง 1 จุดก่อนส่ง QA (`RecordingZokyRepository` สร้าง inline ใน `testWidgets` callback แทนที่จะเป็น `late` field ใน `setUp()` — gotcha เดิมที่เคยเจอมาแล้วหลายครั้งก่อนหน้า) ไม่นับเป็น QA failure — **QA พบ 0 findings เป็นครั้งแรกของสาย ZOKY** (ต่างจาก ZOKY-001 ที่พบ Minor 1 จุด) หลังไล่ Requirements/Design Components/Acceptance Criteria ทีละบรรทัดครบทั้ง 3 หัวข้อแล้วไม่พบช่องว่างแม้แต่จุดเดียว รวมถึงยืนยัน ILIKE safety pattern ถูกต้องและ zero-file-touched ต่อ WYN-009's SearchScreen เดิมจริง; ZOKY-003: Coding เจอและแก้เอง 1 จุดก่อนส่ง QA — บั๊กจริง (ไม่ใช่แค่ testing gap) ที่ทำให้ `ZokyCartScreen` ทั้งหน้าว่างเปล่าทันทีที่ตะกร้ามีสินค้า: `Column` ใน `_buildBottomBar` ไม่มี `mainAxisSize: MainAxisSize.min` ทำให้ค่าเริ่มต้น `MainAxisSize.max` ภายใต้ loose height constraint ของ `Scaffold.bottomNavigationBar` ทำให้ `Scrollable` ของ body ได้ความสูง 0 แบบไม่มี exception ให้ตามรอยเลย (คนละแบบกับ RenderFlex overflow ที่คุ้นเคยจาก WYN-014/015) ไม่นับเป็น QA failure เพราะไม่เคยไปถึง QA ในสภาพที่มีบั๊ก บันทึกเป็น pattern ใหม่ใน `.wyn/learning/PATTERNS.md` — QA พบ Minor 1 จุดไม่ block: ไม่มี regression test end-to-end ครอบคลุมวงจร Cart badge refresh หลัง checkout สำเร็จ (pattern เดิมแบบเดียวกับที่เคยพบใน WYN-012/WYN-013's badge-refresh-after-return) — เป็น task แรกของสาย ZOKY ที่มี RPC ที่ต้อง lock แถวข้าม-ร้านค้าหลายร้านพร้อมกันในธุรกรรมเดียว (`create_orders` ล็อกสินค้าทุกร้านในตะกร้าด้วย `for update order by id` ก่อนเริ่ม loop) QA วิเคราะห์ concurrent scenario (2 request แย่งสินค้าชิ้นสุดท้ายพร้อมกัน) ยืนยันว่าไม่มีทาง stock ติดลบได้จริง; ZOKY-004: **1 Critical security bug จากรอบ 1 — ครั้งแรกในโปรเจกต์นี้ที่เป็นช่องโหว่ security ระดับ blocking ไม่ใช่แค่ UI/UX gap** — `reviews`' `update` RLS policy ไม่มี `with check` ตรวจ delivered-order-ownership gate ซ้ำ (มีแค่ `insert` policy ที่ตรวจ) ทำให้ผู้ใช้แก้ไขรีวิวของตัวเองให้ retarget ไปยังสินค้า/order_item ใดก็ได้ที่ไม่เคยซื้อจริง — แก้แล้วโดยเพิ่ม `with check` mirror `insert`'s `exists` gate เป๊ะ ไม่ต้องแก้ Dart เลย ยืนยันปิดช่องโหว่สมบูรณ์ใน QA รอบ 2
- Repeated bugs: 5 (WYN-002 รอบ 2 คือบั๊ก "ผู้ใช้ค้างหน้าเดิม/นำทางผิด" แบบเดิมที่กลับมาในรูปแบบใหม่ — เกิดจากการแก้บั๊ก Critical รอบ 1 เอง; รอบ 3 ยืนยันว่าแก้ถูกจุดจริงแล้ว; WYN-005 รอบ 2 คือบั๊กรูปแบบเดียวกับรอบ 1 — "capability ที่ Product spec ระบุไว้แต่ Coding มองข้ามเพราะไม่มี pattern ให้อ้างอิงจาก WYN-004" เกิดซ้ำ 2 ครั้งติดกันในงานเดียวกันคนละความสามารถ — Like Comment แล้วก็ Delete Comment; WYN-007 คือบั๊กรูปแบบเดียวกับ WYN-005 รอบ 1/2 อีกครั้ง — "Design Component ที่ระบุไว้ชัดเจนแต่ Coding มองข้าม ไม่บันทึกเป็น scope cut ที่ตั้งใจ" เกิดเป็นครั้งที่ 3 ในโปรเจกต์นี้แล้ว (Like Comment → Delete Comment → Share/Comment tap บนการ์ด Home) — บันทึกข้อเสนอ process improvement ใหม่ใน IMPROVEMENTS.md เพราะบันทึกบทเรียนอย่างเดียวสองครั้งก่อนหน้ายังป้องกันไม่ได้จริง; WYN-015 (Minor, ไม่ block) คือบั๊กรูปแบบเดียวกันอีกครั้งเป็นครั้งที่ 4 — `FromYourClubsFeed`'s empty state ขาดปุ่ม "สำรวจ Club" ที่ Design spec Screen 5 ระบุไว้ตรงๆ — ต่างจาก 3 ครั้งก่อนตรงที่ (ก) ระดับความรุนแรงลดลงเหลือ Minor เพราะมีทางออกอื่นในหน้าเดียวกัน ไม่ใช่ capability หายไปทั้งระบบแบบ WYN-005/007 (ข) QA จับได้เองในรอบ 1 ไม่ต้องมี FAIL/Debug รอบเพิ่ม — แสดงว่า process improvement ที่บันทึกไว้หลัง WYN-007 (ไล่ Design Components ทีละบรรทัดเทียบโค้ดจริงเสมอ) ช่วยให้ QA จับ pattern นี้ได้เร็วขึ้นแม้ Coding จะยังพลาดจุดเดิมซ้ำ — บทเรียนใหม่: การป้องกันที่ระดับ QA (catch ให้ไว) ใช้ได้ผลจริงแม้จะยังไม่ป้องกันที่ต้นทาง (Coding ยังพลาดอยู่) เสนอเพิ่ม pre-submission checklist ให้ Coding เทียบ Design spec ทีละ Component ก่อนส่งงานเป็นขั้นตอนบังคับ ไม่ใช่แค่พึ่ง QA จับปลายทางอย่างเดียว; ZOKY-001 (Minor, ไม่ block) คือบั๊กรูปแบบเดียวกันเป็นครั้งที่ 5 — `ProductDetailScreen`/`StoreScreen` ไม่แสดง "Rating" ทั้งที่ Product Requirements และ Design Components ระบุไว้ตรงๆ ทั้งคู่ (ไม่ใช่แค่ Design ฝ่ายเดียวแบบ WYN-015) — ต่างจาก WYN-015 ตรงที่รอบนี้ Coding ไม่ได้บันทึกเหตุผลไว้เลยว่าทำไมตัด Rating ออก (ขณะที่ "แนะนำสำหรับคุณ"/"ขายดี" ในงานเดียวกันกลับบันทึกเหตุผลไว้ชัดเจนว่าไม่มีข้อมูลรองรับ) แสดงว่าปัญหาไม่ใช่แค่ "ลืมทำ" แต่เป็น "ลืมแม้กระทั่งจะบันทึกว่าตัดใจไม่ทำ" — บทเรียนเพิ่มเติม: pre-submission checklist ที่เสนอไว้หลัง WYN-015 ยังไม่ถูกนำมาใช้จริงเป็นขั้นตอนบังคับ (ยังเป็นแค่ข้อเสนอ) ยืนยันว่าจำเป็นต้องทำให้เป็นขั้นตอนจริงจัง ไม่ใช่แค่บันทึกไว้เฉยๆ ในเอกสาร)
- Rework rate: WYN-002 ใช้เวลา 3 รอบ QA ถึงจะ PASS (2 FAIL, 1 PASS); WYN-003 ใช้เวลา 2 รอบ QA ถึงจะ PASS (1 FAIL, 1 PASS); WYN-004 ใช้เวลา 2 รอบ QA ถึงจะ PASS (1 FAIL, 1 PASS); WYN-005 ใช้เวลา 3 รอบ QA ถึงจะ PASS (2 FAIL, 1 PASS) — มากที่สุดในบรรดา task ที่ผ่าน QA แล้วทั้งหมด เพราะ Coding พลาด checklist item คนละจุดใน "Comment" feature ถึง 2 ครั้งติดกัน (Like แล้วก็ Delete) แม้จะเป็น pattern เดียวกัน (feature ที่ WYN-004 ไม่เคยมี); **WYN-006 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เร็วที่สุดในบรรดา task ที่มี Comment feature ทั้งหมด เพราะมี pattern ที่ถูกต้องครบจาก WYN-005 (หลัง Debug รอบ 2) ให้ reuse ตรง ๆ ตั้งแต่ต้น ไม่ต้องเดา/ประดิษฐ์ใหม่; WYN-007 ใช้เวลา 2 รอบ QA ถึงจะ PASS (1 FAIL, 1 PASS) — Debug รอบเดียวแก้ครบ ไม่มี regression ใหม่เกิดขึ้นจากการแก้ (ต่างจาก WYN-002 ที่การแก้รอบ 1 เอง introduce บั๊กใหม่); **WYN-008 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่สองต่อจาก WYN-006 ที่ผ่านรอบเดียว แม้จะเป็น feature ใหม่ที่มีโครงสร้างต่างจากเดิม (user-to-user ไม่ใช่ content-based) เพราะ Coding ไล่ checklist ครบและพบ/แก้ gap ที่ไม่เห็นชัดเจน (`IndexedStack` sync) เองก่อนส่ง แทนที่จะรอให้ QA จับ; **WYN-013 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่สามต่อจาก WYN-006/WYN-008 ที่ผ่านรอบเดียว แม้เป็นงานที่ใหญ่ที่สุดเท่าที่เคยทำ (rewrite `ViewProfileScreen` สองบุคลิกในไฟล์เดียว + tap-to-profile 5 จุดทั่วแอป + tab ใหม่ 3 อัน + database view ใหม่) เพราะ reuse pattern ที่พิสูจน์แล้วทุกจุด (nullable Follow state, nested InkWell, database-side UNION ALL view) แทนที่จะประดิษฐ์ใหม่; **WYN-009 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่สี่ต่อจาก WYN-006/WYN-008/WYN-013 ที่ผ่านรอบเดียว โดดเด่นตรงที่ Coding เองพบ testing gap ที่ลึกระดับ "test ที่ผ่านแต่ไม่ได้พิสูจน์อะไรจริง" (ไม่ใช่แค่บั๊กในโค้ด production) แล้วแก้ก่อนส่ง QA — QA ทำ red→green ซ้ำเองอิสระยืนยันตรงกับที่รายงานเป๊ะ; **WYN-012 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่ห้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009 ที่ผ่านรอบเดียว แม้เป็น task แรกที่ต้องพึ่ง DB trigger (ไม่ใช่แค่ RLS policy/view) เป็นกลไกหลักในการทำงาน — QA ทำ red→green proof ของ mapping logic ซ้ำเองอิสระด้วยทิศทางตรงข้ามจากที่ Coding เคยทดสอบ (พิสูจน์ว่า test จับบั๊กได้ทั้งสองทิศ ไม่ใช่แค่ทิศเดียว); **WYN-014 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่หกต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012 ที่ผ่านรอบเดียว แม้เป็นงานที่ใหญ่ที่สุดเท่าที่เคยทำและเป็น task แรกที่มีระบบ role-based permission เต็มรูปแบบ (5 RPC function + RLS หลายชั้น) เพราะ reuse pattern ที่พิสูจน์แล้วทุกจุดที่ทำได้ (RPC-over-raw-RLS จาก `increment_pop_view_count`, DropDetailScreen's comment thread, FollowListScreen row shape) และ Coding เจอ/แก้ gap ที่ไม่เห็นชัดเจนเอง 2 จุดก่อนส่ง (layout overflow, CHECK constraint race) — QA ทำ red→green proof อิสระ 2 จุด จุดหนึ่งทำซ้ำ reproduction เดียวกับ Coding อีกจุดทำทิศตรงข้าม; **WYN-015 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่เจ็ดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014 ที่ผ่านรอบเดียว แม้เป็น task แรกที่มี fan-out notification trigger (insert หลายแถวต่อเหตุการณ์เดียว) เพราะ reuse pattern ที่พิสูจน์แล้วทุกจุด (`club_role()` เป็น single source of truth, RLS-only scoping แทน DB view ใหม่, optional-repository-param สำหรับ feature ที่ผูกกับ context เดียว) — QA พบ Minor 1 จุด (component ที่ Design ระบุแต่ Coding มองข้าม — pattern เดิมซ้ำเป็นครั้งที่ 4) แต่ไม่กระทบ Acceptance Criteria จึงไม่ block, ทำ red→green proof อิสระคนละจุดกับที่ Coding ทำ; **ZOKY-001 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่แปดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015 ที่ผ่านรอบเดียว และเป็น task แรกของสาย ZOKY Marketplace (ผลิตภัณฑ์ใหม่ทั้งสาย ไม่ใช่แค่ feature ใหม่ใน WYN Social เดิม) เพราะ reuse pattern ที่พิสูจน์แล้วทุกจุดที่ทำได้ (`ClubMiniCard`/`PopGridTile` shape, `ClubPostImages` carousel, select-all-authenticated RLS ไม่มี write policy) และ Coding เจอ/แก้ gap เอง 2 จุดก่อนส่ง (nav index ผิดตำแหน่ง, `FutureBuilder.hasData` bug จริง) — QA พบ Minor 1 จุด (Rating หายทั้ง Product Detail และ Store — pattern เดิมซ้ำเป็นครั้งที่ 5) แต่ไม่กระทบ Acceptance Criteria จึงไม่ block, ทำ red→green proof อิสระคนละจุดกับที่ Coding ทำ (สูตรคำนวณเปอร์เซ็นต์ส่วนลด แทนที่จะทำซ้ำ FutureBuilder bug เดิม); **ZOKY-002 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่เก้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001 ที่ผ่านรอบเดียว และเป็น **task แรกในสาย ZOKY ที่ QA พบ 0 findings เลย** (แม้แต่ Minor ก็ไม่มี) ต่างจาก ZOKY-001 ที่ยังพบ Rating หายไป — เพราะ reuse pattern debounce/TabBar/`SearchStateMessage` ที่พิสูจน์แล้วจาก WYN-009/WYN-015 ตรงๆ และ ILIKE safety pattern ที่ยึดตาม `ClubRepository.searchClubs` เป๊ะ ไม่มีการ deviate ไปทำ `.or()` string interpolation แบบที่เคยเป็น Minor ใน WYN-009 — QA ทำ red→green proof อิสระบน Sort menu's `PopupMenuItem` value/label binding และไล่ Requirements/Design/AC ครบทั้ง 3 หัวข้อไม่พบช่องว่างแม้แต่จุดเดียว; **ZOKY-003 ใช้เวลาแค่ 1 รอบ QA ถึงจะ PASS** (0 FAIL, 1 PASS) — เป็น task ที่สิบต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001/ZOKY-002 ที่ผ่านรอบเดียว และเป็น **task ที่มีความเสี่ยง data-integrity สูงที่สุดเท่าที่เคยทำในโปรเจกต์นี้** (เงิน/stock/transaction หลายตารางพร้อมกันในธุรกรรมเดียว) เพราะ reuse pattern security-definer RPC ที่พิสูจน์แล้วจาก WYN-014/WYN-006 ตรง ๆ (lock แถวก่อนอ่าน/เขียนเสมอ ไม่เชื่อค่าจาก client) และ Coding เจอ/แก้บั๊กจริงเอง 1 จุดก่อนส่ง (`Column` mainAxisSize.max collapse ทั้งหน้าจอ — รูปแบบบั๊กใหม่ที่ไม่เคยเจอมาก่อน ไม่ทิ้ง exception ให้ตามรอยเลย) — QA ทำ red→green proof อิสระยืนยันบั๊กที่ Coding แก้ (ลบ fix ออกจริง 9/11 test พังจริง คืนค่าแล้ว 11/11 ผ่าน) และวิเคราะห์ concurrent race condition scenario ของ RPC `create_orders` อย่างละเอียด (2 request แย่งสินค้าชิ้นสุดท้ายพร้อมกัน) ยืนยัน atomicity ถูกต้องจริงไม่มีทาง stock ติดลบ พบ Minor 1 จุดไม่ block (Cart badge refresh end-to-end ไม่มี regression test ถาวร); **ZOKY-004 ใช้เวลา 2 รอบ QA ถึงจะ PASS** (1 FAIL, 1 PASS) — เป็น task แรกในสาย ZOKY ที่ไม่ผ่าน QA รอบเดียว และเป็น task แรกในโปรเจกต์นี้ที่ QA พบช่องโหว่ security ระดับ Critical (ไม่ใช่ UI/UX gap แบบที่เคยพบมาก่อนหน้าทั้งหมด) — Debug แก้ด้วย fix ที่เล็กที่สุดเท่าที่เป็นไปได้ (เพิ่ม `with check` clause เดียว ไม่แตะ Dart เลย) ปิด rework loop ได้ในรอบเดียวไม่เกิด regression ใหม่ (`flutter test` จำนวนเท่าเดิมทุกตัวก่อน/หลังแก้) — เป็น task ที่สิบเอ็ดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001/ZOKY-002/ZOKY-003 นับรวมทุก task ที่ผ่าน QA แล้ว และเป็น task สุดท้ายที่ปิดจบ ZOKY Marketplace Customer-facing scope ทั้งสาย
- Deployment failures: UNKNOWN (ยังไม่มี deployment จริง)
- Common mistakes:
  - การแก้บั๊ก navigation โดยไม่ trace ผลกระทบต่อ lifecycle ทั้งหมด (WYN-002) — แก้ให้ "ไปถึงหน้าถัดไปได้" แต่ไม่เช็คว่าขั้นตอนถัดจากนั้นยังทำงานถูกต้องหรือไม่
  - Empty string vs NULL mismatch ระหว่าง app กับ DB constraint (WYN-003) — ฟิลด์ optional ที่ผู้ใช้ยังไม่กรอก ต้องส่งเป็น `null` ไม่ใช่ `''` เมื่อ DB constraint กำหนดความยาวขั้นต่ำไว้ — **แก้จบในรอบเดียวและมี automated test คุ้มครองจริง** (ต่างจาก WYN-002 ที่ยังไม่มี test คุ้มครอง fix)
  - Async button handler ที่อ่านค่า state จาก parameter/closure ที่ถูก capture ไว้ตอน build แทนที่จะอ่านสดใหม่จาก field ในตัว method เอง (WYN-004) — เปิดช่องให้กดปุ่มซ้ำเร็ว ๆ ก่อนหน้าจอ rebuild ทำให้เกิด duplicate network call ที่ใช้ค่าเดิมผิด ๆ ซ้ำกัน — จุดที่ถูกต้องอยู่แล้ว (`PostDetailScreen._toggleLike`) กับจุดที่ผิด (`FeedScreen._toggleLike`) อยู่ในไฟล์เดียวกันของงานเดียวกัน แสดงว่า pattern ที่ถูกต้องไม่ได้ถูกยึดเป็นมาตรฐานเดียวกันทั่วทั้ง feature
- Successful patterns:
  - "Callback-to-parent-rebuild" แทน "สร้าง route ใหม่ (Navigator.push/pushReplacement)" เมื่อ child widget ต้องแจ้งให้ auth-state gate (`AuthGate`) เปลี่ยนหน้าจอ — ดู `.wyn/learning/PATTERNS.md`
  - แยก logic ที่เป็นปัญหาออกมาเป็น pure function (`normalizeOptionalText`) เพื่อให้เขียน regression test ได้โดยไม่ต้องพึ่ง Supabase จริง — ทำให้ WYN-003 ใช้รอบ QA น้อยกว่า WYN-002
- Development bottlenecks:
  - การเชื่อม auth state (Supabase `AuthState` stream) เข้ากับ Navigator/route lifecycle เป็นจุดที่พลาดซ้ำได้ง่ายที่สุดใน WYN-002 — เกิดบั๊กที่เกี่ยวข้องกับจุดนี้ 2 รอบติดกันก่อนจะแก้ถูกจุด
  - ความสอดคล้องระหว่าง Dart-side default values (`?? ''`) กับ Postgres CHECK constraint semantics — พบใน WYN-003 แต่แก้ได้เร็วกว่า WYN-002 มาก (1 รอบ vs 2 รอบ) เพราะ bug เป็น pure logic ไม่ใช่ stateful navigation
  - `PostRepository` เป็น concrete class ผูกกับ `SupabaseClient` ตรง ๆ ไม่มี interface ให้ mock/spy — ทำให้ QA รอบ 1 ของ WYN-004 ยืนยันบั๊ก double-tap ได้แค่ระดับ code-trace **แก้แล้วใน Debug รอบถัดมา**: subclass ธรรมดา (`RecordingPostRepository`) และปลอม Supabase session แบบ local-only ก็เพียงพอ ไม่ต้องเพิ่ม abstract interface ใหญ่ — ดู `.wyn/learning/PATTERNS.md`
- QA rigor เพิ่มเติมที่พิสูจน์ตัวเองว่าคุ้มค่า: QA รอบ 2 ของ WYN-004 ไม่ได้แค่รัน test ที่ Debug Engineer เขียนมาแล้วเชื่อผลลัพธ์ แต่ย้อน logic กลับไปเป็นก่อนแก้ชั่วคราวเพื่อยืนยันว่า test ใหม่ FAIL จริงก่อนแก้ (ไม่ใช่ test ที่ผ่านโดยบังเอิญ) ก่อนจะ PASS งาน
- Common mistakes เพิ่มเติม: **Coding ทำตาม Design/Product spec ไม่ครบทุกข้อ (WYN-005)** — spec ทั้งสองฉบับระบุ "Like Comment" ไว้ตรงกันชัดเจน (Product's Requirements และ Design's Screen 3 Components) แต่ implementation ขาดไปทั้งระบบ (ไม่มีทั้ง schema/repository/UI) โดยไม่ได้ถูกบันทึกไว้เป็น known/deferred scope เหมือนจุดอื่นที่ตัดออกอย่างตั้งใจ (เช่น hashtag click-through) — แสดงว่าเป็นการมองข้ามจริง ไม่ใช่การตัดขอบเขต บทเรียน: Coding ควรไล่ checklist ทุกบรรทัดของทั้ง Product Requirements และ Design Components ก่อนส่งงาน ไม่ใช่แค่ implement ตามความเข้าใจคร่าว ๆ ของ flow หลัก และ QA ควรเทียบ requirement/component list ทีละบรรทัดกับโค้ดจริงเสมอ (ไม่ใช่แค่ทดสอบ flow หลักแล้วผ่าน) — รอบนี้ QA จับได้ก่อนถึง Founder
- Common mistakes เพิ่มเติม (2): **QA เองก็เทียบ spec ไม่ครบทุกช่องทาง (WYN-005 รอบ 1 → รอบ 2)** — QA รอบ 1 เทียบ Requirements + Design Components ทีละบรรทัดแล้วจับบั๊ก Like Comment ได้ แต่ไม่ได้ไล่ "Acceptance Criteria" checklist (ซึ่งเป็นเอกสารคนละหัวข้อในไฟล์เดียวกัน) แบบเดียวกันแบบครบทุกข้อ ทำให้พลาดจับได้ว่า "ลบ Comment ของตัวเองได้" หายไปทั้งระบบเหมือนกัน (มี RLS delete policy ฝั่ง DB รองรับไว้แล้วตั้งแต่รอบ Coding แรก แต่ไม่มี repository method และไม่มี UI เลย — เหมือนบั๊ก Like Comment เป๊ะ) พบตอน QA รอบ 2 ตอนไล่ Acceptance Criteria ทุกบรรทัดใหม่ทั้งหมดแทนที่จะเทียบแค่จุดที่เพิ่งแก้ บทเรียน: QA ต้องไล่ **ทั้ง** Requirements, Design Components, **และ** Acceptance Criteria ทีละบรรทัดแยกกันทุกครั้ง ไม่ใช่แค่หัวข้อใดหัวข้อหนึ่ง เพราะแต่ละหัวข้อในเอกสารเดียวกันอาจระบุรายละเอียดที่อีกหัวข้อไม่ได้พูดถึง — และ QA รอบถัดไปของ task ใดก็ตามที่เคย FAIL ต้องไล่ checklist แบบครบทุกหัวข้อใหม่ทั้งหมดเสมอ ไม่ใช่แค่ตรวจจุดที่ Debug เพิ่งแก้
- Successful patterns เพิ่มเติม: **แยก Severity ตามผลกระทบต่อ Acceptance Criteria จริง ไม่ใช่แค่ "spec ไม่ตรงกับโค้ด" เสมอไป (WYN-005 รอบ 3)** — QA รอบ 3 พบว่า Design Component "เวลาที่โพสต์ (relative time)" หายไปทั้ง Drop header และ comment list แต่ไม่ปรากฏใน Product Requirements/Acceptance Criteria เลย จึงจัด Severity เป็น Minor และไม่ block การอนุมัติ (ต่างจาก Like Comment/Delete Comment ที่ปรากฏใน Product Requirements ตรง ๆ และกระทบ Acceptance Criteria checkbox โดยตรง จึงเป็น Major) — บทเรียน: ไม่ใช่ทุก mismatch ระหว่าง spec กับโค้ดจะต้อง FAIL เสมอไป ให้ประเมินว่ากระทบ Acceptance Criteria (เกณฑ์อนุมัติจริงของ task) หรือเป็นแค่ความสมบูรณ์ของ UI/Design ที่ตามแก้ทีหลังได้โดยไม่กระทบการอนุมัติ
- Successful patterns เพิ่มเติม (2): **การลงทุนสร้าง pattern ที่ถูกต้อง+เอกสารบทเรียนไว้ตั้งแต่ feature แรก จ่ายผลตอบแทนจริงใน feature ถัดไป (WYN-006)** — WYN-006 (Pop) มี Comment ครบทั้ง 3 ความสามารถ (เพิ่ม/ลบ/Like) ตั้งแต่การส่งมอบครั้งแรก และผ่าน QA รอบเดียวโดยไม่มี FAIL เลย ต่างจาก WYN-005 ที่พลาดจุดเดียวกันนี้ไป 2 รอบติดกัน — ความแตกต่างเดียวคือ WYN-006 มีโค้ดที่ถูกต้องของ WYN-005 (หลัง Debug รอบ 2) ให้ reuse ตรง ๆ ระหว่างที่ WYN-005 ไม่มี pattern อะไรให้อ้างอิงเลยตอนเริ่ม (WYN-004 ไม่เคยมี Like/Delete Comment) — บทเรียน: เวลาที่ใช้แก้บั๊ก+เขียน regression test+บันทึก pattern ใน `.wyn/learning/` ไม่ใช่ overhead แต่เป็นการลงทุนที่ลด rework ของงานถัดไปได้จริงและวัดผลได้ (WYN-005: 3 รอบ QA, WYN-006: 1 รอบ QA)
- Successful patterns เพิ่มเติม (3): **สร้าง fake platform implementation ใหม่เมื่อ behavior สำคัญผูกกับ plugin ที่ไม่มี platform channel ในสภาพแวดล้อมทดสอบ (WYN-006)** — `video_player` ปกติจะ throw `MissingPluginException` ใน `flutter test` เพราะไม่มี platform implementation จริง ถ้าปล่อยแบบนั้น view-count recording และ mute/volume propagation จะกลายเป็นจุดที่ทดสอบไม่ได้เลย (เหมือนที่ยอมรับไว้สำหรับ `Image.network` ใน Drop) — แต่ต่างจาก Drop ตรงที่ Pop มี behavior จริงผูกกับความสำเร็จของการเล่นวิดีโอ จึงคุ้มที่จะสร้าง `FakeVideoPlayerPlatform` (implement `VideoPlayerPlatform` แบบง่าย emit `VideoEvent.initialized` ทันที) แทนที่จะยอมรับว่าทดสอบไม่ได้ — บทเรียน: การยอมรับข้อจำกัดของ sandbox (accepted known limitation) ควรใช้เฉพาะเมื่อสร้าง test double ไม่คุ้มค่าจริง ๆ ไม่ใช่ default แรกที่เลือกเสมอ
- Successful patterns เพิ่มเติม (4): **RPC-function-over-raw-RLS สำหรับ permission graph ที่ซับซ้อน scale ได้จริงถึงระบบ role 4 ระดับ (WYN-014)** — ต่อยอดจาก `increment_pop_view_count` (WYN-006) ที่เป็น RPC ตัวแรกของโปรเจกต์ (ใช้กับ counter เดียว) มาถึง WYN-014 ที่มี RPC 5 ตัวจัดการ permission graph เต็มรูปแบบ (Owner/Admin/Moderator×approve/reject/set-role/remove/ban) — ผ่าน QA รอบเดียวไม่มี privilege-escalation gap เลยแม้แต่จุดเดียว เพราะทุก RPC re-derive role ของผู้เรียกเองจาก `club_role()` ไม่เชื่อค่าจาก client และมี NULL-safety guard ที่คิดไว้ตั้งแต่ต้น (`coalesce()` ป้องกัน `null not in (...)` ที่จะ evaluate เป็น NULL แทน true) — บทเรียน: pattern "อย่าเชื่อ role ที่ client อ้าง ให้ query จริงจากฐานข้อมูลเสมอ" ที่เคยพิสูจน์ตัวที่ counter เดียว scale ไปใช้กับ permission graph หลายชั้นได้จริงโดยไม่ต้องคิดใหม่ทั้งหมด
- Successful patterns เพิ่มเติม (5): **Coding คิดการป้องกันเพิ่มเองนอกเหนือจาก spec เมื่อพบช่องโหว่จริงระหว่างออกแบบ (WYN-014)** — `clubs_prevent_owner_id_change` trigger ไม่ได้ถูกระบุไว้ใน Product/Design spec เลยแม้แต่คำเดียว แต่ Coding สังเกตเองว่า RLS update policy ของ `clubs` (ที่อนุญาต Owner/Admin แก้ไขข้อมูล Club) เปิดช่องให้ Admin แอบเปลี่ยน `owner_id` เป็นตัวเองได้ผ่าน update ปกติถ้าไม่มีการป้องกันเพิ่ม จึงเพิ่ม trigger กันไว้เอง — QA ตรวจสอบแล้วยืนยันว่าเป็นช่องโหว่จริงถ้าไม่มี trigger นี้ และการป้องกันที่เพิ่มมาถูกต้อง — บทเรียน: การไล่ตาม spec ให้ครบเป็นฐาน แต่ไม่ควรหยุดแค่นั้น ต้องคิดต่อว่า permission ที่มอบให้จุดหนึ่ง (Edit Info) จะถูกใช้ผิดวัตถุประสงค์ไปทำอย่างอื่น (เปลี่ยนเจ้าของ) ได้หรือไม่ โดยเฉพาะงานที่เพิ่ม column ใหม่ (`owner_id`) เข้าตารางที่มี broad update policy อยู่แล้ว

## อัปเดตล่าสุด

2026-08-15 — หลัง QA รอบ 1 ของ WYN-010 (**PASS** — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่สิบสองที่ผ่าน QA — เติม Share/Copy Link ให้ ZOKY Product Detail/Store ที่ขาดหายมาตั้งแต่สร้าง ตอนนี้ Share ครบทุก content type ในแอปแล้ว (Drop/Pop/Club post/Product/Store) — ค้นพบข้อจำกัดใหม่ของ sandbox ระหว่างทาง: `Clipboard`/`share_plus` platform channel ไม่ถูก mock เลย ทำให้ tap ปุ่มเหล่านี้ hang เงียบ ๆ ไม่มี exception ตลอดมา อธิบายว่าทำไมไม่มีเทสต์ไหนในโปรเจกต์นี้เคย tap ปุ่ม Share/Copy Link เลยตั้งแต่ WYN-005 บันทึกเป็น pattern ใหม่)

2026-08-15 — หลัง QA รอบ 2 ของ ZOKY-004 (**PASS** — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่สิบเอ็ดที่ผ่าน QA และ **task สุดท้ายที่ปิดจบ ZOKY Marketplace Customer-facing scope ทั้งสาย** (Browse → Cart → Checkout → Order → Review ครบวงจร ตาม roadmap Phase 2-3) — รอบ 1 FAIL เพราะพบช่องโหว่ security ระดับ Critical ครั้งแรกในโปรเจกต์นี้ (`reviews`' update RLS policy ไม่ตรวจ delivered-order-ownership gate ซ้ำ) Debug แก้ด้วย `with check` clause เดียว ไม่ต้องแตะ Dart เลย ปิด rework loop ได้ในรอบเดียว)

2026-08-15 — หลัง QA รอบ 1 ของ ZOKY-004 (**FAIL** — ส่งต่อ AI Debug Engineer พร้อม bug report ที่ `.wyn/tasks/bugs/ZOKY-004-review-update-rls-gap.md`, ครั้งแรกที่พบ QA failure ตั้งแต่ WYN-007 — ครั้งแรกในสาย ZOKY ทั้งหมด — พบช่องโหว่ security จริงระดับ Critical: `reviews`' update RLS policy ไม่มี `with check` ตรวจ delivered-order-ownership gate ซ้ำ ทำให้ผู้ใช้แก้ไขรีวิวของตัวเองให้ retarget ไปยัง product/order_item ใดก็ได้โดยไม่ต้องซื้อจริง ขัดกับ security guarantee หลักของทั้งฟีเจอร์ตรง ๆ — พบจากการอ่าน Postgres RLS semantics โดยตรง ไม่ใช่จาก test suite เพราะไม่มี live Postgres ให้ทดสอบ แสดงให้เห็นว่าการอ่าน SQL policy ละเอียดยังจำเป็นแม้ flutter test ทั้ง 253 ผ่านหมด — insert policy ที่มี exists gate ครบถูกใช้เป็น "ต้นแบบที่ถูกต้อง" ยืนยันว่า Coding ไม่ได้ลืมเขียน security check แบบสุ่มสี่สุ่มห้า แต่พลาดเฉพาะจุด update policy ที่ comment ในโค้ดเองให้เหตุผลผิดพลาดบางส่วน)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-006 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task แรกที่ผ่าน QA รอบเดียวตั้งแต่ WYN-004)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-013 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่สามต่อจาก WYN-006/WYN-008 ที่ผ่าน QA รอบเดียว)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-009 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่สี่ต่อจาก WYN-006/WYN-008/WYN-013 ที่ผ่าน QA รอบเดียว)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-012 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่ห้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009 ที่ผ่าน QA รอบเดียว)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-014 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่หกต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012 ที่ผ่าน QA รอบเดียว, task แรกที่มีระบบ role-based permission เต็มรูปแบบผ่าน QA)

2026-08-14 — หลัง QA รอบ 1 ของ WYN-015 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่เจ็ดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014 ที่ผ่าน QA รอบเดียว, task แรกที่มี fan-out notification trigger, เชื่อม Club Core เข้ากับระบบหลักของแอปครบทั้ง 5 จุดตาม roadmap ของ WYN CLUB)

2026-08-14 — หลัง QA รอบ 1 ของ ZOKY-001 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่แปดต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015 ที่ผ่าน QA รอบเดียว, task แรกของสาย ZOKY Marketplace — เพิ่ม Bottom Nav tab ที่ 5 "ZOKY" ให้เรียกดูสินค้า/ร้านค้าได้จริง ยังไม่มี Cart/Checkout จนกว่าจะถึง ZOKY-003)

2026-08-14 — หลัง QA รอบ 1 ของ ZOKY-002 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่เก้าต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001 ที่ผ่าน QA รอบเดียว, task แรกในสาย ZOKY ที่ QA พบ 0 findings เลย — เพิ่ม ZokySearchScreen ค้นหา/กรอง/เรียงลำดับสินค้าและร้านค้าได้จริง เชื่อมจาก ZOKY Home's search bar และ category chip)

2026-08-15 — หลัง QA รอบ 1 ของ ZOKY-003 (PASS — อนุมัติเข้า `.wyn/tasks/approved/` แล้ว, task ที่สิบต่อจาก WYN-006/WYN-008/WYN-013/WYN-009/WYN-012/WYN-014/WYN-015/ZOKY-001/ZOKY-002 ที่ผ่าน QA รอบเดียว, task ที่มีความเสี่ยง data-integrity สูงที่สุดเท่าที่เคยทำในโปรเจกต์นี้ — เพิ่ม Cart/Checkout/Order ให้ ZOKY Marketplace ซื้อสินค้าจบ flow ได้จริงเป็นครั้งแรก (Browse → Cart → Checkout → Order), RPC `create_orders` atomic จริงผ่านการตรวจสอบละเอียด, Coding พบและแก้บั๊กจริง 1 จุดเอง (`Column` mainAxisSize.max collapse) บันทึกเป็น pattern ใหม่)
