# Retrospective Log

บันทึก retrospective หลังจบทุก task ตามคำถามใน `.wyn/company/WORKFLOW.md` (Continuous Self-Improvement)

## รูปแบบ

```
### [YYYY-MM-DD] Task WYN-XXX
1. อะไรที่ทำได้ดี:
2. อะไรที่ผิดพลาด:
3. สาเหตุ:
4. สิ่งที่ควรปรับปรุง:
5. มีข้อผิดพลาดซ้ำหรือไม่:
6. ควรอัปเดตเอกสารหรือไม่:
7. ควรสร้าง checklist ใหม่หรือไม่:
8. ควรปรับปรุง workflow หรือไม่:
```

## รายการ

### [2026-09-05] Task WYN-106 / 107 / 108 / 109 (Home cards)

**1. อะไรที่ทำได้ดี**
- กติกา "ขอดูรูปก่อนเขียนโค้ด" ที่ Founder ตั้งกลางทาง ใช้ได้ผลจริง — ทุกการเปลี่ยนแปลงถูกส่งเป็น
  Artifact ให้ดูก่อน ทำให้ไม่มีงานไหนต้องรื้อทำใหม่เพราะเข้าใจโจทย์ผิด
- ตอนแผนแก้ view ล้มบน production (`42P16`) ไม่ดันทุรังหาทางแก้ view ให้ได้ แต่ถอยมาถามว่า
  "ต้องอ่านจาก view จริงไหม" แล้วเจอว่าเสียบ query เข้า `Future.wait` ที่มีอยู่แล้วได้ฟรี ๆ —
  ได้ฟีเจอร์ครบโดยไม่แตะโครงสร้าง production เลย
- QA รอบ 1 จับบั๊ก Critical ได้ก่อนขึ้น production (`_insertDrop` ส่งคอลัมน์ที่ยังไม่มีเสมอ →
  ถ้า deploy ก่อนรัน SQL การสร้างโพสต์**ทุกชนิด**จะพัง)

**2. อะไรที่ผิดพลาด**
- เขียนนิยาม view `home_feed` ขึ้นมาเองจากความจำเพื่อทำ migration ทั้งที่ของจริงยาว 231 บรรทัด
  จับได้ตอน diff กับของจริง ถ้าไม่จับ = ทำลาย view ของ production
- ซ้อม migration โดยโหลด `schema.sql` จาก repo → ทดสอบกับ*สิ่งที่เราเขียนเอง* ไม่ใช่ production
  ทำให้ migration ที่ "ผ่านการซ้อม" ไปล้มจริงต่อหน้า Founder
- QA รอบ 1 ปล่อยผ่านเทสต์ที่ตรวจด้วย `source.contains(...)` ซึ่งจับได้แค่การ revert แบบตรงตัว
  เปลี่ยนชื่อตัวแปรก็ผ่าน — QA รอบ 2 ต้องมาเขียนใหม่ให้วัดสิ่งที่ render จริง
- commit แรกเผลอเพิ่มคอลัมน์เข้านิยาม view ใน `schema.sql` ทั้งที่ตัดสินใจแล้วว่าจะไม่แก้ view จริง
  = เพิ่ม drift เข้าไปอีกชั้นบนโรคที่กำลังรักษาอยู่

**3. สาเหตุ**
- ข้อ 1-2 มาจากรากเดียวกัน: **สันนิษฐานว่า repo สะท้อน production** ทั้งที่ SCHEMA-004 พิสูจน์แล้วว่าไม่
- ข้อ 3: เขียนเทสต์ให้ "ผ่าน" แทนที่จะเขียนให้ "ล้มถ้าบั๊กกลับมา"
- ข้อ 4: แก้ทีละจุดโดยไม่ไล่ดูว่าการตัดสินใจใหม่ทำให้ของเก่าที่เขียนไว้กลายเป็นเท็จตรงไหนบ้าง

**4. สิ่งที่ควรปรับปรุง**
- ก่อนเขียน migration ที่แตะ view: ดึง `pg_get_viewdef` จาก production มาก่อนเสมอ ห้ามเขียนจาก repo
- ก่อนเขียน migration ที่แตะ view: ถามก่อนว่าอ่านจากตารางต้นทางแบบ batch แทนได้ไหม
- เทสต์ regression ต้องวัดพฤติกรรม ไม่ใช่ข้อความในซอร์ส
- ทุกครั้งที่ตัดสินใจ "จะไม่ทำ X" ต้องไล่ลบร่องรอยของ X ออกจาก repo ให้ครบ

**5. มีข้อผิดพลาดซ้ำหรือไม่**
ซ้ำ — "CI เขียว ≠ production ใช้ได้" เป็นบทเรียนเดียวกับเหตุการณ์ Vercel 2026-09-02 รอบนี้จึงไม่
ประกาศว่าเสร็จจนกว่า Founder จะเปิดดูจริง (และ session นี้ถูกบล็อก egress ยืนยันเองไม่ได้)

**6. ควรอัปเดตเอกสารหรือไม่**
อัปเดตแล้ว: `DECISIONS.md` (ไม่แก้ view), `LESSONS_LEARNED.md` (3 บทเรียน),
`SCHEMA-004` (ลดความเร่งด่วน), `schema.sql` (คอมเมนต์ว่าทำไมจงใจไม่ใส่คอลัมน์เข้า view)

**7. ควรสร้าง checklist ใหม่หรือไม่**
**ควร** — checklist "migration ที่แตะ view" ยังไม่มี: (ก) เลี่ยงได้ไหม (ข) ดึง `pg_get_viewdef`
จาก production (ค) ซ้อมบนนิยามจริง (ง) ตรวจว่าเป็น append-only

**8. ควรปรับปรุง workflow หรือไม่**
**ควร** — 2 จุด:
- QA รอบนี้ทำโดย session เดียวกับที่เขียนโค้ดบางส่วน (subagent ถูกตัดเพราะ rate limit)
  workflow ควรระบุว่าถ้า QA agent ล้ม ต้องบันทึกว่าใครตรวจอะไร ไม่ใช่กลืนไปเงียบ ๆ
- WORKFLOW.md ยังไม่มีขั้น "production verification" ที่ชัดเจนว่าใครเป็นคนยืนยันและยืนยันอะไรบ้าง
  รอบนี้ต้องด้นสดเอง
