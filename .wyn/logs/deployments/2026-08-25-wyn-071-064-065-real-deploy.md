# Deployment Log — WYN-071 (WYNOS Visual Refresh) + WYN-064 (Home tab scroll) + WYN-065 (Profile UI polish) — REAL PRODUCTION DEPLOY, SUCCESS

> Continuation of `.wyn/logs/deployments/2026-08-25-wyn-071-064-065-deploy-blocked.md`, which recorded the first blocker (missing GitHub Actions secrets). This entry covers everything from there through a real, verified production deploy.

```
Release: WYN-071 (WYNOS Visual Refresh -- Light theme, multi-image Drop, Profile Recommendation Section, Profile tab restructure, Search/Notification icons, haptics), WYN-064 (Home tab tap-to-scroll-to-top/refresh), WYN-065 (Profile screen UI polish -- full-width Edit button, AppBar icon spacing, Skeleton loading)
Version: build main.dart.js (3,985,507 bytes), built via GitHub Actions run 32837045512 from `main` @ `a9b5160` (release mode, tree-shaken icons), workflow_dispatch
QA Status: PASS for all three (see the blocked-log entry and each task's own doc under `.wyn/tasks/approved/`)
Build Status:
  - `flutter build web --release --dart-define-from-file=dart_define.json`: succeeded, 49.6s compile, `build/web/` produced
Deployment Target: Vercel project `warren14/wynteam` (VERCEL_PROJECT_ID secret), production alias **https://wynteam.vercel.app** -- note this is a *different* alias than the one used in earlier deploy logs (`https://web-neon-sigma-66.vercel.app`, the "web" project); `VERCEL_PROJECT_ID`/`VERCEL_ORG_ID` as configured by Founder point at the `wynteam` project instead. Flagging this discrepancy rather than silently treating either as "the" production URL -- Founder should confirm which one is the actual user-facing production domain going forward (see Next Steps).
Changes: PR #179 (`claude/pending-tasks-ogs3jb` -> `main`, commit `9c29346`) + PR #180 (`fix/deploy-workflow-trim-secrets` -> `main`, commit `a9b5160`, the CI whitespace-trim fix). No app code changed since the blocked-log entry -- only the deploy workflow itself and the 5 GitHub Actions secrets (added by Founder).

Deployment attempts this session, in order (all against `main`, via `deploy-web.yml` workflow_dispatch):
  1. Run 32811578947 (`main`@`9c29346`) -- FAILED at `flutter build web`: `SUPABASE_URL` secret's value had a trailing newline (mobile clipboard paste), broke the JSON in `dart_define.json`. Root-caused from job logs, not guessed.
  2. Fixed via PR #180: strip whitespace/newlines from all 5 secrets before use, merged as `a9b5160`.
  3. Run 32811787822 (`main`@`a9b5160`) -- build succeeded this time; FAILED at "Deploy to Vercel production": `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` still empty (Founder had only added the 2 Supabase secrets at that point).
  4. Run 32812517152 -- build succeeded, Vercel auth succeeded, upload succeeded (3.8MB); FAILED at the Vercel API call itself: `Error: Resource is limited - try again in 24 hours (more than 100, code: "api-deployments-free-per-day")` -- Vercel Hobby (free) plan's 100-deployments/day cap, shared across the account's 4 auto-preview projects (each PR opened today triggered 4 preview deploys) plus this session's own repeated test runs. Not a config/code problem -- scheduled a 24.5h retry (`send_later`, trigger `trig_013p2cd9s61WWWhBw9T6hA8m`) rather than guess when the rolling window would clear.
  5. Founder asked to check early (~5h later, run 32837045512) -- **SUCCEEDED**. The daily cap is evidently a per-calendar-day (or shorter rolling) reset, not a strict 24h-from-last-attempt window.
Deployment Result: **SUCCESS.** Vercel CLI output: `Inspect https://vercel.com/warren14/wynteam/HvfaW9rdp4vhm2bZhxrtQTp8CkTC`, `Production https://wynteam-eufax6ydu-warren14.vercel.app`, `Aliased https://wynteam.vercel.app`, `Ready in 4s`.
Production Verification: Done for real against the live alias, not assumed:
  - `curl https://wynteam.vercel.app/` -> HTTP 200, valid Flutter web `index.html`.
  - `curl https://wynteam.vercel.app/main.dart.js` -> HTTP 200, 3,985,507 bytes (matches a real compiled build, not an error page or empty response).
  - `curl https://wynteam.vercel.app/manifest.json` -> HTTP 200, valid PWA manifest.
  - `curl https://wynteam.vercel.app/flutter_bootstrap.js` -> HTTP 200.
  - **Not done**: a real-browser smoke test (sign-in, Home feed render, tapping into the new WYN-071 screens) -- this session's Chromium/Playwright path has the same egress restrictions noted in prior deploy logs. Recommend Founder open https://wynteam.vercel.app in an actual browser to visually confirm the Light theme, multi-image Drop composer, Profile Recommendation Section, and new Profile tabs (Posts/ReDrops/Replies/Media/Likes) all render as designed.
Rollback Plan:
  - Immediate: `vercel rollback` (Vercel CLI, needs the same VERCEL_TOKEN) or re-running `deploy-web.yml` against a prior commit restores an earlier build within a minute -- Vercel keeps deployment history.
  - Code: `git revert` merge commits `9c29346` (PR #179) and/or `a9b5160` (PR #180) on `main`, then redeploy.
  - Database: no schema changes in this batch beyond what WYN-071's own QA already verified (`drop_images`, `profile_recommendation_dismissals`) -- both already live in production from earlier in this project's history, unaffected by this deploy.
```

## Root causes fixed this session (for future deploys)

1. **GitHub Actions secrets were never populated** after the `deploy-web.yml` workflow was added (PR #177/#178) -- both of Founder's own manual attempts on 2026-08-24 failed silently on this. Founder added all 5 secrets (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`) via the GitHub web UI (mobile browser) this session.
2. **Mobile clipboard paste added a trailing newline** to at least the `SUPABASE_URL` secret's value, which broke `dart_define.json`'s JSON syntax. Fixed permanently at the workflow level (PR #180) by stripping whitespace/newlines from every secret before use -- this protects against the same mistake recurring on any future secret update, not just this one.
3. **Vercel Hobby plan's 100-deployments/day cap** was hit from cumulative testing (this session's repeated attempts + every PR's 4 auto-preview deployments today). Not fixed (it's a plan limit, not a bug) -- documented here so a future session recognizes the error message immediately instead of re-diagnosing it.

## Next Steps

1. **Founder**: open https://wynteam.vercel.app in a real browser and confirm the WYN-071/064/065 changes look and behave correctly (Light theme, multi-image Drop, Profile Recommendation Section, new Profile tabs, Home tab tap-to-scroll, wider Edit button, Skeleton loading) -- this session could not complete a real-browser check itself.
2. **Founder**: clarify which Vercel alias is the actual production domain going forward -- `https://wynteam.vercel.app` (used by this deploy, per the `VERCEL_PROJECT_ID` secret as configured) vs `https://web-neon-sigma-66.vercel.app` (used by the WYN-056/057/058 deploy earlier). If they're meant to be the same site, one of the two Vercel projects/secrets is misconfigured and should be corrected so future deploys don't alternate between two URLs.
3. Consider whether the Vercel Hobby plan's shared 100/day quota (across 4 auto-preview projects + production) is workable long-term, or whether disabling auto-preview deploys for stale/merged PRs (or upgrading to Pro) would reduce contention with real production deploys.
4. Native mobile (iOS/Android) build/distribution remains blocked on `google-services.json`/`GoogleService-Info.plist` + a distribution channel -- unchanged from every prior log.
