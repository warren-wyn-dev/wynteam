# Deployment — WYN-106 / 107 / 108 / 109 (Home cards)

วันที่: 2026-09-05 03:01–03:04 UTC
Deploy โดย: AI (session `01LMSqZdpbTAizfDoJWyHpVy`) ตามคำสั่ง Founder "ทำต่อ ให้เสร็จทุกอย่าง พร้อมใช้เลย"
อำนาจ: `.wyn/company/RULES.md` — "Deploy การเปลี่ยนแปลงที่ได้รับอนุมัติแล้ว" อยู่ในอำนาจของ AI Team

## สิ่งที่ deploy

- Commit: `cb4d02d` (merge commit ของ PR #225 เข้า `main`)
- PR: https://github.com/warren-wyn-dev/wynteam/pull/225 — Founder merge เอง 2026-09-04 18:18 UTC
- Workflow run: https://github.com/warren-wyn-dev/wynteam/actions/runs/33940689609 (run #63)
- ปลายทาง: Vercel project "web" → `wynos.online` (Flutter Web release build)

## สิ่งที่เปลี่ยน

| งาน | สาระ |
|---|---|
| WYN-106 | ปุ่ม X ปิด `HomeExplainerBanner` ขยาย touch target 19×19 → 44×44 (ภาพไอคอนเท่าเดิม) |
| WYN-107 | การ์ดฟีด Home (`HomeDropCard` + `HomePopCard`) เปลี่ยนเป็นสองคอลัมน์ avatar ซ้าย / เนื้อหาขวา ทุก section เริ่มที่ x=78 บนจอ 390 แถวรูปชนขอบขวา |
| WYN-108 | `WynHeartIcon` (`CustomPainter` ทรง lucide) แทน `Icons.favorite` ของ Material ครบ 13 จุดทั้งแอป สีเดิมทุกสถานะ |
| WYN-109 | ผู้โพสต์เลือกอัตราส่วนรูปได้ 4 แบบ (ต้นฉบับ / 1:1 / 4:5 / 16:9) ค่าเริ่มต้น 4:5 · ลากเลือกจุดครอปได้ · ฟีด Home อ่านค่าผ่าน `HomeRepository._fetchAspectRatios` จากตาราง `drops` |

## Database

- **รันไปแล้วก่อนหน้า** โดย **Founder เอง** 2026-09-04: `supabase/migrations_wyn109a_column_only.sql`
  (เพิ่ม `drops.image_aspect_ratio text` + CHECK constraint · ไม่แตะ view ใด ๆ) ผล `Success. No rows returned`
- **ไม่มี SQL เพิ่มเติมสำหรับ deploy ครั้งนี้** — โค้ดอ่านค่าจากตาราง `drops` โดยตรง ไม่พึ่ง view
- AI ไม่ได้รัน SQL กับ production เอง (ตามกติกา `.wyn/company/RULES.md`)

## ผล verification

| รายการ | ผล |
|---|---|
| CI บน `main` หลัง merge (run 33905187301) | **success** — Flutter, Admin (Next.js), Supabase Edge Functions, schema.sql ordering ผ่านครบ |
| `flutter analyze` (local, ก่อน merge) | สะอาด |
| `flutter test` (local, ก่อน merge) | 1253/1253 ผ่าน |
| QA & Security รอบ 2 | PASS ทั้ง 4 งาน — `.wyn/docs/qa/wyn-106-107-108-109-home-cards-qa-round2.md` |
| Deploy workflow run #63 | **success** ครบ 9 step รวม "Deploy to Vercel production" |

### Production verification — **ผ่าน** (Founder ยืนยัน 2026-09-05)

Founder เปิด `wynos.online` ตรวจด้วยตาแล้วตอบว่า **"เช็คแล้ว ใช้ได้"** → ถือว่า deployment นี้
verified แล้ว ไม่ต้อง rollback สถานะงาน: COMPLETED

รายการที่ขอให้ตรวจไว้ (บันทึกไว้เพื่อให้รู้ว่าขอบเขตของการยืนยันคืออะไร):

### สิ่งที่ AI verify เองไม่ได้ จึงต้องให้ Founder ดู

**สภาพแวดล้อมของ session นี้ถูก block ไม่ให้ออกเน็ตไปที่ `wynos.online`** (egress proxy) จึง
**ไม่สามารถยืนยันด้วยตัวเองได้ว่าเว็บจริงเปิดขึ้นและใช้งานได้** ยืนยันได้แค่ว่า workflow รายงาน
success ทุก step ซึ่ง**ไม่เท่ากับ**การพิสูจน์ว่า production ใช้งานได้จริง — บทเรียน 2026-09-02
(Vercel GitHub App auto-deploy ทับ) แสดงแล้วว่า deploy สำเร็จกับเว็บใช้ได้เป็นคนละเรื่อง

สิ่งที่ Founder ควรเช็คด้วยตา:
1. เปิด `https://wynos.online` — เข้าได้ ไม่ขึ้น `NOT_FOUND` / `DEPLOYMENT_NOT_FOUND`
2. หน้า Home — การ์ดโพสต์เป็นสองคอลัมน์ ปุ่มอยู่ตรงแนวชื่อ ไม่ใช่ใต้รูปโปรไฟล์
3. หัวใจ — ทรงใหม่ กดแล้วเป็นสีแดง
4. ลองโพสต์รูปแล้วเลือก **16:9** → หน้า Home และหน้าโพสต์ต้องแสดงเป็น 16:9 ทั้งคู่
5. โพสต์เก่า — ต้องยังเป็น 4:5 เหมือนเดิม ไม่มีอะไรเพี้ยน

## วิธี rollback

**AI ห้าม rollback เองโดยเด็ดขาดไม่ว่ากรณีใด** (`.wyn/company/RULES.md`) ถ้าพัง ให้ Founder เลือก:

1. **เร็วที่สุด — Vercel Instant Rollback**: Vercel Dashboard → project "web" → Deployments →
   หา deployment ก่อนหน้า (จาก run #62, commit `a871a415`, 2026-09-04 12:42 UTC) → `⋯` → **Promote to Production**
   ใช้เวลาไม่กี่วินาที ไม่ต้อง build ใหม่
2. **ผ่าน CI**: `git revert -m 1 cb4d02d` บน `main` แล้ว push → รัน `deploy-web.yml` ใหม่ (ช้ากว่า ~5 นาที)

**คอลัมน์ในฐานข้อมูลไม่ต้อง rollback** — `drops.image_aspect_ratio` เป็น nullable additive
โค้ดเวอร์ชันเก่าไม่รู้จักมันและไม่แตะมันเลย ปล่อยทิ้งไว้ได้อย่างปลอดภัย ไม่ต้อง drop
(การ drop กลับจะทำให้ข้อมูลที่ผู้ใช้เลือกไว้หายถาวร)

## หมายเหตุ

- PR #225 ถูก merge ตอนที่ Flutter CI บน PR **ยังรันไม่จบ** แต่ CI บน `main` หลัง merge ผ่านครบ
  จึงถือว่าตรวจแล้ว
- QA รอบ 2 มีข้อจำกัดเรื่องความเป็นอิสระ — subagent QA ถูกตัดกลางคันเพราะชน rate limit
  ส่วนที่เหลือทำโดย session เดียวกับที่เขียนโค้ด (ระบุไว้ในรายงาน QA แล้ว)
- **หน้า Saved ยังวาด 4:5 ทุกรูป** — อยู่นอกขอบเขต WYN-109 ยังไม่ได้ทำ
- SCHEMA-004 (view `home_feed` บน production เพี้ยนจาก `schema.sql`) ยัง open แต่ไม่ block งานไหนแล้ว
