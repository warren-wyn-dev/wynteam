# Deployment Log — WYN-071 (WYNOS Visual Refresh) + WYN-064 (Home tab scroll) + WYN-065 (Profile UI polish) — merge complete, production deploy BLOCKED on missing secrets

```
Release: WYN-071 (WYNOS Visual Refresh -- Light theme, multi-image Drop, Profile Recommendation Section, Profile tab restructure, Search/Notification icons, haptics), WYN-064 (Home tab tap-to-scroll-to-top/refresh), WYN-065 (Profile screen UI polish -- full-width Edit button, AppBar icon spacing, Skeleton loading)
Version: N/A -- code-integration only, no build has successfully reached production yet (see Deployment Result)
QA Status: PASS for all three
  - WYN-071: independent QA PASS (`.wyn/tasks/approved/WYN-071-wynos-visual-refresh.md`)
  - WYN-064/WYN-065: QA PASS per their own task docs (`.wyn/tasks/approved/WYN-064-home-tab-scroll-refresh.md`, `.wyn/tasks/approved/WYN-065-profile-screen-ui-polish.md`), 768/768 at the time
  - Combined after merge: `flutter analyze` clean, `flutter test` 790/790 pass (see PR #179)
Build Status:
  - `flutter analyze`: 0 issues (verified locally on the merged branch before opening the PR)
  - `flutter test`: 790/790 pass (verified locally)
  - `flutter build web --release`: succeeded in CI on both of the prior workflow_dispatch runs against `main` (run 32786807409, 32787402382) -- the Flutter build itself is not the problem
Deployment Target: Vercel project "web" (production alias https://web-neon-sigma-66.vercel.app), via the new `.github/workflows/deploy-web.yml` manual GitHub Actions workflow (added in PR #177/#178 specifically so deploys no longer depend on a session's own sandbox having egress to api.vercel.com -- see the 2026-08-24 pending-tasks-merge-to-main log for why that was needed)
Changes: PR #179 (`claude/pending-tasks-ogs3jb` -> `main`) merged as commit `9c29346` -- `main` now contains all three tasks together, as Founder requested ("มีงานนี้ด้วยนะ จะได้ deploy พร้อมกัน")
Deployment Result: **NOT DEPLOYED -- blocked, code-integration only.**
  - Checked the workflow's run history before triggering a third attempt: both prior runs (triggered by Founder directly, 2026-08-24 22:53 and 23:01 UTC, against `main` @ `34794b7`) completed the full Flutter build successfully, then failed at the "Deploy to Vercel production" step with `Error: You defined "--token", but it's missing a value`.
  - Root cause, confirmed from the job logs: the workflow's `env:` block for that step printed `VERCEL_TOKEN: `, `VERCEL_ORG_ID: `, `VERCEL_PROJECT_ID: ` -- all three empty. **These GitHub Actions repository secrets are not set.**
  - Also noticed in the same logs: the `dart_define.json` written a few steps earlier had `"SUPABASE_URL": ""` and `"SUPABASE_PUBLISHABLE_KEY": ""` -- **these secrets are empty too.** Even if the Vercel step were fixed, this specific build would ship with no working Supabase connection (every network call in the app would fail against an empty base URL).
  - Did not re-trigger the workflow a third time against the newly-merged `9c29346` -- it would fail identically for the same reason, and running it again produces no new information, just a wasted ~2.5 min CI run.
  - Also checked whether this session could deploy directly (bypassing the missing-secrets Actions workflow) the way an earlier session did for WYN-056/057/058: `curl` to `api.vercel.com` returned 403 and to the Supabase project's own subdomain returned 404 from this session's own egress proxy -- both blocked, consistent with every session since the 2026-08-24 pending-tasks-merge-to-main log. Not attempted to route around per the proxy's own operating instructions.
Production Verification: N/A -- nothing new reached production this session. https://web-neon-sigma-66.vercel.app is still serving whatever the last *successful* deploy shipped (WYN-056/057/058, per the 2026-08-24 real-deploy log) -- WYN-062/063 and WYN-064/065/071 are merged to `main` but not yet live.
Rollback Plan: N/A -- no new deployment was made, so there is nothing to roll back on the Vercel side. Code-wise, `git revert` of merge commit `9c29346` on `main` would undo the code-integration step if ever needed, but that's unrelated to today's actual blocker.
```

## What's actually blocking production right now

Not code, not tests, not QA -- **GitHub Actions repository secrets were never populated** after the deploy workflow was added (PR #177/#178). Five secrets are needed under **Settings → Secrets and variables → Actions** on `warren-wyn-dev/wynteam`:

- `SUPABASE_URL`
- `SUPABASE_PUBLISHABLE_KEY`
- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

The Supabase project ref/publishable key are meant to be public (client-embedded either way), so low sensitivity. `VERCEL_TOKEN` is a real deploy credential -- same category as what was pasted in chat twice before and flagged for rotation; putting it in GitHub Secrets instead of chat is the more durable fix, since Actions secrets are encrypted at rest and never appear in logs (confirmed above -- the failed run's log printed empty strings, not the value).

## Next Steps

1. **Founder**: add the 5 secrets above in GitHub repo settings. `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` can be found under the "web" Vercel project's Settings → General; `VERCEL_TOKEN` is generated under the Vercel account's own Settings → Tokens; `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` are already visible in the Supabase project's API settings (same values already used in the last successful manual deploy).
2. Once set, this session (or any future one) can trigger `.github/workflows/deploy-web.yml` via `workflow_dispatch` on `main` and it should complete end-to-end -- the build half already proved out twice.
3. Native mobile (iOS/Android) build/distribution remains blocked on `google-services.json`/`GoogleService-Info.plist` + a distribution channel -- unchanged from every prior log.
