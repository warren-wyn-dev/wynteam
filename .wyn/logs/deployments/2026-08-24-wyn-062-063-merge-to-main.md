# Deployment Log — WYN-062/063 (V1.0 Beta UX/UI Fix & Unified Home Feed Algorithm) merged to `main`

```
Release: WYN-062 (WYNOS V1.0.0 Beta UX/UI Fix & Feature Update) + WYN-063 (Unified Home Feed Algorithm V1.0)
Version: N/A (no versioned build yet -- code-integration merge only, see Readiness Gate below)
QA Status: PASS
  - WYN-062: Independent QA (2026-08-24) -- flutter analyze clean, flutter test 743/743 pass (found and fixed 2 real bugs during QA: EditDropCaptionScreen letting an image-less Drop's caption be saved empty, DoubleTapLike's heart animation staying mounted-but-invisible after completing). Full report in .wyn/tasks/approved/WYN-062-v1-beta-ux-ui-update.md.
  - WYN-063: Self-QA (2026-08-24) -- flutter analyze clean, flutter test 759/759 pass. get_wynos_ranked_feed() SQL RPC additionally verified against a real local PostgreSQL 16 instance under RLS (not just read for correctness) -- 10/10 functional checks pass via the new permanent regression script supabase/tests/wyn_063_unified_home_feed_test.sh. Full report in .wyn/tasks/approved/WYN-063-unified-home-feed-algorithm.md.
Build Status:
  - flutter analyze (Flutter 3.47.1 stable, installed in-session -- no SDK preinstalled in this environment): 0 issues, both rounds.
  - flutter test: 759/759 pass (final count after both merges) -- 725 baseline (post WYN-056/057/058) + WYN-062's 18 new tests + WYN-063's 16 new tests.
  - Native flutter build apk/ipa: NOT attempted -- no Android SDK/Xcode in this sandbox (re-verified this session: `flutter doctor` shows Android toolchain and Chrome/Linux desktop targets all unavailable; only "Connected device" is a bare CLI target, not a real emulator/device).
  - GitHub PR checks: PR #168 (WYN-062) and PR #169 (WYN-063) both showed only Vercel/Netlify preview-deploy checks (unrelated to the Flutter app -- this repo also holds `admin/`, a separate Next.js app) -- Vercel failed both times on its own free-tier daily rate limit (`api-deployments-free-per-day`, not a code issue), Netlify preview built successfully both times.
Deployment Target: `main` branch on GitHub (warren-wyn-dev/wynteam) only -- a code-integration step, not a deploy to any live/production/user-facing environment. See Readiness Gate below for why a real production deploy is still not possible.
Changes:
  - PR #168 (`claude/wynos-v1-ux-ui-update-hd6t3k` -> `main`, merge commit `2d5bbc7`): WYN-062's 7 requirements -- Drop posts now accept image-only/caption-only/both (was image-required), Pop hidden from Profile UI (system untouched), double-tap-to-like with heart animation + duplicate-like guard, editable @username with live availability check, hashtag autocomplete with post counts in the composer. Home feed continuous-scroll/auto-refresh and Profile grid full-height scroll were audited and found already correct from prior work -- no code change needed for those two.
  - PR #169 (`claude/wynos-v1-ux-ui-update-hd6t3k` -> `main`, merge commit `e46810c`): WYN-063 -- new `get_wynos_ranked_feed()` SQL RPC computing a 6-component "Wynos Score" server-side (Personalized Interest 35% / Following 25% / Engagement 15% / Trending 10% / Recency 10% / Discovery 5%, all stored in a new `feed_ranking_config` table so weights are adjustable later without a code change), new `feed_signals` table capturing Hide/Profile-Visit user signals, and a client-side `feed_diversity.dart` pass capping same-author streaks and surfacing Discovery content periodically over the backend's already-ranked candidates.
  - Schema changes in this batch (both additive, no destructive migration): WYN-062 added the `drops_has_content` CHECK constraint (`image_url is not null or caption is not null`) plus an `edit_drop()` guard; WYN-063 added `feed_ranking_config`, `feed_signals`, `content_save_count()`, and `get_wynos_ranked_feed()`. None of these alter or drop any existing column/table.
Deployment Result: Both merged to `main` cleanly (no conflicts). `origin/main` verified at `9c17af6` after fetch (PR #170, an unrelated docs-only deployment log for WYN-056/057/058, merged immediately after and is not part of this batch).
Production Verification: Not applicable -- no production environment exists. Readiness Gate (unchanged since the last 20+ assessments, e.g. `.wyn/logs/deployments/2026-08-24-wyn-056-057-058-merge-to-main.md`) -- re-verified directly in this session, not assumed:
  - No real Supabase project -- `app/lib/core/env.dart` still reads `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` from `String.fromEnvironment(...)`, unset anywhere in the repo. No `supabase/config.toml` anywhere in the repo.
  - No native OAuth/Firebase config -- no `google-services.json`/`GoogleService-Info.plist` anywhere in the repo.
  - No distribution channel (TestFlight / Google Play Internal Testing / Firebase App Distribution) set up.
  - No CI pipeline -- `.github/workflows/` does not exist.
  - No Android SDK/Xcode in this sandbox -- `flutter doctor` confirms Android toolchain, Chrome, and Linux desktop targets are all unavailable; `flutter build apk`/`flutter build ipa` cannot be attempted here (this session did install the Flutter SDK itself for `analyze`/`test`, which is a different thing from a native platform toolchain).
  This is the same whole-project blocker documented in every prior deployment log, not specific to this batch. No new infra has appeared since the last assessment (2026-08-24, WYN-056/057/058).
Rollback Plan:
  - Code: `git revert` merge commit `e46810c` (WYN-063) and/or `2d5bbc7` (WYN-062) on `main` restores the pre-merge state. Reverting `2d5bbc7` alone would also need `e46810c` reverted first (WYN-063's `fetchRankedFeed()` rewrite depends on WYN-062's schema being present) to avoid a conflicting revert.
  - Database: both migrations are additive-only (new tables/constraint/functions, nothing altered or dropped) -- a revert would need a matching `DROP TABLE IF EXISTS feed_ranking_config, feed_signals` / `DROP FUNCTION IF EXISTS get_wynos_ranked_feed, content_save_count` / `ALTER TABLE drops DROP CONSTRAINT IF EXISTS drops_has_content` cleanup pass once a real database exists; nothing to roll back today since no production database exists yet.
  - Distribution: not applicable -- nothing has been distributed to any real device/store.
```

## Next Steps (Founder-only, per RULES.md's Founder-authority list — "โครงสร้างพื้นฐาน production")

Unchanged from every prior deployment log's Next Steps: a real Supabase project, native OAuth/Firebase config, a distribution channel, and either an Android SDK/Xcode-equipped environment or a CI pipeline are all still outstanding, all still Founder-gated. Nothing WYN-062/063-specific is needed beyond those — the code side is done, tested (including the new SQL against a real Postgres instance), and merged, waiting on infrastructure only Founder can provision.

**WYN-062 and WYN-063 are both code-complete and merged to `main`.** The full V1.0 Beta UX/UI spec (7 requirements) and the Unified Home Feed Algorithm are both live in the codebase, ready to ship the moment production infra exists.
