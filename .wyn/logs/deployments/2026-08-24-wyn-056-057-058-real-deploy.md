# Deployment Log — WYN-056/057/058 — FIRST REAL PRODUCTION DEPLOY

> อัปเดตต่อจาก `.wyn/logs/deployments/2026-08-24-wyn-056-057-058-merge-to-main.md` (ซึ่งบันทึกไว้ตอนที่ยังไม่มี Supabase project จริง) — Founder ส่ง credentials จริงมาระหว่าง session เดียวกัน ทำให้ deploy จริงขึ้น production ได้เป็นครั้งแรกในประวัติโปรเจกต์นี้

```
Release: WYN-056/057/058 -- Club Discovery Visual Refresh, Create Club + Club Page visual polish (Flutter Web build)
Version: build main.dart.js, built 2026-08-24 from main @ 92afeaa (release mode, tree-shaken icons)
QA Status: PASS (unchanged from the earlier log -- flutter analyze clean, flutter test 725/725 pass on the source this build compiled)
Build Status:
  - `flutter create . --platforms web` (regenerates the gitignored web/ scaffold, per app/.gitignore's own instructions -- flutter_bootstrap.js survived regeneration as intended)
  - `flutter build web --release --dart-define-from-file=dart_define.json` -- succeeded, 63.8s compile, build/web/ produced (42MB). dart_define.json (gitignored, never committed) held SUPABASE_URL/SUPABASE_PUBLISHABLE_KEY -- these are meant to be public/client-embedded (protected by RLS, not secrecy), unlike the other credentials Founder sent.
Deployment Target: Vercel project "web" (prj_bzoZIUdyxaRvXiSLG1uSfjDsyS5a, existing project -- linked via .vercel/project.json in the build output dir, not newly created), production alias https://web-neon-sigma-66.vercel.app
Changes: Same code changes as the merge-to-main log (PR #166, #167) -- this entry covers the *build+deploy* step only, no new code changes.
Deployment Result: `vercel deploy --prod` via the Vercel CLI (npx) succeeded. Deployment dpl_D3PCvMr3679du1tue4d9DfSJL1pi, readyState READY, target production, aliased to https://web-neon-sigma-66.vercel.app (the same URL Founder had already been using -- confirmed this is the correct/existing production project, not a stray new one).
Production Verification: Done for real, not "not applicable" -- first time in this project's history:
  - `curl https://web-neon-sigma-66.vercel.app/main.dart.js` downloaded and diffed byte-for-byte identical to the local `build/web/main.dart.js` -- the live site is serving exactly this build, not a stale one.
  - index.html / manifest.json / flutter.js all return HTTP 200 from the live URL.
  - Supabase REST API reachable from outside with the real publishable key: `GET /rest/v1/profiles?select=id&limit=1` -> `200 []` (table reachable, RLS/PostgREST working end-to-end, table legitimately empty -- fresh project, no real users yet).
  - Supabase Auth reachable and functional: `POST /auth/v1/signup` with an empty body (anonymous sign-in, which this app already relies on per `.wyn/company/DECISIONS.md`) returned a real signed JWT + session. **Side effect**: this created one throwaway row in `auth.users` (id `55c985dd-...`, is_anonymous: true, no email/phone/profile) -- did not create a `public.profiles` row (confirmed still empty after), so no user-visible pollution. Left as-is rather than attempting a service-role delete, since I was not given a service_role key and deleting auth users needs one.
  - Google OAuth: already configured and enabled on the Supabase project (`external_google_enabled: true`, client ID matches exactly what Founder sent) -- **this was already done before I touched anything**, not something I configured this session. Confirmed via the Supabase Management API, not assumed.
  - Browser-level smoke test (Playwright/Chromium, the kind that caught the WYN-005-era PostgREST embed-ambiguity bug in a prior session) was **attempted but not completed** -- this sandbox's egress proxy returned `ERR_CONNECTION_RESET` for Chromium's HTTPS traffic even with `--proxy-server`/`ignoreHTTPSErrors` configured (curl through the same proxy works fine; browser automation traffic apparently doesn't, and the proxy's own README lists "HTTP/2-only APIs"/some browser-automation-shaped traffic as a known unsupported class). Did not work around this (per the proxy README's explicit instruction not to route around unsupported traffic) -- flagging as a real gap: no confirmation yet that the compiled JS actually boots correctly in a real browser and calls Supabase successfully client-side, only that the raw HTTP endpoints (frontend assets + backend API) are independently reachable and correct.
Rollback Plan:
  - Immediate: `vercel rollback` (Vercel CLI) or re-deploy the previous `build/web/` output restores the prior live version in under a minute -- Vercel keeps deployment history (`deploymentsToKeep: 10` per the project's `deploymentExpiration` config).
  - Code: same as the merge-to-main log -- `git revert` the two merge commits on `main`, then rebuild+redeploy.
  - Database: no schema changes in this batch; the one throwaway anonymous auth user (noted above) is the only production data this session touched, and it carries no PII (no email/phone) and created no rows other users could ever see.
```

## Credentials handling (important — read before any future session touches this project)

Founder pasted the following directly into chat this session: Supabase project ref + publishable key (meant to be public, fine), **Supabase Personal Access Token** (account-level Management API access — broad), **database password** (direct Postgres superuser-equivalent access), **Vercel deploy token**, and **Google OAuth Client Secret**. None of these were committed to git (verified: `app/dart_define.json` and the equivalent hold only the two public values and are gitignored; nothing else was ever written to a tracked file). They were used only for this session's live API calls (Supabase Management API over HTTPS, Vercel CLI deploy) and are not persisted anywhere in this repository.

**Recommendation to Founder**: because these were shared in plaintext chat (which may be logged/retained per your own tooling, independent of anything this session did), consider rotating the Supabase Personal Access Token, the database password, and the Vercel deploy token after confirming this deploy looks right — regenerating them costs a few minutes and closes the exposure window. The Google OAuth Client Secret is lower-urgency (it was already configured server-side in Supabase before this session started) but the same logic applies if you want to be thorough.

## Next Steps

1. **Founder**: open https://web-neon-sigma-66.vercel.app and confirm the Club redesign looks right in a real browser (this session could not complete that check itself — see Production Verification above).
2. **Founder** (optional, recommended): rotate the 3-4 sensitive credentials listed above.
3. Native mobile (iOS/Android) build/distribution is still blocked on `google-services.json`/`GoogleService-Info.plist`, a distribution channel (TestFlight/Play Internal Testing), and either a CI pipeline or an Android SDK/Xcode-equipped environment — unchanged from every prior deployment log. The web deploy done today does not unblock native.
