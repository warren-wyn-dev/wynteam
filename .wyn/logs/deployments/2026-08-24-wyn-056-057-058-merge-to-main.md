# Deployment Log — WYN-056/057/058 (Club Discovery + Create Club + Club Page visual refresh) merged to `main`

```
Release: WYN-056/057/058 -- Club Discovery Visual Refresh, Create Club cover-picker polish, Club Page Join button hierarchy
Version: N/A (no versioned build yet -- code-integration merge only, see Readiness Gate below)
QA Status: PASS
  - WYN-056: Independent QA (2026-08-24) -- flutter analyze clean, flutter test 724/724 pass (found and fixed 2 real bugs during dynamic testing: a ClubRankedRow layout overflow, a PopupMenuButton generic-type mismatch). Full report in .wyn/tasks/approved/WYN-056-club-discovery-visual-refresh.md.
  - WYN-057/058: flutter analyze clean, flutter test 725/725 pass (full suite, no regressions; added coverage for the new cover-picker hint and the Join button's type/color per state). Full report in .wyn/tasks/approved/WYN-057-create-club-visual-refresh.md and WYN-058-club-page-visual-refresh.md.
Build Status:
  - flutter analyze (Flutter 3.47.1 stable, installed in-session -- no SDK was preinstalled in this environment): 0 issues, both rounds.
  - flutter test: 725/725 pass (final count after both PRs) -- 724 baseline + WYN-056's 7 new tests, then +1 more from WYN-057 (WYN-058 added assertions to existing tests, no new test count).
  - Native flutter build apk/ipa: NOT attempted -- no Android SDK/Xcode in this sandbox (see Readiness Gate).
  - GitHub PR checks: PR #166 (WYN-056) and PR #167 (WYN-057/058) both showed only Vercel/Netlify preview-deploy checks (unrelated to the Flutter app -- this repo also holds `admin/`, a Next.js app on the Phase 7 stack) -- all completed success/neutral, none failed.
Deployment Target: `main` branch on GitHub (warren-wyn-dev/wynteam) only -- a code-integration step, not a deploy to any live/production/user-facing environment. See Readiness Gate below for why a real production deploy is still not possible.
Changes:
  - PR #166 (`claude/wynos-mobile-ui-design-caztyr` -> `main`, merge commit `deb0b9f`): WYN-056 (ExploreClubsScreen redesign: hero block, "Club แนะนำสำหรับคุณ"/ranked "กำลังนิยม" rows, 2-column grid, new ClubRecommendedCard/ClubRankedRow/ClubJoinButton widgets, ClubRepository.fetchPendingClubIds, Bottom Nav Drop button glow) + a same-day Founder follow-up (removed the Club profile-icon upload from Create/Edit Club, cover image only) + a roadmap doc fix (Phase 8 task numbers shifted to avoid colliding with WYN-056).
  - PR #167 (`claude/wynos-mobile-ui-design-caztyr` -> `main`, merge commit `92afeaa`): WYN-057 (CreateClubScreen cover picker: dashed border via new DashedRectBorderPainter, camera icon in a circle, "แนะนำอัตราส่วน 16:9" hint) + WYN-058 (ClubPage header Join button: "เข้าร่วม" state elevated to FilledButton/full cyan fill; "รออนุมัติ" recolored from primary to outline to match "เข้าร่วมแล้ว", since both are disabled/non-actionable) + a second roadmap numbering fix (Phase 8 shifted again to WYN-059/060/061).
  - No schema/RLS/database changes in any of the three tasks -- verified by diff (no `supabase/schema.sql` touched).
Deployment Result: Both merged to `main` cleanly (no conflicts). `origin/main` verified at `92afeaa` after fetch, matching local. `main` previously sat at `590499c` (the WYN-053/054/055 Phase-7-complete merge).
Production Verification: Not applicable -- no production environment exists. Readiness Gate (unchanged since the last 20+ assessments, e.g. `.wyn/logs/deployments/2026-08-23-wyn-043-merge-to-main.md`) -- re-verified directly in this session, not assumed:
  - No real Supabase project -- `app/lib/core/env.dart` still reads `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` from `String.fromEnvironment(...)`, unset anywhere in the repo. No `supabase/config.toml`.
  - No native OAuth/Firebase config -- no `google-services.json`/`GoogleService-Info.plist` anywhere in the repo.
  - No distribution channel (TestFlight / Google Play Internal Testing / Firebase App Distribution) set up.
  - No CI pipeline -- `.github/workflows/` does not exist.
  - No Android SDK/Xcode in this sandbox -- `flutter build apk`/`flutter build ipa` cannot be attempted here (this session did install the Flutter SDK itself for `analyze`/`test`, which is a different thing from a native platform toolchain).
  This is the same whole-project blocker documented in every prior deployment log, not specific to this batch. No new infra has appeared since the last assessment (2026-08-23).
Rollback Plan:
  - Code: `git revert` merge commit `92afeaa` (WYN-057/058) and/or `deb0b9f` (WYN-056) on `main` restores the pre-merge state. Reverting `deb0b9f` alone would also need `92afeaa` reverted first (WYN-057/058 built on top of WYN-056's redesigned files) to avoid a conflicting revert. Reverting either returns `ExploreClubsScreen`/`CreateClubScreen`/`ClubPage`/`RootShell`'s Drop button to their pre-refresh state -- a cosmetic regression, not a functional break (no data model or RLS was touched, so nothing to "lose" by reverting).
  - Database: no changes in this batch -- nothing to roll back.
  - Distribution: not applicable -- nothing has been distributed to any real device/store.
```

## Next Steps (Founder-only, per RULES.md's Founder-authority list — "โครงสร้างพื้นฐาน production")

Unchanged from every prior deployment log's Next Steps: a real Supabase project, native OAuth/Firebase config, a distribution channel, and either an Android SDK/Xcode-equipped environment or a CI pipeline are all still outstanding, all still Founder-gated. Nothing WYN-056/057/058-specific is needed beyond those — the code side is done and merged, waiting on infrastructure only Founder can provision.

**Club (WYN-014/015/017/056/057/058) is now feature-and-visual-complete on `main`** — the three screens from the Founder's mockup (CLUB/Explore, Create Club, Club Page) are all redesigned, QA-passed, and merged. Nothing else in Club is queued.
