# Deployment Log — WYN-026 (Report) + WYN-027 (Block) + WYN-028 (Mute)

Release: code-integration merge to `main` (not a production/end-user deploy — see Readiness Gate below)
Date: 2026-08-22

## QA Status

**PASS** for all three tasks, each independently re-verified (not just Coding's own numbers):
- WYN-026: QA round 1 PASS — `.wyn/tasks/approved/WYN-026-report-system.md`
- WYN-027: QA round 1 PASS — `.wyn/tasks/approved/WYN-027-block-system.md`
- WYN-028: QA round 1 PASS, no findings — `.wyn/tasks/approved/WYN-028-mute-system.md`
- Final `flutter analyze`: 0 issues, `flutter test`: 395/395 pass (feature branch HEAD before merge)

## Build Status

Re-verified independently before merge (feature branch `claude/phase-1-safety-trust-481y98` HEAD):
- `flutter analyze`: 0 issues
- `flutter test`: 395/395 pass
- Fast-forward-eligible merge into `main` (merge-base = `main` HEAD before merge, `main` had not moved since branching, no conflicts)

## Deployment Target

**`main` branch on GitHub only** (`warren-wyn-dev/wynteam`). This is a code-integration step, not a deploy to any live/production/user-facing environment — see Readiness Gate below for why a real production deploy is not currently possible.

## Changes

11 commits, 41 files changed (+4464/-68) across WYN-026/027/028:
- **WYN-026 (Report)**: `reports` table + `submit_report()` RPC, `ReportRepository`/`ReportSheet` reused across 7 entry points (Profile/Drop Detail/Home/Search/Club/Club Post/comments)
- **WYN-027 (Block)**: `blocks` table + `block_user()`/`unblock_user()`/`block_relationship()` RPCs, block-aware RLS on Drop/Pop/Club Post SELECT and Like/Comment/Follow/Mention INSERT policies, `BlockRepository`/`BlockedListScreen`/`SettingsScreen` (new), Blocked persona on `ViewProfileScreen`. Fixed a genuine RLS self-referential-trap bug found via live-Postgres testing (see `.wyn/company/DECISIONS.md`, 2026-08-22 "WYN-027 ... AI Coding เจอและแก้บั๊ก").
- **WYN-028 (Mute)**: `mutes` table (no RPC needed), `home_feed` view gets a `not exists` mute filter (scoped to Home Feed/Trending only, does not touch Search/Club/Profile), plain toggle in `ViewProfileScreen`'s More menu, `MuteRepository`/`MutedListScreen`. Also fixed a testability gap WYN-027 QA had flagged (`ViewProfileScreen`'s report/block repositories were hardcoded instead of injectable).

Full history: `.wyn/tasks/approved/WYN-026-report-system.md`, `.wyn/tasks/approved/WYN-027-block-system.md`, `.wyn/tasks/approved/WYN-028-mute-system.md`

## Deployment Result

**Merged to `main` via PR #131 (merge commit, not squash/rebase), pushed successfully.** `origin/main` now at commit `38ced04`, verified matching local `main` after fetch. All 5 CI checks on the PR were green (Vercel × 4 deploy previews, Netlify deploy preview) before merging.

## Production Verification

**Not applicable — no production environment exists.** Readiness Gate (unchanged since the last assessment, `.wyn/logs/deployments/2026-08-13-wyn-002-readiness.md`):
- No real Supabase project — `app/lib/core/env.dart` reads `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` from `--dart-define`, currently unset anywhere in the repo (correctly — never hardcode real credentials)
- No native OAuth/Firebase config
- No distribution channel (TestFlight / Google Play Internal Testing / Firebase App Distribution) set up
- This session's environment has a working Flutter SDK (enables `flutter analyze`/`flutter test`) but no Android SDK/Xcode for a real `flutter build apk`/`flutter build ipa`

This joins the same "approved, waiting for real infra" queue as every prior approved task (WYN-005 through WYN-024/DS-009) — none of this has ever been task-specific; it is a whole-project blocker that needs Founder action.

## Rollback Plan

- **Code**: `git revert` the merge commit `38ced04` on `main` restores the pre-merge state exactly (or revert the individual commit range `3a2da1a..9681643` if a partial rollback is ever needed — no destructive history rewrite required)
- **Database**: schema changes exist this time (`reports`, `blocks`, `mutes` tables + several RLS policy replacements + `home_feed` view redefinition) — but since no real Supabase project exists yet, none of this has been applied to any live database. When a real project exists, `supabase/schema.sql` should be applied fresh (it is idempotent via `create table if not exists`/`create or replace`); rolling back would mean dropping those three new tables and reverting the touched RLS policies/view to their pre-WYN-026 shape
- **Distribution**: not applicable — nothing has been distributed to any real device/store

## Next Steps (Founder-only, per RULES.md's Founder-authority list — "โครงสร้างพื้นฐาน production")

Unchanged from every prior deployment log in this project:
1. Create a real Supabase project, run `supabase/schema.sql` against it
2. Set up native OAuth Client ID (Google) + Apple Developer account (Apple Sign-In capability)
3. Add real `google-services.json`/`GoogleService-Info.plist`
4. Choose and configure a distribution channel (TestFlight / Play Internal Testing / Firebase App Distribution)
5. Provide an environment with Android SDK/Xcode for real `flutter build` release artifacts, or a CI pipeline that has them

AI Deploy & DevOps can execute steps 2-5's technical configuration once step 1's account/project exists; step 1 itself requires Founder action (real account, billing).
