# Deployment Log — WYN-026 (Report) + WYN-027 (Block) + WYN-028 (Mute) — Phase 1 Safety & Trust Foundation

Release: code-integration merge to `main` (not a production/end-user deploy — see Readiness Gate below)
Date: 2026-08-22

## QA Status

**PASS** — all three tasks passed a genuine independent QA cycle (separate session from Coding, not self-QA):

- `.wyn/tasks/approved/WYN-026-report-system.md` — QA round 1 (self-QA) PASS, then **Independent QA — Round 1 PASS confirmed** (14/14 SQL cases, independent fixtures)
- `.wyn/tasks/approved/WYN-028-mute-system.md` — QA (self-QA) PASS, then **Independent QA — Round 1 PASS confirmed** (13/13 SQL cases, independent fixtures)
- `.wyn/tasks/approved/WYN-027-block-system.md` — QA (self-QA) PASS, then **Independent QA — Round 1 FAIL (Major)**: a SECURITY DEFINER helper function (`is_blocked_either_way`, plus 4 sibling author-lookup helpers) that RLS policies rely on internally was left executable directly via Supabase RPC by any authenticated client (no `revoke execute`), leaking block relationships between arbitrary users who had never interacted with each other. Full report: `.wyn/tasks/bugs/WYN-027-is-blocked-either-way-rpc-exposure.md` (now closed).
  - AI Debug Engineer fixed it (commit `1d2fc70`): moved the 5 helper functions from `public` to a new `internal` schema that PostgREST does not expose, updated 23 call sites.
  - **Independent QA — Round 2 PASS**: re-verified the fix independently (fresh Postgres DB, fresh 87-case SQL harness re-run with 0 regressions, fresh probe scripts not reused from Debug, confirmed `public.is_blocked_either_way` no longer resolves, confirmed `anon` role still gets `permission denied for schema internal`, confirmed the residual `authenticated`-role SQL-level access to `internal` is expected/required for RLS to function and is not reachable by any real end user because PostgREST only routes to schemas in `exposed_schemas` — default `public` only).

`flutter analyze`/`flutter test` independently re-verified multiple times across both QA rounds: 0 issues, 395/395 tests passing, no regressions.

## Build Status

Re-verified independently at the new `main` HEAD (post-merge, not just the feature branch's own numbers), using Flutter 3.47.1 (stable) from `/home/user/flutter`:

- `flutter pub get`: clean (21 packages have newer versions available but none are blocking; no resolution errors)
- `flutter analyze`: **0 issues**
- `flutter test`: **395/395 pass** (0 failures; the `NetworkImageLoadException` lines seen mid-run are expected test-environment noise from stubbed image URLs, not failures)
- Merge method: **fast-forward** (`claude/phase-1-safety-trust-481y98-imed6c` → `main`, commit `3a2da1a..c7f06f1`, no conflicts). Verified before merging that `main` (`origin/main` at `38ced04`) is a strict ancestor of the feature branch tip (`c7f06f1`) and that local `main` had not diverged — `git merge-base --is-ancestor` confirmed both directions before running `git merge --ff-only`.

## Deployment Target

**`main` branch on GitHub only** (`warren-wyn-dev/wynteam`). This is a code-integration step, not a deploy to any live/production/user-facing environment — see Readiness Gate below for why a real production deploy is not currently possible.

## Changes

46 files changed (5266 insertions, 68 deletions) merged into `main` (`38ced04..c7f06f1`):

- **WYN-026 (Report System)**: universal Report flow reusable across User/Drop/Comment/Club/Club Post (Message target reserved for Phase 2 Chat, no UI yet) — `report_category.dart`, `report_target_type.dart`, `report_repository.dart`, `report_sheet.dart`, `reports` table + `submit_report()` security-definer RPC in `schema.sql`, self-report guard, duplicate-open-report guard, "Other" category requires free-text detail, reporter privacy (reported party never sees who reported them).
- **WYN-027 (Block System)**: mutual block enforcement at the RLS layer (not client-side filtering) across `drops`/`pops`/`drop_comments`/`pop_comments`/`club_posts`/`club_post_comments` content visibility, `drop_likes`/`drop_comments`/`drop_comment_likes`/`pop_likes`/`pop_comments`/`pop_comment_likes`/`club_post_likes`/`club_post_comments` interaction blocking, Follow relationship auto-removed both directions on block, Follow blocked while a block is active, Mention silently no-ops without notification — `block_relationship.dart`, `block_repository.dart`, `block_dialogs.dart`, `blocked_list_screen.dart`, `blocks` table + `block_user()`/`unblock_user()`/`block_relationship()` RPCs, plus the `internal` schema fix from the security round.
- **WYN-028 (Mute System)**: one-directional, silent feed-hiding — filters only the `home_feed` view (and by extension Trending, which reads the same view); explicitly does **not** touch Search/Profile/Club/Follow/Like/Comment/Mention/Notification — `mute_repository.dart`, `muted_list_screen.dart`, `mutes` table with simple RLS (no RPC needed, no atomic side effects).
- **Shared**: new `SettingsScreen` (Settings → Safety, houses both Blocked List and Muted List), `ViewProfileScreen` More menu gains Report/Block/Mute entries plus a "blocked persona" banner state, `HashtagText` checks block relationship before navigating to a mentioned profile, `ReportRepository`/`BlockRepository`/`MuteRepository` all made constructor-injectable on `ViewProfileScreen`/`report_sheet.dart` (a testability fix that also closed a QA-round-1 finding on WYN-027 about untestable More-menu state).
- Backlog additions for the next Phase 1 items: `.wyn/tasks/backlog/WYN-029-moderation-queue.md`, `.wyn/tasks/backlog/WYN-030-appeal-system.md`.

Full history: `.wyn/tasks/approved/WYN-026-report-system.md`, `.wyn/tasks/approved/WYN-027-block-system.md`, `.wyn/tasks/approved/WYN-028-mute-system.md`, `.wyn/tasks/bugs/WYN-027-is-blocked-either-way-rpc-exposure.md` (closed)

## Deployment Result

**Merged to `main`, pushed successfully.** `origin/main` now at commit `c7f06f1`, verified matching local `main` after push (`git fetch` + `git rev-parse origin/main main` both returned `c7f06f1`). This brings the WYN-027 security fix (`1d2fc70`) and both rounds of independent QA documentation onto `main` for the first time — `main` previously sat at `38ced04`, the pre-fix merge commit, which contained the vulnerable `is_blocked_either_way` RPC exposure.

## Production Verification

**Not applicable — no production environment exists.** Readiness Gate (unchanged since the last several assessments, e.g. `.wyn/logs/deployments/2026-08-13-wyn-002-readiness.md`, `.wyn/logs/deployments/2026-08-22-wyn-024-ds-009-merge-to-main.md`) — re-verified directly in this session, not assumed:

- **No real Supabase project** — `app/lib/core/env.dart` reads `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` from `String.fromEnvironment(...)` (compile-time `--dart-define`), currently unset anywhere in the repo (correctly — never hardcode real credentials). No `supabase/config.toml` exists either (QA round 2 flagged this as a Minor defense-in-depth recommendation for whenever a real project is created — see "Next Steps" below).
- **No native OAuth/Firebase config** — searched the whole repo for `google-services.json`/`GoogleService-Info.plist`: none found. `Firebase.initializeApp()` in `main.dart` remains wrapped in a silent try/catch specifically because there's no real Firebase project config yet.
- **No distribution channel** (TestFlight / Google Play Internal Testing / Firebase App Distribution) set up.
- **No Android SDK/Xcode in this sandbox** — this session's environment has a working Flutter SDK (found at `/home/user/flutter`, version 3.47.1 stable, confirmed by running `flutter analyze`/`flutter test` successfully) but `flutter build apk`/`flutter build ipa` cannot be attempted (no Android SDK, no Xcode). No CI/build-tooling config (`.github/workflows/`, etc.) exists in the repo either.

This is now the **sixteenth+ approved batch** in this project's history to reach this exact same gate (WYN-005/006/007/008/009/012/013/014/015/019-022/ZOKY-001-004/SELLER-001+/WYN-024/DS-009/**WYN-026/027/028**) — all "approved, merged to `main`, waiting for real infra." None of this has ever been a WYN-026/027/028-specific blocker; it is a whole-project blocker that needs Founder action. No new infra has appeared since the last assessment — verified directly rather than assumed.

## Rollback Plan

- **Code**: `git revert c7f06f1..38ced04` on `main` (i.e. revert the 4-commit range `d3db8b1..c7f06f1`, or revert the fast-forward range as a whole) restores the pre-merge state exactly — no destructive history rewrite needed since this was a fast-forward, not a squash/rebase. Note: reverting would re-introduce the WYN-027 RPC-exposure vulnerability that Round 2 QA closed, so a full revert is discouraged; if a partial rollback is ever needed, revert only from `main`'s tip backward to no further than `38ced04` and never ship `38ced04`'s state to any real backend.
- **Database**: `supabase/schema.sql` grew by the `reports`, `blocks`, `mutes` tables, their RLS policies, the `submit_report()`/`block_user()`/`unblock_user()`/`block_relationship()` RPCs, the `internal` schema + 5 helper functions, and a `home_feed` view redefinition (to add the Mute filter). Since there is no live database yet, rollback here simply means "don't apply this `schema.sql` version" — no live migration to reverse. Once a real Supabase project exists, standard practice will be to version schema changes as separate migration files rather than one monolithic `schema.sql` apply, so a real rollback path exists in production.
- **Distribution**: not applicable — nothing has been distributed to any real device/store.

## Next Steps (Founder-only, per RULES.md's Founder-authority list — "โครงสร้างพื้นฐาน production")

To make an actual production deploy possible for WYN-026/027/028 (and everything already queued behind this same gate):

1. **Create a real Supabase project** (requires Founder: real account, billing) and run `supabase/schema.sql` against it.
   - While setting it up, also create `supabase/config.toml` with `[api] schemas = ["public"]` explicit (a Minor recommendation from WYN-027's Round 2 QA — the RPC-exposure fix relies on `internal` never being added to PostgREST's exposed-schemas list; today that's an unwritten assumption since no `config.toml` exists yet, and codifying it removes the risk of someone accidentally re-opening the same class of leak later, e.g. during WYN Admin/Phase 7 backend work).
2. **Set up native OAuth Client ID (Google) + Apple Developer account** (Apple Sign-In capability) — required since WYN-002, still outstanding.
3. **Add real `google-services.json`/`GoogleService-Info.plist`** (unblocks WYN-016 Push Notifications too).
4. **Choose and configure a distribution channel** (TestFlight / Play Internal Testing / Firebase App Distribution) — Founder previously approved "Internal Testing" as the target (see `.wyn/company/DECISIONS.md`, 2026-08-13).
5. **Provide an environment with Android SDK/Xcode** for real `flutter build` release artifacts, or a CI pipeline that has them.

AI Deploy & DevOps can execute steps 2-5's technical configuration once step 1's account/project exists; step 1 itself requires Founder action (real account, billing) and cannot be done by the AI team per `.wyn/company/RULES.md`.
